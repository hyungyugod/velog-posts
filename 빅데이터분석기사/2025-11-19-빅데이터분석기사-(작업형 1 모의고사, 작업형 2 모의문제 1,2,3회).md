# 📌 1. 모의고사 작업형 1
## 1-1. 1번 문제
- 앞에서 부터 50%까지의 데이터에서 'target'컬럼이 0 값을 가진 데이터만 활용해 'proline'컬럼의 평균을 구하시오 (소수점 절사(버림), 정수형 출력)
- 정확히 몇 퍼센트로 잘라낼때는 len(a)*50//100 이런식으로 하면 정확하다.
- iloc과 loc은 “함수”가 아니라 “인덱서(indexer) 객체"다 즉 함수처럼 느끼지만, 실제로는 메소드 호출이 아니라 속성 접근이다.
- 즉 아래와 같이 속성으로 접근하면 해당위치로 연결해주는 생성자를 호출하는 것과 같다.
```py
a.iloc[0]        # __getitem__(0) 호출
a.iloc[0, 1]     # __getitem__((0,1)) 호출
a.loc['row']     # __getitem__('row') 호출
```
```py
a = a.iloc[0:int(len(a)*50//100),:] # 50% 잘라내기
cond = a['target'] == 0 # 타겟이 0인 조건
m = a[cond]['proline'].mean() # 해당 조건을 만족하는 dataframe에서 'proline' 칼럼의 평균
answer = int(m) # 정수변환
print(answer)
```
- a[:숫자]는 열(column) 접근이 아니라 행(row) 슬라이스다
- a['col'] 문자열이면 → 열 이름 접근 (이때는 딕셔너리처럼 동작하는 것.)
- a[숫자] 숫자를 직접 쓰면 → 열 번호 접근도 행 번호 접근도 아님 → 에러
- 하지만 슬라이스 a[:숫자]는 예외적으로 ‘행 슬라이스’로 동작 
- 즉 첫줄을 아래처럼 써도 됨
- 이때 소숫점이 나왔을때는 그냥 절삭하면 딱 그만큼의 퍼센트가 된다. 즉 그냥 int로 절삭해도 된다.
```py
a = a[:int(len(a)/2)]
```

## 1-2. 2번 문제
- s1, s2, s3, s4, s5, s6, 컬럼의 행(row)별 합을 구하고 그 합이 0.1보다 큰 값의 수를 구하시오
- print(sum(a.sum(axis=1)>0.1)) # 열방향합이 0.1보다 큰 데이터들의 합
```py
# 사용자 코딩
cols = ['s1', 's2', 's3', 's4', 's5', 's6'] # 해당하는 columns만 선택
a = a[cols] # 해당 컬럼들에 해당하는 데이터프레임으로 덮어씀
print(sum(a.sum(axis=1)>0.1)) # 열방향합이 0.1보다 큰 데이터들의 합
```

## 1-3. 3번 문제
- 고유한 값이 가장 많은 컬럼의 이름은?
- a.nunique() # a.nunique() 는 DataFrame의 각 열(column)마다 “고유한(unique) 값이 몇 개 있는지” 세어서 Series로 반환
```py
a = a.nunique() # a.nunique() 는 DataFrame의 각 열(column)마다 “고유한(unique) 값이 몇 개 있는지” 세어서 Series로 반환
print(a.sort_values(ascending=False).index[0]) # 현재 인덱스가 칼럼명이므로 내림차순 정렬 후 가장 위에 있는 인덱스가 해당하는 칼럼의 이름이 된다.
```

# 📌 2. 작업형 2 모의문제 1 (이진분류)
## 2-1. 데이터 전처리 : 결측치, 이상치, 인코딩, 스케일링
- 결측치는 isna().sum()으로 이상치는 describe()로 확인
- 해당 문제는 결측치가 존재하지 않아서 우선 인코딩함
- 인코딩은 baseline(아예 문자형을 지워버림), onehot인코딩, label인코딩 시도하기
- one-hot encoding -> 근거는 obj_cols의 colums 적용하기 전의 nunique값이 작은지 한번 확인은 해보기
- 우선 문자형 컬럼들만 분리 -> 문자형만 하려면 include 뺀 나머지 하려면 exclude하면된다.
- colums만 넣어야 하는것 주의하자
- 또한 설계변수는 과적합의 우려가 있어서 일단 학습에선 제외하고 답지에 포함해야하면 pop을 통해 test_id 변수에 따로 저장해둔다.
```py
cols = train.select_dtypes(include='object').columns
cols

train = train.drop('CLIENTNUM', axis=1)
test_id = test.pop('CLIENTNUM')
```
- 구조는 아래처럼 하나씩 하면서 주석으로 막고 하면된다. -> 변수명은 동일한거 사용하기
- 삭제 전후에는 print문 찍어보기
-  LabelEncoder의 fit_transform() 은 Series를 매개변수로 받아서, numpy 배열(ndarray)을 반환한다. 그러나 pandas는 numpy array를 특정 열에 대입하면 자동으로 Series로 변환해준다.
```py
# baseline
# print(train.shape, test.shape)
# train = train.drop(cols, axis=1)
# test = test.drop(cols, axis=1)
# print(train.shape, test.shape)


# label
from sklearn.preprocessing import LabelEncoder

for col in cols:
    le = LabelEncoder()
    train[col] = le.fit_transform(train[col])
    test[col] = le.transform(test[col])

train[cols].head()


# # one-hot
# train = pd.get_dummies(train, columns=cols)
# test = pd.get_dummies(test, columns=cols)
print(cols)
```

## 2-2. 검증데이터 분리
- 여기서 기존의 train은 변형하지 말고 drop을 통해 타겟데이터만 분리하는게 더 낫다.
```py
from sklearn.model_selection import train_test_split

X_tr, X_val, y_tr, y_val = train_test_split(
    train.drop('Attrition_Flag', axis=1), train['Attrition_Flag'], test_size=0.2, random_state=2022
)
```

## 2-3. 모델 학습 및 평가
- 모델은 lightgbm과 randomforest를 사용해서 검증해본다.
- 둘다 import에 뒤에 Classifier 혹은 Regressor로 
- 아래는 lightgbm이다. 
```py
from lightgbm import LGBMClassifier # 임포트문 외우기
lgbm = LGBMClassifier(random_state=42)
lgbm.fit(x_train, y_train)
lgbm_val_pred = lgbm.predict(x_val)
lgbm_val_pred_proba = lgbm.predict_proba(x_val)
```
- 아래는 RandomForest이다.
```py
from sklearn.ensemble import RandomForestClassifier
rf = RandomForestClassifier(random_state=42)

rf.fit(x_train, y_train)

rf_val_pred = rf.predict(x_val)
rf_val_pred_proba = rf.predict_proba(x_val)
```
- 아래는 metrics에 포함된 분류모델의 평가지표들이다.
- sklearn 문서에서 명시적으로 이렇게 설명돼 있다. 모든 metric signature(함수 형태)는 가능하면 다음 형태를 따른다: metric(y_true, y_pred, **kwargs)
- 즉:
- 첫 번째 인자 = 정답
- 두 번째 인자 = 예측값
```py
from sklearn.metrics import accuracy_score, precision_score, recall_score, f1_score, roc_auc_score

# 정확도
print(accuracy_score(y_val, pred))

# 정밀도
print(precision_score(y_val, pred))

# 재현율 (민감도)
print(recall_score(y_val, pred))

# F1
print(f1_score(y_val , pred))

# baseline
# 0.9666872301048736
# 0.9444444444444444
# 0.8435114503816794
# 0.8911290322580645

# label
# 0.9685379395434917
# 0.948936170212766
# 0.851145038167939
# 0.89738430583501
```
- roc_auc_score의 경우에는 문제에서 요구하는 값 중 0 or 1의 값을 선택하여 제시해야한다.
```py
# roc-auc
pred = model.predict_proba(X_val)
print(roc_auc_score(y_val, pred[:,1]))
# baseline
# 0.9894048160692921

# label
# 0.9899707351049547
```
- 이렇게 머신에 classes_를 붙여서 어떤 순서로 라벨링 되어있는지 확인할 수 있다.
```py
lgbm.classes_
```

## 2-4. 예측 및 제출
- 위에서 가장 맞는 인코딩 + 머신을 선택하여 해당 머신으로 예측 후 제출한다.
```py
pred = model.predict_proba(test)
```
- 제출시 series나 배열을 합쳐서 dataframe으로 만든다. 이때 아까 빼두었던 설계변수도 앞에 붙여준다.(필요할 경우)
```py
submit = pd.DataFrame({
    'CLIENTNUM':test_id,
    'Attrition_Flag':pred[:,1]
})
```
- 요구하는 제출 양식에 맞추어 csv파일을 제출한다. 이때 index=False를 꼭 붙이는 것을 잊으면 안된다.
```py
submit.to_csv('00000.csv', index=False)
```

# 📌 3. 작업형 2 모의문제 2 (회귀)
## 3-1. 데이터 전처리
- 아래를 통해 데이터 수와 비슷하게 다양한 값을 갖는 값들이나 답지에 제출하지 않을 설계변수는 제외한다.
- 또한 답안에 제출할 설계변수 id는 따로 보관해둔다.
- 이후 남은 하나의 결측치를 0으로 대체한다. 물론 정확하게 칼럼이름을 찍어서 대치하는것이 더 좋다.
```py
# 결측치 칼럼과 다른 칼럼 중 학습에 필요없는 칼럼 삭제
drop_cols = ['name', 'host_name', 'host_id', 'last_review']
train = train.drop(drop_cols, axis=1)
test = test.drop(drop_cols, axis=1)

# 설계변수 id 삭제, test는 삭제 후 따로 칼럼에 보관
train = train.drop('id', axis=1)
id_test = test.pop('id')

# 결측치 대치 -> 아마 리뷰는 없어서 결측치인거 아닐까라는 생각으로 0으로 대치
train = train.fillna(0)
test = test.fillna(0)
```
- 다 해보면 좋지만 원핫인코딩이 너무 칼럼수가 많아질 것 같아 제외한다.
- obj_cols = train.select_dtypes(include='object').columns에서 뒤에 columns 붙는거 항상 주의해야 한다.
```py
# 인코딩
obj_cols = train.select_dtypes(include='object').columns

# baseline
# print(train.shape)
# train = train.drop(obj_cols,axis=1)
# test = test.drop(obj_cols,axis=1)
# print(train.shape)

# onehot
# train = pd.get_dummies(train, columns = obj_cols)
# test = pd.get_dummies(test, columns = obj_cols)

# labelencoding
from sklearn.preprocessing import LabelEncoder
for col in obj_cols:
  le = LabelEncoder()
  train[col] = le.fit_transform(train[col])
  test[col] = le.transform(test[col])
```

## 3-2. 검증 데이터 분리
- 확실히 데이터를 분리할때 타겟을 고려하는 것이 깔끔한 것 같다.
```py
from sklearn.model_selection import train_test_split
x_train, x_val, y_train, y_val = train_test_split(train.drop('price',axis=1), train['price'], train_size=0.8, random_state=42)
print(x_train.shape, x_val.shape, y_train.shape, y_val.shape)
```

## 3-3. 모델학습 및 평가
- 변수를 모델로 통일하고 같은 변수를 쓰면서 주석으로 막으면서 하는게 좋은 것 같다.
- lightgbm과 randomforest를 사용하되 classifier와 regressor만 구분하면 된다.
```py
# lightgbm
# from lightgbm import LGBMRegressor
# model = LGBMRegressor(random_state=42)
# model.fit(x_train, y_train)
# pred = model.predict(x_val)

# randomforest
from sklearn.ensemble import RandomForestRegressor
model = RandomForestRegressor(random_state=42)
model.fit(x_train,y_train)
pred = model.predict(x_val)
```
- 아래는 회귀분석의 주요 평가지표들로 r2_score, mean_absolute_error, mean_squared_error가 기본이고 나머지는 np로 계산해야 한다. 하지만 굳이 계산할 줄 몰라도 문제를 푸는데 큰 지장은 없다.
```py
import numpy as np
from sklearn.metrics import r2_score, mean_absolute_error, mean_squared_error
def rmse(y_val, pred): #RMSE
    return np.sqrt(mean_squared_error(y_val, pred))

def rmsle(y_val, pred): #RMSLE
    return np.sqrt(np.mean(np.power(np.log1p(y_val) - np.log1p(pred), 2)))

def mape(y_val, pred): #MAPE
    return np.mean(np.abs((y_val - pred) / y_val)) * 100

    print(r2_score(y_val, pred))
print(mean_absolute_error(y_val, pred))
print(mean_squared_error(y_val, pred))
print(rmse(y_val, pred))
print(rmsle(y_val, pred))
print(mape(y_val, pred))
```

## 3-4. 예측 및 제출
- result.to_csv('0000.csv', index=False)를 또 까먹을 뻔 했다.
```py
pred1 = model.predict(test)
result = pd.DataFrame({
    'id': id_test,
    'pred':pred1
})
result.to_csv('0000.csv', index=False)
```

# 📌 4. 작업형 2 모의문제 3 (회귀-하이퍼파라미터 튜닝)
## 4-1. 데이터 전처리
- 아래 전처리 결과 결측치, 인코딩, 이상치 없음 -> 설계변수만 제거
```py
# 결측치, 인코딩, 이상치 없음
train.info()
train.head()
train.isna().sum()
train.describe()

# id 분리
train = train.drop('id', axis=1)
test_id = test.pop('id')
```

## 4-2. 모델 학습 및 평가
- 검증 데이터 분리는 위에서 했던 것들과 같아서 생략함'
- 하이퍼 파라미터 설정은 아래와 같음
- lightgbm -> max_depth = 3~12 , n_estimator = 100~1000, learning_rate = 0.1 ~0.01 -> 이때 n_estimator가 오르면 learning_rate는 내려가야함.
- 부스팅 모델은 각 트리가 “이전 오차를 조금씩 수정”하는 방식인데, 트리를 많이 만들수록(= n_estimators 증가) 각 트리의 영향력(= learning_rate)은 작아져야 전체 모델이 폭주하지 않고 안정적으로 학습될 수 있다.
- randomforest -> max_depth = 3~12 , n_estimator = 100~1000 만 바꿔보면 된다.
- 이때 바꿔서 값이 높아지면 이를 꼭 주석으로 메모해두어야한다.
```py
# lightgbm
from lightgbm import LGBMClassifier
model = LGBMClassifier(random_state=42, verbose=-1, max_depth = 6, n_estimator = 100, learning_rate = 0.1)
model.fit(x_train, y_train)
pred_pb = model.predict_proba(x_val)
pred = model.predict(x_val)

from sklearn.metrics import roc_auc_score, accuracy_score, f1_score
print(roc_auc_score(y_val, pred_pb[:,1]), accuracy_score(y_val, pred), f1_score(y_val, pred))

# max_depth = 6, n_estimator = 100, learning_rate = 0.1
# 0.8193979933110368 0.7755102040816326 0.8
```
```py
# randomforest
# from sklearn.ensemble import RandomForestClassifier
# model = RandomForestClassifier(random_state=42)
# model.fit(x_train, y_train)
# pred_pb = model.predict_proba(x_val)
# pred = model.predict(x_val)

# print(roc_auc_score(y_val, pred_pb[:,1]), accuracy_score(y_val, pred), f1_score(y_val, pred))

# 0.810200668896321 0.7959183673469388 0.8214285714285714
```

## 4-3. 예측 및 제출
- 마지막에 read_csv로 잘 저장되었는지 확인해보는것이 중요하다.
```py
pred1 = model.predict_proba(test)
result = pd.DataFrame({
    'id':test_id,
    'output':pred1[:,1]
})
result.to_csv('수험번호.csv', index=False)
pd.read_csv('수험번호.csv') # 확인까지 꼭 하기
```


