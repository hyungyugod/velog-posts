# 스케줄러 위임문 정본 (10개) — 2026-09-02 구조 v2

> 스케줄드 태스크의 프롬프트는 **위임문만** 갖는다. 절차·규칙은 전부 `spec/`이 정본. 아래 텍스트가 실제 태스크 프롬프트와 글자 단위로 같아야 한다(바꾸면 여기 먼저 고치고 `update_scheduled_task`로 반영).

## 공통 머리 (모든 위임문 첫 문단)

```
전부 한국어로 진행한다. velog-posts 마운트는 늦게 붙는다 — bash에서 `find /sessions/*/mnt -maxdepth 2 -type d -name velog-posts`를 5회(5초 간격) 재시도해 ROOT를 잡고, 실패하면 아무것도 생성하지 말고 "velog-posts 접근 불가"를 보고하고 종료한다. 파일 도구 경로는 /Users/hg/Desktop/velog-posts/. 엔진·템플릿·스크립트(_시험엔진/engine/)는 절대 수정하지 않는다.
```

## 데일리퀴즈 3개

**gongin-daily-quiz** (월~토 09:00)
```
[공통 머리]
"공인중개사 데일리 문제지" 자동 생성. Read로 `_시험엔진/spec/데일리퀴즈.md` → `_시험엔진/spec/문항작성.md` → `_시험엔진/spec/프로파일_공인중개사.md` 순으로 정독한 뒤(하나라도 없으면 생성하지 말고 "spec 부재" 보고), 절차대로 실행: `python3 $ROOT/_시험엔진/engine/prepare_quiz.py --exam gongin --kind daily` → plan의 원천 노트 전문 정독 → `_work/<date>.questions.json` 작성 → `python3 $ROOT/_시험엔진/engine/render_quiz.py --exam gongin --kind daily` 통과까지 반복. 우선순위: 프로파일 > 문항작성 > 절차 > 이 위임문.
핵심 가드: 오늘 하루치 1개(exists면 그 파일 제시) / 노트에 없는 내용·⚠️ 확인 필요 항목 출제 절대 금지 / 정답 위치는 지정하지 않는다(렌더가 배정) / 재도전(🔁) 문항 금지 — 오답퀴즈 전담 / 검증 통과 전 종료 금지·기준 완화 금지.
```

**bupsa-2cha-daily-quiz** (화·목 09:15)
```
[공통 머리]
"법무사 2차 데일리 문제지" 자동 생성. Read로 `_시험엔진/spec/데일리퀴즈.md` → `spec/문항작성.md` → `spec/프로파일_법무사2차.md` → `spec/참조_법무사2차_문형사전.md` 순 정독 후 절차대로: `prepare_quiz.py --exam bupsa2 --kind daily` → 원천 정독 → questions.json(전 문항 단답·sub·exam:"2차") → `render_quiz.py --exam bupsa2 --kind daily`. 램프업 단계·문항 수·미니답안 수는 plan이 정한다(임의 조정 금지). skip이면 "원천 없음" 1줄 보고.
핵심 가드: 오늘 하루치 1개 / 노트 밖 내용·⚠️ 항목 금지 / 객관식 혼입 금지 / 개정법 필터(프로파일) / 재도전 금지 / 검증 통과 전 종료 금지.
```

**bupsa-1cha-daily-quiz** (화·목 09:20)
```
[공통 머리]
"법무사 1차 데일리 문제지" 자동 생성. Read로 `_시험엔진/spec/데일리퀴즈.md` → `spec/문항작성.md` → `spec/프로파일_법무사1차.md` → `spec/참조_법무사1차_기출문형.md` 순 정독 후 절차대로: `prepare_quiz.py --exam bupsa1 --kind daily` → 원천 정독 → questions.json(5지·exam:"1차"·단답 치환 ≤plan) → `render_quiz.py --exam bupsa1 --kind daily`. 1차 노트 0개면 skip 보고가 정상.
핵심 가드: 오늘 하루치 1개 / 노트 밖 내용·⚠️ 항목 금지 / OX·계산·4지 금지 / 발문 화이트리스트·판례 평서화(기출문형) / 개정법 필터 / 재도전 금지 / 검증 통과 전 종료 금지.
```

## 오답 파이프라인 3개

**gongin-odap-quiz** (월~토 08:40)
```
[공통 머리]
"공인중개사 오답 수거→원장→재도전 퀴즈". Read로 `_시험엔진/spec/오답퀴즈.md` → `spec/문항작성.md` → `spec/프로파일_공인중개사.md` 순 정독 후 절차대로: `prepare_quiz.py --exam gongin --kind retry` → plan의 picks(samples·missedTop·causeTop·promote_sa)로 questions.json → `render_quiz.py --exam gongin --kind retry`. due0이면 "듀 0 — 오답퀴즈 없음" 1줄 보고. exists면 수거·원장까지만 하고 기존 파일 제시.
핵심 가드: 공인중개사 전용(다른 시험 불가침) / Downloads 읽기 전용 / 원 문제 복사 금지·변형 출제 / conceptKey는 pick 값 그대로 / 승격·조준·원인 라우팅은 문항작성 §2 / 검증 통과 전 종료 금지.
```

**bupsa-2cha-odap-quiz** (수·금·토·일 08:50 — 화·목은 데일리, 하루 한 종류)
```
[공통 머리]
"법무사 2차 오답 수거→원장→재도전 퀴즈". Read로 `_시험엔진/spec/오답퀴즈.md` → `spec/문항작성.md` → `spec/프로파일_법무사2차.md` → `spec/참조_법무사2차_문형사전.md` 순 정독 후: `prepare_quiz.py --exam bupsa2 --kind retry` → questions.json(전 문항 단답·sub, 감점 패턴 각도·missedTop 조준) → `render_quiz.py --exam bupsa2 --kind retry`. due0이면 1줄 보고.
핵심 가드: 법무사 2차 전용(1차·중개사 불가침) / Downloads 읽기 전용 / 원 문제 복사 금지 / conceptKey는 pick 값 그대로 / 개정법 필터 / 검증 통과 전 종료 금지.
```

**bupsa-1cha-odap-quiz** (평일 08:55)
```
[공통 머리]
"법무사 1차 오답 수거→원장→재도전 퀴즈". Read로 `_시험엔진/spec/오답퀴즈.md` → `spec/문항작성.md` → `spec/프로파일_법무사1차.md` → `spec/참조_법무사1차_기출문형.md` 순 정독 후: `prepare_quiz.py --exam bupsa1 --kind retry` → questions.json(5지 + 승격 단답) → `render_quiz.py --exam bupsa1 --kind retry`. 1차 원장 미가동(원천 0건·듀 0) 동안은 보고 후 종료가 정상.
핵심 가드: 법무사 1차 전용 / Downloads 읽기 전용 / 원 문제 복사 금지 / conceptKey는 pick 값 그대로 / 함정 패턴표 각도 / 검증 통과 전 종료 금지.
```

## 주간리포트 3개 (월요일)

**gongin-weekly-report** (07:00) / **bupsa-2cha-weekly-report** (07:30) / **bupsa-1cha-weekly-report** (07:40)
```
[공통 머리]
"<시험명> 주간 오답 리포트". Read로 `_시험엔진/spec/주간리포트.md` → `spec/프로파일_<시험>.md` 정독 후: `prepare_quiz.py --exam <gongin|bupsa2|bupsa1> --kind weekly` → plan의 사실만으로 `<시험폴더>/claude_ox_오답/<date>-claude_oxquize.md` 작성(구성 ①~⑨) → `_runs.log` 1줄. exists면 종료, 원장 초기(exit 2)면 보고 후 종료.
핵심 가드: 원장·결과 JSON에 있는 것만(추정 금지) / 노트를 Downloads에 쓰지 않는다 / 이 시험 전용 / 퀴즈를 만들지 않는다.
```

## Anki 1개

**anki-weekly-deck** (월 08:00)
```
[공통 머리]
Read로 `_시험엔진/spec/앙키덱.md`를 처음부터 끝까지 정독하고 그대로 실행한다(맥락: `_시험엔진/공부루틴_OS_v1.md` §3·§6). 원문에 없는 내용은 절대 카드화하지 않는다. 덱 생성(§1~5) 후 반드시 §6 숫자 총정리 갱신까지 수행하고 `_runs.log`에 1줄 남긴다.
```
