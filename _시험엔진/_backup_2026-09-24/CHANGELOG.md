# CHANGELOG — HG 무한복습체계 엔진

> 실행자는 이 파일을 읽지 않는다. 사고·교훈·개정 이력을 여기에만 남긴다(최신이 위). 각 항목: 날짜 · 무엇 · 왜 · 영향 파일.

## 2026-09-19 — 하루 1문제지 원칙 · Anki 신규 30/일 · 일요일 오후 음악 고정 · 앙키덱 품질 규칙 4개 (OS v2.9 · spec v1.5)

- **무엇(스케줄)**: 아침 문제지를 2개 → **1개**. 월·수·금 공인 데일리 50 / 화·토 공인 오답 25 / 목 법무사 2차 데일리 ≤15(램프업 — 현재 S2 12문) / 일 없음. `exams.json`: gongin `schedule_days(_daily) "월·수·금"`·`schedule_days_retry "화·토"`; bupsa2 `schedule_days_daily "목"`·`schedule_days_retry ""`(빈 값 = 미실행일 집계 제외 — `prepare_quiz.py` 기존 분기, 코드 무수정); bupsa1·bupsa2 `transition_2026_11_01`의 "cron 화·목 → 평일"을 "11/2 이후 월·수·금 1차 데일리 / 화·목 1차 오답 / 토 2차 1개"로 대체. 데일리 50의 내부 쿼터·검증 상수는 불변.
- **무엇(Anki)**: `_anki.preset` 40→**30**/일·리뷰 400→**300**, `weekly.card_target` 280→**210**, `card_ceiling` 320→**240**. `spec/앙키덱.md` v1.5 §2-Q 품질 규칙 4개 — ⓐ 뒷면 원문 한정 ⓑ 작성 단계 가림 시험(검수리포트 머리에 자체 점검 수 기재) ⓒ basic 앞면 질문 1개·답 1개 ⓓ `card_ceiling` 초과 주 단계 임포트 안내(`tag:기출직결`부터). 스케줄러 위임문 `anki-weekly-deck` 본문의 "목표 280"을 "`_anki.weekly.card_target`(현재 210)"로 교체.
- **무엇(일과)**: OS §7 주말표 — 일요일 오후 = 음악 작업(FL Studio) 고정(P2 보호영역), 발행은 일요일 오전으로 이동(삭감 아님). 일요일은 문제지 0개.
- **왜**: 최근 14일(09-05~09-19) 문제지 **30개 생성·제출 9개(30%)** — 공인 19개 중 7, 법무사 2차 11개 중 2. 공급이 실소화량(하루 0.64개)의 3배였고, 듀 큐 증가(109→143→151)의 원인도 문제지 부족이 아니라 결과 미회수였다. Anki 원자층이 숫자·기간·요건을 전담하므로 문제지는 Anki가 못 채우는 세 가지(적용 판단 / 오래된 노트의 망각 회수 / 오답 재도전·졸업)만 맡는다(OS §3). 통합 문제지 신설안은 기각 — `validate_quiz.js`가 본편 40·유형 합 24·장기복습 20을 상수로 검사해 코드 수정이 필요하다(§8 #1 엔진 동결, 시험 D-42).
- **비용(기록)**: 공인 오답이 주 4회→2회(주 100문→50문). 듀 큐 151은 시험까지 늘 가능성이 높다 — 주 2회 × 상위 25문(상습·망각위험순) — 화요일 결과가 제출돼야 토요일에 다음 25문으로 넘어간다(미제출이면 같은 25문 재선정). 하위 개념은 10/17 이후 '시험 전 총복습 리스트'로 보완한다. 법무사 2차 오답은 서빙만 정지 — 듀(41)는 계속 쌓이고 목요일 데일리 결과도 원장에 수거된다. 요일 배치(법무사 2차를 목요일에 유지)는 사용자 결정.
- **사용자 액션**: ① 스케줄드 태스크 요일 — `gongin-daily-quiz` 월·수·금 / `gongin-odap-quiz` 화·토 / `bupsa-2cha-daily-quiz` 목 / `bupsa-2cha-odap-quiz` 일시정지 ② 데스크톱 Anki 프리셋 30/300 — **둘 다 09-19 사용자 완료 보고**(엔진 쪽에서 검증 불가 — 다음 주 `_runs.log` 요일로 확인). ③ **미완: `anki-weekly-deck` 태스크 프롬프트 본문을 위임문 정본으로 교체**(09-19에 2곳 변경 — "목표 280 미달이면" → `card_target` 참조, ⑤에 자체 가림 시험 줄)(위임문은 실제 프롬프트와 글자 단위로 같아야 한다 — 안 바꾸면 09-21 덱이 280을 채우려 들 수 있다).
- **관찰**: 09-19(토) 공인 데일리 스케줄 실행이 15:25 `plan.json`만 남기고 완료되지 않았다(`questions.json`·HTML·`_runs.log` 줄 없음). 같은 날 법무사 2차 오답(일요일 전용)이 토요일에 돌았다 — 09-16 요일 개정이 그때까지 태스크에 미반영이었다는 뜻.
- **사고·교훈**: 읽기 목적의 `git status`가 연결 폴더(삭제 불가 마운트)에서 0바이트 `.git/index.lock`을 남겼다(git이 잠금 파일을 unlink하지 못함 → 다음 commit 차단). `.git/index.lock.stale-claude-20260919`로 개명해 해소(사용자가 지우면 된다). **이 마운트에서 git 조회는 `git --no-optional-locks …`만 쓴다.**
- **영향 파일**: `engine/exams.json` · `공부루틴_OS_v1.md`(v2.9 — 19곳) · `README.md` · `spec/스케줄러_위임문.md`·`오답퀴즈.md`·`프로파일_법무사2차.md`·`프로파일_법무사1차.md`(11/2 체크리스트 ①)·`프로파일_공인중개사.md`(요일 + 오답 상한 요약 20→25 묵은 표기 정정)·`앙키덱.md`(v1.5) · `_설계_하루1문제지_Anki30_1차80_2026-09-19.md`(신설). 코드·템플릿·테스트 수정 0줄. 백업: `_backup_2026-09-19/`.
- **독립 검토(Opus 서브에이전트, 결론을 주지 않고 파일만 제공)**: 산술·JSON은 이상 없음. 지적 → 조치: ① 법무사 2차 데일리를 '≤12'로 쓴 오기(12는 오답 상한, 데일리 램프업 상한은 15) → 전 파일 정정 ② '유지' 단계 문항 수 — 프로파일 표기 8/6이 정본 `rampup`(12/8)과 어긋나 있던 묵은 불일치 → 프로파일 정정 ③ 법무사 1차 프로파일 체크리스트 ①·`bupsa1.schedule_days` 문구가 여전히 '평일 전환' → 하루 1문제지 요일로 교체(안 바꾸면 11/2부터 하루 2개) ④ 단계 임포트가 의존하는 `기출직결` 태그가 §2 태그 규칙에 없었음(빠지면 ★★까지 보류) → §2에 필수 태그로 명문화 + 0장이면 안내 생략 + 머리에 ★★ 행 수/태그 행 수 기재 ⑤ '듀 큐 동결'은 부정확(서빙만 정지) → 정정 ⑥ 위임문 머리말·`_rampup_note`·프로파일 2차 상한 근거의 낡은 현재형 문장 정정 ⑦ §5 고정 안내의 '신규 30/일' 하드코딩 → `preset.new_per_day` 참조. **미조치(오늘 범위 밖 묵은 불일치 — 기록만)**: OS §3 게이트 2의 `lane_cap_notes 60노트` 서술(v2.6 목표 모델과 불일치)·OS §7 국면 2의 '중개사 레인 15노트'(해당 키 없음)·스킬 `odap-quiz-bupsa` 설명의 '일요일 복습'.
- **검증**: `tests/run.sh` 전체 통과(수정 후·검토 반영 후 각각 실행) · `parse_schedule_days` 직접 호출로 요일별 문제지 수 검산(월~토 각 1·일 0) · `card_target == new_per_day × 7` · `exams.json` 라운드트립 동일성(indent 2) 확인 후 원자적 교체.

## 2026-09-16 — 하루 2문제지 원칙(슬롯 교대 · 공인 오답 25) + Downloads 수거완료 이동·동명이본 가드 (OS v2.8)

- **무엇(스케줄)**: 아침 문제지를 3개 → **2개**. 데일리 50(월~토)은 매일, 2번째 슬롯은 **공인 오답(월·수·금·토) ↔ 법무사 2차(화·목 데일리 · 일 오답)** 교대. `exams.json`: gongin `schedule_days_daily "월~토"`·`schedule_days_retry "월·수·금·토"` 신설, `retry.cap` **20→25**; bupsa2 `schedule_days_retry "수·금·토·일" → "일"`. 주간리포트 미실행일 집계는 이 값을 읽으므로 자동 추종. 원장의 `pick`·졸업후보 예약칸(min(5, 25//4)=5)은 다음 수거 때 재계산(프로덕션 자동).
- **무엇(수거)**: `prepare_quiz.py ingest()` — ① **동명이본 가드**: Downloads 파일과 `_inbox` 동명 사본의 내용이 다르면 `_inbox`에 ` (N).json`으로 추가 수거(copy2로 mtime 보존 → `build_ledger.find_jsons`가 최신본 채택). ② **수거완료 이동**: `_inbox` 사본과 바이트 일치가 확인된 원본만 `Downloads/<_paths.downloads_sweep_dir>/`(기본 `_오답_수거완료`)로 `os.rename`(같은 볼륨 — 삭제 권한 불필요, 이름 충돌은 ` (N)`). null 이면 종전대로 Downloads 불변. 글롭 밖 파일·타 시험 파일·`legacy_until` 이후 레거시 파일은 불변. plan `ingest`에 `collided/moved/move_failed/sweep_dir` 추가, 콘솔 요약에 "이동 N건".
- **왜**: (스케줄) Anki 원자층 가동 뒤 아침 총량(공인 오답 20 + 데일리 50 + 법무사 12 + Anki)이 자기조절 한계를 넘어 이탈 위험(OS §8 #2). 사용자 결정: 하루 2개. 오답 요일 6→4일 축소의 적체(듀 큐 143, 09-05→09-16 109→123 증가 중)를 상한 25로 보정(주 120→100문). 법무사 2차는 시험 13개월 전이라 복습 주 1회로 후퇴(11/1 중개사 종료 시 복원 검토 — 프로파일 체크리스트). (수거) Downloads에 결과 JSON 42개가 쌓여 정리 요청 — 그런데 원본을 치우면 **같은 날 다시 저장한 결과가 브라우저 ` (1)` 없이 같은 이름으로 떨어져 `cp -n`에 막혀 최신 결과가 조용히 유실**되는 구멍이 생긴다(중간제출→최종제출 경로). 가드 ①이 그 구멍을 막고 나서야 ②가 안전하다. 삭제(rm)가 아니라 이동인 이유: 스케줄 세션의 삭제 권한이 불확실하고, 잘못 옮겨도 되돌릴 수 있다(원장 원천은 `_inbox`, 수거완료 폴더는 사용자가 비운다).
- **1회 정리**: Downloads 결과 JSON 42개(공인 30·법무사2차 9·레거시 법무사_ 3) 전부 `_inbox` 사본과 SHA-256 일치 확인 후 `Downloads/_오답_수거완료/`로 이동.
- **사용자 액션(필수)**: Cowork 스케줄드 태스크 요일 변경 — `gongin-odap-quiz` 월~토 → **월·수·금·토**, `bupsa-2cha-odap-quiz` 수·금·토·일 → **일**. 프롬프트 본문은 불변(위임문 §헤더만 갱신). 바꾸기 전까지는 종전 요일대로 3개가 생긴다(엔진에 요일 게이트 없음 — 온디맨드 스킬이 같은 prepare 를 쓰므로 일부러 두지 않음).
- **영향 파일**: `engine/exams.json` · `engine/prepare_quiz.py`(ingest·_collect_one·_sweep_one·요약 출력·docstring) · `engine/tests/ingest_smoke.py`(신설 19건) · `engine/tests/run.sh` · `engine/tests/e2e_smoke_all.sh`(라벨) · `README.md` · `spec/스케줄러_위임문.md`·`오답퀴즈.md`·`데일리퀴즈.md`·`프로파일_법무사2차.md` · `공부루틴_OS_v1.md`(v2.8).
- **검증**: `tests/run.sh` 전체 통과(기존 + ingest 19건) · E2E 10케이스 통과(`/tmp/e2e-0916`, 오답 계약 "25문 = min(상한 25, 듀 143) · 졸업후보 예약 5칸") · 프로덕션 Downloads 1회 정리 후 `prepare --json` 드라이런으로 수거 0·이동 0·경고 없음 확인.

## 2026-09-14 — Anki 목표 모델 1주차 실측 반영: 후보 추출기 신설 · ⚠️ 표 양식 고정 · 밀도 규칙 · 보류 표 (spec v1.4 · OS v2.7 · 코어 v3.11)

- **무엇**: ⑩ `engine/extract_anki_candidates.py` 신설 — 창 계산(파일명 날짜 −7일)·표식 추출(★★/★/n: 줄머리 + 인라인 `{}`·`!!`/`!` 원표기)·**출제 금지 마킹 3경로**(줄 안 ⚠️ / 정리본 하단 `## ⚠️ 확인 필요` 표의 `위치`·키워드 역참조 + `(⚠️ N)` 인라인 참조 / 백지복습 보고서 ⑦)·기출고 key·원장 상습(기출고 제외·실수 전용 표시)·직전 검수리포트 「이월」(상단 N행 소진 건너뜀)·「보류」(대기 주수 +1, 3주 표면화) 파싱·`n:` 수집을 스크립트가 하고 AI는 문안·인출 설계만 한다. `tests/candidates_smoke.py`(29건) + `run.sh` 연결. ④ 코어 v3.11 §4: 하단 ⚠️ 표 양식 고정(열 `# | 위치 | 원문 표현 | 확인 포인트 | 범위`, `위치`=소제목 번호, `범위` 항목/전체, 인라인 `(⚠️ N)`, 출력 전 자기점검) → `build_조립.py`로 조립완성본 4개 재생성. ⑤ 백지보고서 ⑦을 두 번째 금지 원천으로 명문화 + 스킬 `baekji-chaejeom`에 정리본 역기입 절차(§2-1). ⑦ `_anki.weekly.mandatory_lanes`에 `n:신규`(총정리에 처음 편입된 항목), fill_order `n:`→`n:기존`. ① `_anki.density` 신설(줄 1 = 카드 1 기본·분할 ≤2·빈칸 ≤3). ③ `{}` 앞면 문맥 규칙(동의어·전개·괄호 풀이는 뒷면). ⑧ 검수리포트 `## 보류 (사용자 확정 대기)` 고정 표(열 `원천 | 항목 | 지목 근거 | 대기 주수 | 해소 조건`). 스케줄러 위임문·예약 작업 `anki-weekly-deck` 프롬프트 교체(낡은 "60노트·140장" 문구 정정).
- **왜**: 09-14 정규 덱 실측 — 이번 주 노트 8개 표식 253개(줄 251) → 382장으로 상한 320 무력화·9.6일치; 독립 검증(3 에이전트) 지적 96건 중 답 노출 39건이 `{}` 빈칸 옆 동의어·전개 문맥; 하단 ⚠️ 양식이 표/불릿/번호/없음으로 제각각이라 역참조 4건이 사람 판독에 걸림(학개론 노트는 "(하단 확인 필요 ②)"만 있고 섹션 부재); 백지보고서 ⑦ 2건이 정리본에 없어 두 갈래를 읽어야 함; 상습 후보 47건 중 46건 기출고 중복; n: 신규 3건이 카드 미편입; 절단 이월이 없는 모델에서 확인 대기 항목 3건이 2주째 추적 없이 누적.
- **정정(P1)**: 밀도 규칙의 소급 효과는 계획 단계 추정 "382 → 약 300"이 아니라 **375장(−2%)**이다(줄→행 분포 1:223·2:25·3:3, 행 빈칸 1:226·2:39·3:14·4+:3). 카드 수의 바닥은 표식 줄 수(251)라 밀도 규칙은 총량 장치가 아니라 예측 가능성 장치다. 총량 지렛대는 표식 습관(★★·★ 예산 — 이번 주 세법 노트 ★★24·★3)과 프리셋뿐이며 둘 다 사용자 결정으로 남긴다. spec §2·OS §6-2·`_anki.density._note`에 이 수치를 박았다.
- **추출기 실측(09-14 노트 8개)**: 표식 ★★117·★105·{}31·n:7 — 당일 수동 실측과 **동일**. 금지 마킹 19건 ⊇ 수동 16건(추가 3: 백지 ⑦-3이 지목한 공법 82행 전체, 민소 하단 #6 '판독 불확실'이 지목한 {청구취지}·{이당사자 대립구조}·{공방에 의한 판결} 줄, 그리고 같은 단락에 ⚠️가 있는 {소금장승신} 334행) — 기계 판정이 사람 판정보다 보수적이며, 노트 쪽에서 `범위=전체`·⚠️ 자기 줄 분리로 풀어야 한다. 사람 판독(unresolved) 0건.
- **영향 파일**: `engine/extract_anki_candidates.py`(신설) · `engine/tests/candidates_smoke.py`(신설) · `engine/tests/run.sh` · `engine/exams.json`(`_anki`: mandatory_lanes·fill_order·density·banned_sources) · `spec/앙키덱.md`(v1.4) · `공부루틴_OS_v1.md`(v2.7) · `spec/스케줄러_위임문.md` · `README.md` · `ai-guidelines/20_프롬프트-라이브러리/1_코어엔진.md`(v3.11) + 조립완성본 4개 · 스킬 baekji-chaejeom · 예약 작업 anki-weekly-deck. 백업: `_backup_2026-09-14_anki/`(원본 7개). 09-14 덱 자체는 재출고하지 않았다(세법 노트 파일명 09-11→09-13 개명에 따른 src 치환·재빌드만, 같은 key).
- **검증**: `build_조립.py --verify` OK · `tests/run.sh` 전체 통과(payload 10·render 31·template 47·apkg 10·후보 추출기 29) · 추출기 실측 대조 위와 같음.

## 2026-09-10 — 💬 단답 코멘트 (템플릿 v2.2 → 원장 → 다음 출제 플랜)

- **무엇(템플릿 v2.2)**: 단답 자가채점(⭕·❌) 직후 **≤120자 코멘트 칸**(`.sa-comment`)이 열린다. 적은 문항만 payload `results[].comment`로 실리고(빈 칸이면 키 자체가 없어 종전 제출과 payload 문자열 단위 동일 · `wrong[]`엔 넣지 않음 · 중간제출 미응답분 제외), 진행 저장·복원에 포함되며 채점 후에는 잠긴다. `#result` 요약에 `💬 코멘트 N개` 1줄. `tests/template.test.js` 37→**47건**.
- **무엇(엔진)**: 원장이 **모든 결과 항목**(⭕ 비재도전 포함)의 코멘트를 모은다 — 최상위 `comments{count, recent[≤100], byConcept{개념:[≤5]}}` · `dueQueue[].commentCount/lastComment` · `samples[].comment`(있을 때만 키) · md 보고서 `## 💬 코멘트 (최근 14일)` 표. 플랜으로는 retry `picks[].comments`(개념별 최신 ≤3) · daily `recent_comments`(최근 14일 ≤30, 0건이면 키 없음) · weekly `comments_week`(최근 7일)로 흐르고, 각 `ai_brief`에 조준 문장 1줄(우선순위 missedTop → **comments** → causeTop 각도)이 붙는다. 파라미터는 `exams.json._comments` 신설(시험 공통).
- **왜**: 채점 직후 1초의 자기진단("요건 순서를 반대로 씀")이 가장 정확한 조준 정보인데, 종전에는 ❌의 missedKeys/errorCause만 남고 **⭕로 맞힌 문항의 아쉬움은 통째로 버려졌다**(⭕ 비재도전은 이벤트 자체가 없어 원장에 흔적이 없다). 사용자 결정: 코멘트는 **주석 전용 — FSRS 스케줄·상태·카운터에 영향 0**. 재출제를 강제하지 않고 "다음에 그 개념이 나올 때의 각도"와 주간리포트 표시로만 쓴다.
- **주석 전용 보증**: 코멘트는 이벤트 스트림(w/c)과 분리된 리스트에 모으고 상태·FSRS 계산이 끝난 뒤에 블록을 만든다. E2E가 **코멘트 유무만 다른 두 결과 세트로 원장을 두 번 빌드해** comments 블록·lastComment/commentCount·samples[].comment를 뺀 나머지가 동일함을 assert한다(망각위험 R만 허용오차 0.002 — 실행 시각 함수). `build_ledger.py --v3-compat`이면 `PATCH["comments"]`가 꺼져 `comments` 키 자체가 없다(프로덕션 복사본에서 v3.1 출력과 바이트 동일 재확인).
- **영향 파일**: `engine/quiz_template.html`(v2.2) · `engine/build_ledger.py`(코멘트 수집·`PATCH["comments"]`) · `engine/prepare_quiz.py`(daily/retry/weekly 플랜 필드·brief) · `engine/exams.json`(`_comments`) · `engine/tests/e2e_smoke.py`(seed-ledger 픽스처에 코멘트 3건 + `comment_contract` A/B 회귀) · `spec/문항작성.md`·`오답퀴즈.md`·`데일리퀴즈.md`·`주간리포트.md`(구성 ⑩)·`프로파일_법무사2차.md` · `README.md`.
- **검증**: `tests/run.sh` 98건(payload 10·render 31·template 47·apkg 10) + E2E 10케이스 전부 PASS. 프로덕션 복사본 재빌드에서 코멘트 0건(`count 0`)·기존 원장과 그 외 전부 동일.

## 2026-09-09 — 템플릿 v2.1: ? 마킹 + ❓ 물음표 해설 모음

- **무엇**: 보기·ㄱㄴㄷ 지문 마킹을 4상태(`· → O → X → ?`)로 확장(`?` 호박색, 흐림 없음). OX형은 보기 대신 **문항 ? 플래그**(·↔?). 채점(전체·중간) 시 점수 요약 아래 **`❓ 확신 없던 것 N개 — 해설 모음`** 섹션: ? 항목마다 판정(정답/오답 보기 · 참/거짓 지문 — 정답 조합×부정형 발문으로 산출, 툴팁에 근거 · OX는 정답 O/X)과 그 문항 해설·출처를 한자리에, 문항 앵커 링크. 미응답(중간제출) 문항의 ?는 제외. payload는 ?가 있을 때만 `results[].uncertain[]`·`uncertainCount` 추가(없으면 문자열 단위 불변). 진행 저장·복원에 `?` 포함.
- **왜**: "찍어서 맞은 것"은 원장에 안 잡히는 구멍이다 — ?는 그 자백이고, 채점 직후 모아 읽는 것이 가장 싼 재학습이다. `uncertain`은 훗날 오답퀴즈 조준·Anki 후보로 쓸 수 있게 데이터만 남긴다(원장은 아직 미사용).
- **모르겠음 강조**(같은 날 추가 요청): 🤷 무답 문항은 채점 후 카드 테두리를 **무지개 그라데이션**(`div.q.dkq`) + 헤더 배지 `🤷 모르겠음`으로 표시 — 전체·중간 제출·재로드 복원 경로 동일. 표시 전용(payload 불변).
- **영향 파일**: `engine/quiz_template.html`(v2.1, +약 200줄 — 상태 클래스 `q`와 카드 클래스 `.q` 충돌을 `div.q`로 못 박음) · `engine/tests/template.test.js`(27→37건). 당일(09-09) 문제지 4개를 `rerender_quiz.py`로 v2.1 교체(결과 미수거 확인). `tests/run.sh` 전체 통과(payload 10·render 31·template 37·apkg 10).

## 2026-09-03 — Anki 주간 공급 상한 확대 (25→60노트) · _anki 정본화 · 이월/n: 원천 · 빌더 소프트 상한 경고

- **무엇**: 주간 Anki 경쟁 레인 상한을 25노트 → **60노트**로 올리고, 상한·프리셋·국면 규칙을 `engine/exams.json` `_anki` 블록으로 정본화(preset·weekly·phase2_gate·final_phase). 후보 원천에 **④ 이월**(전주 검수리포트 「## 이월 (다음 주 우선)」 표, 열 `원천 | 레인 | 항목 | 이월 횟수 | 사유` 고정)과 **⑤ 줄머리 `n:`**(최저 우선순위·자동 편입)를 추가. 절단 우선순위 상습 > 백지 구멍 > ★ > n:, 같은 레인 안에서 이월 → 시험일 가까운 순 → 노트 날짜 순. 총 카드 **소프트 상한 140장/주**(= 신규 20 × 7)는 자르지 않고 `build_apkg.py`가 경고만 낸다(정규 덱만, 특집 제외, exit 0 불변). 정규 덱 30카드 초과 시 별도 에이전트 전수 원문 대조를 규칙화(D8), 같은 덱 재돌리기 금지를 여유 프로토콜에 명문화(D9), Phase 2 승격 게이트(수동)와 10/1·10/17 국면 2 공급 연동을 `_anki`에 박았다.
- **왜**: 프리셋은 주 140장 소화인데 실제 공급이 25노트(≈50~90카드)뿐이라 실효 신규 6~13장/일 — 큐가 비어 같은 덱을 하루 2~3번 재돌리던 것(집중연습이라 이득 0)이 원인이었다. 병목은 Anki 설정이 아니라 공급 상한이었고, 08-31 덱에서 상한 25가 백지 구멍 15건+를 실제로 잘라내고 있었다. Anki 앱 설정(20/200/0.90)은 그대로 두고 공급만 프리셋에 맞췄다.
- **영향 파일**: `engine/exams.json`(`_anki` 신설) · `engine/build_apkg.py`(소프트 상한 경고 — 모델 ID·guid·deck id·검증 로직 불변, 테스트 전용 `HG_ANKI_CEILING_OVERRIDE`) · `engine/tests/apkg_smoke.sh`(신설 10건) · `engine/tests/run.sh`(스모크 연결) · `spec/앙키덱.md`(v1.1) · `공부루틴_OS_v1.md`(v2.4→v2.5) · `spec/스케줄러_위임문.md`(anki-weekly-deck).
- 설계서: `_설계_Anki공급상한_2026-09-03.md`. 백업: `_backup_2026-09-03_anki/`(원본 6개).

## 2026-09-02 (2차) — 템플릿 v2 · 법무사 2차 리듬 재설계 · evidence 게이트

- **템플릿 v2** (`engine/quiz_template.html` 775→1093줄): ① 객관식 보기별 **O/X 마킹**(👁 소거 대체 — `·→O→X`, O 초록 테두리·X 빨강+흐림, ㄱㄴㄷ 지문 마킹과 동일 문법, 채점 후 잠금·payload 무영향) ② **중간 제출**(`✂ 여기까지 제출` — 답한 문항만 채점, 미응답은 정답·해설 비공개 + 결과 미확정, payload에만 `partial/answeredCount/skippedCount` 추가·`results[].skipped:true·correctAnswered:null`, 이어 풀고 다시 제출 가능) ③ **진행 저장·복원**(localStorage `hgq:<subject>:<quizId>` — 선택·타이핑·자가채점·진단·마킹·채점분 전부, 배너 `↩ 이전 진행 복원됨`, 전체 제출 시 삭제). 전체 제출 payload는 종전과 **문자열 단위 동일**(payload_regression 10/10 + 키 순서 검사). 테스트 `tests/template.test.js` 27건.
- **원장**: 중간제출 JSON은 `correctAnswered:null` 항목이 이벤트로 잡히지 않음을 드라이런으로 확인(미응답 개념 변화 0) + `partial`은 만점 집계 제외.
- **법무사 2차 리듬 재설계**(사용자 결정 — 서술형은 하루 10~15문이 한계): **화·목 데일리(신규) / 수·금·토·일 복습(오답 재도전) / 월 주간리포트, 하루 한 종류만**. `exams.json` bupsa2: S3 20→**15**(신규 9+장기 3+한달전 3)·S2 12·S1 9·S0-2 8·S0-1 6·유지 8|6, `retry.cap` 8→**12**(졸업후보 예약 3칸), `schedule_days_daily/retry` 신설. 스케줄러 `bupsa-2cha-odap-quiz` cron 평일 → **수·금·토·일 08:50**. `prepare_quiz.py` 버킷 배분을 최대잔여법으로(3문 = 1·1·1, 4문 = 1·1·2), 상한 상향 직후 `pick` 부족분 듀 순서 보충 + 경고, 주간리포트 미실행일을 kind별 요일로 집계.
- **evidence 게이트**(품질·환각 방지): questions.json 단답 문항(데일리 본편 전 시험)에 `evidence:{note, quote}` **필수** — 렌더가 quote(15~120자)를 노트 원문(정규화 후 부분 문자열)에서 대조, 실패 시 렌더 중단·전부 열거, 통과분은 HTML에서 제거. retry 단답은 `note:"samples"`로 원장 samples 대조. `spec/문항작성.md` §1-4.
- **최근 개념 중복 검사**: `validate_quiz.js` 데일리 신규 문항 conceptKey가 같은 폴더 최근 7일 문제지와 정확히 일치하면 FAIL(`recent_concept_dup`, `exams.json._validate.recent_concept_days`, 시험별 override 가능). 레거시 중개사 문제지 69개 중 31개가 걸리는 수준의 반복이 있었다 — 새 경로에서만 차단.
- **재렌더 도구** `engine/rerender_quiz.py` 신설 — 기존 문제지의 문항·META를 그대로 두고 현재 템플릿으로 다시 렌더(결과가 이미 수거된 문제지는 건너뜀, 쿼터·듀 큐 검사만 허용·구조 검사는 통과 필수, 이전본 `_work/<date>.pre-rerender.html` 보존). 당일(09-02) 중개사 데일리·오답, 법무사 2차 오답 3개를 v2 템플릿으로 교체(jsdom 스모크: optox 160/52, 중간제출 버튼 활성 확인).
- 검증: `tests/run.sh` 68건(payload 10·render 31·template 27) + E2E 10케이스(bupsa2 S3 15문·retry 12문·bad-evidence 실패 경로 포함) 전부 PASS.

## 2026-09-02 — 구조 v2 통합 공사 (업그레이드 계획서 v1 결정 D1~D12 실행)

- **아키텍처 전환**: "AI가 HTML 토큰 치환·정답 배정·검증까지" → **AI는 `questions.json`만, 나머지는 `engine/` 스크립트**(`prepare_quiz.py` → `render_quiz.py` → `validate_quiz.js`). 정답 위치 사전 배정을 AI에서 걷어내 9/1 정합 사고류를 구조적으로 제거. 조합형 지문 순서·ㄱ 편향도 렌더가 처리.
- **단일화**: 템플릿 6벌 → `engine/quiz_template.html` 1벌(토큰 23종, jsdom payload 회귀 10건 deep-equal — 기대 차이 3: 중개사 payload에 `monthlyOf` 신규 기록·법무사 `calc` 지원·1차 단답 승격 `exam` 오기(2차→1차) 수정). `build_ledger.py` 3벌 + `fsrs_vendor` 3벌 → 1벌(`--exam`, 원장 JSON·md 바이트 동일성 검증). 검증 스크립트 인라인 6벌 → `validate_quiz.js`(89개 실파일 + 합성 6건 등가성, 의도 예외 1: 장기복습 20). 시험 파라미터 → `engine/exams.json`.
- **원장 행동 패치 4건**(`build_ledger.py` v4.0): ① 졸업후보 예약칸 min(5, N//4)(gongin 5·bupsa2 2) — 졸업후보 15개 중 10개가 44일간 미서빙이던 기아 해소 ② `timesWrong==0` 고아 5개 듀 큐 제외 ③ samples 최근 3개 유지(FIFO — 상습 개념에서 `causeNote` 유실 방지) ④ FSRS 최대 간격 = clamp(min(cap, D-day−7), 7, cap): gongin 52·법무사 120.
- **쿼터 변경(D2)**: 공인중개사 데일리 신규 30→**20**, 장기복습 10→**20**(B1 6·B2 7·B3 7). 근거: 7일 이상 노트 198개 중 153개(77%) 미서빙, D-59.
- **문서 다이어트**: 베이스 4·프로파일 3·README·OS의 이력·복붙(정합 문단 5회·마운트 블록 11회) 제거 → `spec/` 10파일. 규칙 233KB → spec 약 60KB. `프로파일_법무사_v1`(구본)·`매강의인출양식`·인출기록 잔재·`build_ledger_v2_backup`·`_봉합_런북`·`공인중개사/CLAUDE.md`·`오답노트_지침_v4`·`_지침아카이브/`·`kakao_push_v1_backup` 삭제(D7 — git 이력 신뢰). `_raw/` → `_inbox/` 통합(D10).
- **운영**: `_runs.log` 실행 로그(prepare EXISTS/SKIP/DUE0, render OK/FAIL) + 주간리포트가 미실행일·표식 지표(노트당 ★) 집계(Phase 2 관찰). 회귀 테스트 고정: `engine/tests/run.sh`(payload 10·render 27), `e2e_smoke_all.sh`(9 케이스). 렌더는 검사기 부재·실행 실패 시 **중단**(무검증 문제지 금지).
- **드리프트 해소(D11)**: 코어엔진 로컬 v3.3 → 프로젝트 지침 v3.9 역이식(`ai-guidelines/20_프롬프트-라이브러리/1_코어엔진.md` 무버전 파일명), 모듈 법무사 v3.8, 지식파일 17호 편입, 조립본 4개 재생성. INDEX·대시보드(`NOTE_AGE_DAYS` 14→7)·프로파일 자기모순 정정. Anki 노트 수집 기준 mtime → 파일명 날짜(옛 노트 편집 부활 방지, 백지복습만 mtime 유지).
- **결정 보류**: 표식(★/n:) 파이프라인 유지(D4-A) — 4주 연속 노트당 ★<3이면 축소. 11/1 법무사 평일 전환 체크리스트는 프로파일에 그대로(그 전 실행 금지).
- 백업: `_backup_2026-09-02/`(구 문서·템플릿·스크립트·스킬 원문 107파일) — 컷오버 안정 확인 후 삭제.

## 이전 이력 (구조 v1 — README_구조_v1.md·각 파일 헤더에서 이관)

- **2026-09-01 밤 정답-해설 정합 사고 + 게이트**: 중개사 데일리 1번에서 정답 사전 배정과 보기 배치가 어긋남(부정형인데 answer가 참 지문, 해설은 D 서술 — 사용자 발견). 즉시 교정 + 정합 휴리스틱(해설 첫 문장↔보기 2-gram Dice) 검증 4곳 탑재. 과거 전수 재검(Opus) 추가 진짜 오류 0(단발 사고). 보류 2건(데일리 07-07 Q23 공간재구조화 결정권자 오기재, 06-24 Q15 함정 번호 어긋남 — 창 지난 파일).
- **2026-09-01 밤 조합형 ㄱ 편향**: 조합형 325문 실측 ㄱ 정답 포함 95%·ㄴ 83%·ㄷ 59%·ㄹ 40% → 위치-참거짓 무상관 규칙 + 기계 검사(≥4문일 때 25~75%). 지문 O·X 마킹 토글(템플릿 v6).
- **2026-09-01 밤 🤷 모르겠음 버튼(v5)**: 무답 = 오답 집계 + 원인 자동 개념부재. 찍어 맞히면 구멍이 기록조차 안 된다. **사고**: 패치 스크립트가 patch 실패 전 파일을 비워 `데일리퀴즈/2026-08-31.html` 0바이트 유실 → 결과 JSON에서 전량 복원. 교훈: 결과 JSON이 퀴즈 원문 백업, 미제출 퀴즈는 유실되면 끝.
- **2026-09-01 밤 오답 원인 진단 루프(v4)**: 틀린 객관식마다 개념부재/혼동/함정/실수 1개 필수 선택 → 원장 `errorCauses`·`dueQueue[].causeTop` → 재출제 각도(실수는 각도 불변) + Anki(실수뿐인 개념 제외, 혼동은 뒷면 상대 명기).
- **2026-09-01 Anki 강조 레인 등급 분리**: ★★만 상한 제외, ★는 25 안 경쟁(상습 > 백지 구멍 > ★). 검수리포트 표식 원문 줄 인용 게이트 — 08-31 덱 실측 노트 47→카드 89 중 상한 밖 강조 22장의 표식 근거는 5장뿐.
- **2026-09-01 진단 기타 입력 / 단답 승격 / 놓친 포인트 조준 / 교정 입력 게이트**: 놓친 포인트 `＋ 기타(직접 입력)`; 오답퀴즈 반복 실패 개념(상습∨retryMissed≥1∨timesWrong≥3) 단답 승격 최대 5(D-14 0); 단답 자가채점 missedKeys → 원장 → 재도전 조준(1문항 1포인트); ❌ 후 모범답 타이핑 일치도(2-gram Dice ≥0.55) 게이트·모범답 가림 상시화.
- **2026-08-31 밤 절차 다이어트(OS v2.1·코어 v3.9)**: 인출수거 파이프라인 폐지(타이핑 제출·인출기록/ 신규 생성 중단). 사전추측·매강 키워드는 종이 전용, AI 인계는 정리본 위임 + 필기본 사진 1회. 퀴즈·Anki 원천 = 정리본+백지복습/+오답원장.
- **2026-08-31 / 08-30 / 08-29 밤**: 낙차 AI 이관(OS v2.0) → 강의 블록 파이프라인(OS v1.9) → 인출기록 파이프라인 신설(OS v1.8) — 전부 08-31 폐지로 수렴.
- **2026-08-29 단답 치환(B안)**: 객관식 데일리에 단답 치환(중개사 ≤5·법1차 ≤2, D-14부터 0). 근거 Kang·McDermott·Roediger 2007. 중개사 템플릿 v3.0 혼합형.
- **2026-08-29 전수 감사 반영(구조 v1.3)**: 장기복습 창 −14일 잔존 정정, 백지복습 glob 매치 0건 결함, 앙키덱 §0 조기종료로 §6 유실, 오답 상한·요일 하드코딩, 버킷 경계일 중복, `N:` 오정규화, 인용 버전 스큐.
- **2026-08-29 표식 규약 통일**: 줄머리 앵커만 유효, ★/⚠️ 용법 분리(겹치면 확인 필요 우선), `:n`→`n:` 정규화(`N:` 제외).
- **2026-08-28 법무사 기출 실전형 전환**: 1차 전 문항 5지·발문 화이트리스트·부정형 65~90%·함정 패턴 T1~T11(29~31회 600문항 실측); 2차 모범답안체·`sub` 소유형·미니답안형(문형사전).
- **2026-08-27 사이드 루틴**: `n:` 숫자 총정리(주간), 특집 덱(세율 27·날짜 19노트). Anki 신규 20/리뷰 200. 중개사 퀴즈 월~토 확장.
- **2026-08-26 OS v1 제정**: 4계층(이해·구조·적용·원자)·Anki 원자층·! 태그·7~14일 복습 공백 보정(장기복습 창 −14일→−7일, B1 7~45일)·엔진 동결 선언(리스크 1: 시스템 공사가 공부를 대체).
- **2026-08-25 트랙 분할·정본 일원화**: 법무사 1차/2차 트랙 프로파일 분리(노트 폴더 공유, `-2차-` 스위치). 정본을 ai-guidelines → velog-posts/_시험엔진으로 이전 — 08-25 아침 스케줄 세션이 ai-guidelines를 못 읽어 자동 실행 실패한 실사고(마운트 상속 규칙).
- **2026-08-24 재도전 분리(데일리 v5)**: 데일리퀴즈는 원장 미참조, 재도전(🔁)은 오답퀴즈 전담.
- **2026-08-20 원장 v3.1**: FSRS-6(py-fsrs 6.3.2 벤더링) + 과목 별칭 정규화. v3: 고정 사다리 [3,7,16,35] → FSRS.
- **2026-07-27 수거 글롭 사고**: `공인중개사_오답_*.json`이 과목명 낀 변형(`공인중개사_민법전범위_오답_*.json`)을 조용히 누락 → `*오답_*` 형태 필수. 마운트 지연 5회 재시도 규칙.
- **2026-07-02 원장 v2**: 닫힌 루프 상태머신(상습/재도전중/졸업후보/졸업), `_inbox` 드롭 폴더, 전체 이력 누적.
