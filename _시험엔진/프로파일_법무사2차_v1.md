# 프로파일_법무사2차 v1.5 (2026-08-29)

> 베이스(데일리퀴즈·오답파이프라인·주간리포트)의 `⟨프로파일: KEY⟩` 슬롯을 채우는 파생 정의. 베이스와 충돌 시 이 파일이 우선.
> v1.5 (2026-08-29): **전수 감사 반영** — ① 검증 JS의 장기복습 창을 지침 본문과 맞춰 `>=14` → `>=7`, 신규 카운터 경계를 `d<7`로 좁혀 7일차 중복 계수 제거 ② 백지복습 수집 glob `20*.md` → `*.md`(실물 보고서가 `20`으로 시작하지 않아 매치 0건이던 결함) + 트랙 판별 규칙 명문화 ③ 장기복습 버킷 배분 규칙 신설 ④ 오답 상한 슬롯(N = min(8, 듀)) 신설 ⑤ 코어엔진 인용 형식. 파일명은 v1 유지.
> v1.4 (2026-08-29): ★ 가중에 **앵커 규정**(줄머리 표식만 유효·문장 중간·백지시트 장식 ★ 무시)과 **⚠️ 우선 규칙**(확인 필요와 겹치면 출제 금지) 명시 (`공부루틴_OS_v1.md` §4 표식 규약). 파일명은 v1 유지. *(제목 줄 버전 표기가 v1.2에 머물러 있던 것을 함께 정정.)*
> v1.3 (2026-08-28): **모범답안체 전환** — 발문 기출 축약형(하시오체·甲乙 사례), 정답 답안체(결론 선행·조문 괄호·판례 문형·사안의 경우), `sub` 소유형 필드 신설, 미니답안형 1~2문 고정 편성, 검증 강화. 문형 정본 `참조_법무사2차_문형사전_v1.md` **실행 시 필독**. 근거: 기출·모범답안 문체 전수 분석(2026-08-28) — 현행 83문항 정답에 답안 논증체 표지 0건. 파일명은 v1 유지.
> v1.2 (2026-08-26): 장기복습 창 −7일(베이스 v1.2 정합) + ★ 강조 우선 출제 (`공부루틴_OS_v1.md` §4·§5). 파일명은 v1 유지.
> v1.1 (2026-08-25): 신규 원천에 `법무사/백지복습/` 추가(백지복습 보고서 편입 — 코어엔진 §4-3 (v3.3+)). 파일명은 v1 유지(스케줄러 참조 보호).
> **트랙 분할**: 법무사는 1차(객관식)·2차(주관식 단답)를 별개 시험처럼 돌린다. 이 파일은 **2차 트랙** — 노트 원천은 `법무사/` 최상위를 1차와 공유하되 **파일명 `-2차-` 포함분만** 이 트랙 소속. 산출물·원장·결과는 `법무사/2차/` 아래 완전 분리.
> 계보: 프로파일_법무사_v1(단일 트랙)에서 분할. 현행 법무사 데이터(오답 9건)는 전부 2차라 이 트랙이 승계함(2026-08-25 이관).

## 공통 파라미터

| KEY | 값 |
|---|---|
| EXAM_NAME | 법무사 2차 |
| EXAM_DIR | `법무사/2차` (velog-posts 하위 — 중첩 경로) |
| RESULT_GLOB | `법무사2차_*오답_*.json` |
| 레거시 수거(전환기) | Downloads에 구명 `법무사_*오답_*.json`(2026-08-25 이전 생성 퀴즈의 결과)이 보이면 **접두를 `법무사2차_`로 바꾼 이름으로 _inbox에 복사**한다(Downloads 원본은 불변). 새 템플릿은 처음부터 신명으로 저장하므로 9월 중 이 규칙은 소멸 |
| 데일리 결과 파일명 | `법무사2차_오답_{날짜}.json` (오답퀴즈는 `…-RQ.json`) — 미제출 경보 판정 시 구명(`법무사_` 접두, 2026-08-25 이전 퀴즈 결과)도 전환기엔 신호로 인정 |
| 원장 | 오답 수거함(`claude_ox_오답`)의 `_ledger/build_ledger.py` (2026-08-25 트랙 분리판 — RESULT_GLOB만 2차 전용) |
| 대시보드 | 없음 |
| 데일리 템플릿 | `데일리퀴즈/_template.html` (법무사 2차판 — 단답형·exam 필드 지원) |
| 오답퀴즈 템플릿 | `오답퀴즈/_template.html` (`-RQ` 파일명) |
| 카톡푸시 | 연동 금지(P-5). `_push/` 페이로드는 기록용만, subject "법무사2차" |

## 데일리퀴즈 (베이스_데일리퀴즈용)

- **신규 원천 규칙**: `$ROOT/법무사` **최상위**(-maxdepth 1)의 `20*.md` 중 파일명 날짜(mtime 아님)가 오늘−7일 이후 ∧ **파일명에 `-2차-` 포함**. 날짜 프리픽스 없는 파일·0바이트 파일 제외(0바이트는 보고에 명시). 전 문항 exam="2차" **단답형**. 정리본의 `📐 공식` 블록·판별식·⚠ 예외가 최우선 소재. **백지복습 원천(v1.1 · 수집 완화 v1.5)**: `$ROOT/법무사/백지복습` 최상위의 `*.md` 중 **mtime −7일 이내**(보고서는 갱신 시 재출제 창이 다시 열려야 하므로 파일명 날짜가 아니라 mtime). **트랙 판별**: 파일명에 `-2차-`가 있으면 2차(이 트랙), 없으면 front matter `exam:` 값으로 판별(`2차`면 이 트랙), 둘 다 없으면 보고 후 제외 — "남은 구멍"·"교정된 개념" 표가 최우선 소재, "내가 쓴 오답" 열은 오답 보기 재료. `status: 닫힘`·⚠️ 섹션 출제 금지.
- **장기복습 원천 규칙**: 같은 폴더·같은 `-2차-` 필터, 파일명 날짜 ≤ 오늘−7일.
- **장기복습 버킷 배분 (v1.5)**: 장기복습 문항의 버킷 배분은 베이스 §1-3 비율(B1:B2:B3 = 3:3:4)을 문항 수에 비례 적용하고 나머지는 B3부터 채운다 (예: 4문 = 1·1·2).
- **★ 가중(v1.2 · 앵커·우선순위 v1.4)**: 노트 안 `★`/`★★`(또는 `!`/`!!`) 표시 항목은 신규 문항의 우선 소재 — 과목 균형 내에서 ★★ 최우선. **유효한 표식은 항목·줄 맨 앞(불릿 뒤 포함)에 붙은 것만이다** — 문장 중간의 ★, 백지시트 장식(`★…★`로 감싼 형태)은 표식이 아니므로 무시한다. **같은 항목에 `⚠️`나 "확인 필요"가 있으면 ★이어도 출제 금지**(확인 필요가 우선).
- **출제 필터 (개정법)**: `ai-guidelines/20_프롬프트-라이브러리/19_법무사_개정법워치_v1.md` §2를 실행 시 직접 읽는다. **ai-guidelines 미마운트 시 최소 규칙 폴백**: 형사소송법 수사·기소 분리(2026.10 시행) 전까지 형소 수사 파트 출제 제외, 개정 영향 소재는 해설에 `⚠ 개정 반영`, 구법 확정 소재 금지, 못 읽었음을 보고.
- **구성 모델 (램프업형 — 주관식 완전체 20)**: 기계적 판정, 위에서부터 첫 일치. `신규`·`장기`=위 필터 통과 노트 수, `한달전창`=자기 데일리퀴즈 폴더 html 수:

| 단계 | 조건 | 총 문항 | 구성 |
|---|---|---|---|
| 스킵 | 신규 0 · 장기 0 | 0 | 생성 없이 "원천 없음" 1줄 보고 종료 |
| 유지 | 신규 0 (9/21~10/31 중개사 올인 구간) | 장기 ≥3이면 **12**, 아니면 **8** | 전부 장기복습(⏪) |
| S3 완전체 | 한달전창 ≥1 ∧ 장기 ≥3 | **20** | 본편 16(신규 12+장기 4) + 한달전 4 |
| S2 | 장기 ≥3 | **16** | 신규 12 + 장기복습 4 |
| S1 | 신규 ≥3 | **12** | 전부 신규 |
| S0-2 | 신규 = 2 | **10** | 전부 신규 |
| S0-1 | 신규 = 1 | **8** | 전부 신규 |

  단계·총 문항을 보고 첫머리에 명시. 노트에 없는 내용으로 문항 수를 맞추지 않는다 — 모자라면 줄이고 사유 보고. *(주관식은 문항당 인출 부하가 커서 객관식 트랙의 40% 규모가 완전체다.)*
- **한달전 폴백**: 없음 — 창이 비면 한달전 블록 미편성. 한달전은 4문·과목당 ≤2. 한달전 복원 문항은 원문 복사 원칙 그대로 — v1.3 이전 구형 문체·`sub` 부재 허용(신규 검증 면제, 전환은 30일 후 자연 완료).
- **추가 토큰**: `{{ALERT_HTML}}` → 미제출 경보 문구 또는 빈 문자열.
- **템플릿 토큰 문구**: `{{META_LINE}}` = "2026년 M월 D일 (요일) · {단계명} 단답 {N}문항(미니답안 M) · 장기복습 N(⏪) · 한달전 N(🔄)" / `{{TAGS_HTML}}` = `<span class="tag">⚖️ 법무사 2차</span><span class="tag">✍️ 단답 인출 N</span>` + 장기복습·한달전 태그(해당 시) + 과목명 태그들.
- **과목 배분**: 발견 과목 수로 균등(차이 ≤1).
- **유형 쿼터 (단답 소유형 — 전 문항 `sub` 필드 필수, 비율은 가이드·편중 금지)**: 결론이유형 ~25% / 요건·개수형 ~20% / 판별식형 ~15% / 판례문구형 ~15%(노트에 판례 태도가 있을 때만 — **상습 미달 유형**(실측 4%)이니 소재가 있으면 우선 편성, 판례 현출이 2차 사활) / 목차·순서형 ~10% / 용어·정의형 ~10%(단순 정의 15% 이하). 여기에 **미니답안형 고정 편성: 총 문항 12 이상 = 2문, 8~10 = 1문, 유지 모드 = 0문**(중개사 올인 구간 보호) — 본편에서만, 한달전 제외. `sub` 값 7종: `결론이유`·`요건개수`·`판별식`·`목차순서`·`판례문구`·`용어정의`·`미니답안`. **객관식·계산형 금지 — 이 트랙에 opts 있는 문항이 나오면 잘못 만든 것.**
- **문항 특칙 (모범답안체 계약 v1.3 — 문형·표준 예시 정본은 `참조_법무사2차_문형사전_v1.md`, 실행 전 필독)**:
  - 필드 계약: `answer`(모범답안체 문자열) + `keywords`(논증 단계 채점 앵커 2~4개 배열) + `sub`(소유형) + `exam:"2차"`, `opts` 없음. 정답 힌트 발문 유출 금지.
  - **발문 = 기출 축약형**: 종결은 하시오체(서술하시오/기재하시오/논하시오 — 쓰라·나열하라 해라체 금지). 사례형은 甲·乙·X 토지 표기로 사실관계 1~3문장 압축. 설문은 기출 상투구("…에 대한 결론과 그 이유를 기재하시오" 등 — 문형사전 §1-2). 배점 표기는 미니답안형만 "(10점)". 마커(⏪🔄🔁)는 발문 맨 앞 유지.
  - **정답 = 답안체 6규칙** (문형사전 §2): ① 답안지에 그대로 옮겨 적을 수 있는 문장만 — 해설자 요약체 금지 ② 결론을 묻는 문항은 결론 단문 선행 + 발문 어미와 문법 호응 ③ 조문은 문장 뒤 괄호 "(형법 제21조 제1항)" — 노트에 명시된 것만, 낫표(「」) 금지 ④ 판례는 "판례는 ~는 입장이다" 문형, 인용은 홑따옴표 ⑤ 사례형은 "사안의 경우" 포섭 표지 + "~라 할 것이다/~함이 타당하다/~하여야 한다" 종결 표지 ⑥ 짧은 단문 — 일반 정답 2~4문장·260자 이내. 학설 서술 금지(판례가 취한 학설 명칭 1개만 허용).
  - **미니답안형**: 10점형 2단 골격을 `<br>` 문단 3개로 — [결론 1문장]<br>[법리: 요건·조문 + 판례 태도]<br>[사안의 경우 포섭 → 종결]. 150~500자, 문항 목표 3~4분. (화면의 목표시간은 90초/문 고정 표시라 미니답안 포함 시 실제 총 소요는 표시보다 +5분 내외 — 정상.)
  - keywords = [결론 단어]·[조문/요건]·[판례 핵심어]·[포섭 변수] 중 문항에 있는 것을 답안 등장 순서대로 2~4개. expl은 3문장 250자 유지하되 셋째 문장 = 감점·함정 포인트(문형사전 §5).
  - 기술 제약: 줄바꿈은 `<br>`만(개행문자는 렌더 안 됨), `<`는 br 외 절대 금지(innerHTML 주입), 큰따옴표·백틱 금지 유지. "(원문 확인 필요)"류 태그 금지(그 소재는 버림).
  - **역할 경계 (P4)**: 이 퀴즈는 문형 각인·논점 인출까지 — 완전 서술 연습은 주말 슬롯·순환 모의고사 몫(18호 §5-4 실증). 미니답안형을 하루 2문 넘게 편성하지 않는다.
- **추가 안티패턴**: 구법 소재 / 확인-필요 태그 잔존 / 객관식 혼입 / 해설자 요약체 정답(답안체 6규칙 위반) / br 외 태그·낫표 조문 인용 / 해라체 발문 / 미니답안형 초과 편성.
- **검증 스크립트** (그대로 실행, 통과까지 반복):

```bash
cd "$QD" && node -e '
const fs=require("fs"),path=require("path");
const today=process.argv[1];
const NOTES=path.resolve("../..");
const day=s=>Math.round((new Date(today)-new Date(s))/864e5);
const mds=fs.readdirSync(NOTES).filter(f=>/^20\d{2}-\d{2}-\d{2}.*\.md$/.test(f)&&f.includes("-2차-")&&fs.statSync(path.join(NOTES,f)).size>0);
const nNew=mds.filter(f=>{const d=day(f.slice(0,10));return d>=0&&d<7;}).length;
const nLong=mds.filter(f=>day(f.slice(0,10))>=7).length;
const nMon=fs.readdirSync(".").filter(f=>/^20\d{2}-\d{2}-\d{2}\.html$/.test(f)&&day(f.slice(0,10))>=27&&day(f.slice(0,10))<=33).length;
let stage,exp;
if(nNew===0&&nLong===0){stage="스킵";exp=0;}
else if(nNew===0){stage="유지";exp=nLong>=3?12:8;}
else if(nMon>=1&&nLong>=3){stage="S3";exp=20;}
else if(nLong>=3){stage="S2";exp=16;}
else if(nNew>=3){stage="S1";exp=12;}
else {stage="S0";exp=nNew===2?10:8;}
console.log("단계="+stage+" 기대문항="+exp+" (신규 "+nNew+"·장기 "+nLong+"·한달전창 "+nMon+")");
const html=fs.readFileSync(today+".html","utf8");
const Q=eval(html.match(/const QUESTIONS = (\[[\s\S]*?\n\]);/)[1]);
let fail=[]; const ok=(c,m)=>{ if(!c) fail.push(m); console.log((c?"PASS":"FAIL")+" — "+m); };
ok(!html.includes("{{"),"토큰 잔존 없음");
ok(!/\((원문|판례 번호|예규 번호|현행 예규) 확인 필요\)/.test(html),"태그 위생: 확인-필요류 잔존 0");
ok(Q.length===exp,"문항 수 "+exp+" (현재 "+Q.length+")");
const M=Q.filter(q=>q.monthlyOf), B=Q.filter(q=>!q.monthlyOf);
ok(M.length===(stage==="S3"?4:0),"한달전 "+(stage==="S3"?4:0)+" (현재 "+M.length+")");
if(M.length) ok(Q.slice(-M.length).every(q=>q.monthlyOf)&&Q.slice(0,Q.length-M.length).every(q=>!q.monthlyOf),"한달전 블록 맨 뒤");
ok(Q.every(q=>q.q&&q.expl&&q.src&&q.cat&&q.type&&q.conceptKey&&q.exam),"필수 필드(exam·conceptKey)");
ok(Q.every(q=>q.type==="단답"&&q.exam==="2차"),"전 문항 단답·exam 2차 (혼입 금지)");
ok(Q.every(q=>typeof q.answer==="string"&&q.answer.length>=2&&!q.opts),"단답: answer 문자열·opts 없음");
ok(Q.every(q=>Array.isArray(q.keywords)&&q.keywords.length>=2&&q.keywords.length<=4),"단답: keywords 2~4개");
ok(Q.every(q=>!q.calc),"계산형 없음");
const SUBS=["결론이유","요건개수","판별식","목차순서","판례문구","용어정의","미니답안"];
ok(B.every(q=>SUBS.includes(q.sub)),"본편 sub 소유형 유효 (7종)");
const mini=B.filter(q=>q.sub==="미니답안");
const miniExp=(stage==="유지")?0:(exp>=12?2:1);
ok(mini.length===miniExp,"미니답안 "+miniExp+"문 (현재 "+mini.length+")");
ok(mini.every(q=>q.answer.includes("<br>")&&q.answer.length>=150&&q.answer.length<=500&&/\(\d+점\)/.test(q.q)),"미니답안 골격(br 문단·150~500자·배점 표기)");
ok(B.filter(q=>q.sub!=="미니답안").every(q=>q.answer.length<=260),"일반 정답 260자 이내");
ok(Q.every(q=>!/<(?!br\s*\/?>)/i.test(String(q.q)+String(q.answer)+String(q.expl))),"태그는 br만 (그 외 < 금지)");
ok(Q.every(q=>!/「|」/.test(String(q.answer))),"조문 인용 괄호형 (낫표 금지)");
ok(B.every(q=>/(하시오|하십시오|기재하시오|서술하시오|쓰시오|논하시오|채우시오)/.test(q.q)),"발문 기출체 (하시오체 포함)");
const re=B.filter(q=>q.retryOf||String(q.q).includes("🔁")).length;
ok(re===0,"재도전 0 — 오답퀴즈 전담 (현재 "+re+")");
const lr=B.filter(q=>q.q.includes("⏪")).length;
if(stage==="S2"||stage==="S3") ok(lr===4,"장기복습 4 (현재 "+lr+")");
if(stage==="유지") ok(lr===B.length,"유지 모드: 전부 장기복습 (현재 "+lr+"/"+B.length+")");
ok(B.filter(q=>q.q.includes("⏪")).every(q=>/\(장기복습·\d+일전\)/.test(q.src)),"장기복습 표기 일관");
if(M.length){
  ok(M.every(q=>q.q.includes("🔄")&&/\(한달전 재출제·원본 \d{4}-\d{2}-\d{2}\)/.test(q.src)&&/^\d{4}-\d{2}-\d{2}$/.test(q.monthlyOf)),"한달전 표기 일관");
  const mc={}; M.forEach(q=>mc[q.cat]=(mc[q.cat]||0)+1);
  ok(Math.max(...Object.values(mc))<=2,"한달전 과목 골고루(≤2) "+JSON.stringify(mc));
}
const seq=B.map(q=>q.cat); const seen={}; let contig=true;
seq.forEach((c,i)=>{ if(!(c in seen)) seen[c]=1; else if(seq[i-1]!==c) contig=false; });
ok(contig,"같은 과목 연속 배치");
const cats={}; B.forEach(q=>cats[q.cat]=(cats[q.cat]||0)+1); console.log("  과목 분포: "+JSON.stringify(cats));
const norm=s=>String(s).replace(/<[^>]+>/g,"").replace(/[\s🔁🔄⏪]/g,"");
const past=new Set();
fs.readdirSync(".").filter(f=>f.endsWith(".html")&&!f.startsWith(today)&&!f.startsWith("_")).forEach(f=>{
  (fs.readFileSync(f,"utf8").match(/q: *"[^"]*"/g)||[]).forEach(m=>past.add(norm(m.replace(/^q: *"/,"").replace(/"$/,""))));
});
const dup=B.filter(q=>{const n=norm(q.q);return [...past].some(p=>p.slice(0,30)===n.slice(0,30));});
ok(dup.length===0,"과거 문제문과 도입부 중복 없음"+(dup.length?" — "+dup.map(d=>d.q.slice(0,20)).join(" | "):""));
console.log(fail.length? "\n❌ "+fail.length+"건 — 수정 후 재실행":"\n✅ 전체 통과");
process.exit(fail.length?1:0);
' "$(date +%F)"
```

- **푸시 페이로드** (검증 통과 후 `$QD/_push/{오늘날짜}.json` — 기록용, kakao 미연동): 구 법무사 스키마 그대로에 `"subject":"법무사2차"`. `counts.재도전` 항상 0, `ox` 3개(오늘 원천 노트 개념에서, 큰따옴표·백틱 금지), `alert` = 경보 문구 또는 null, `dueBacklog` = 듀 큐 길이(원장 없으면 null). 생성 후 python json 파싱 검증.
- **미제출 경보**: 직전 2개 실행일의 데일리 제출 신호 확인 — 신호 = `법무사2차_오답_{날짜}.json`(± 사본, 전환기엔 구명 `법무사_` 접두도 인정), `-RQ` 제외. Downloads 미마운트면 확인 불가 처리(오탐 금지). 2회 연속 미제출 시 `{{ALERT_HTML}}` = `⚠ 최근 2회 미제출 · 인출 루프 단절 — 풀지 않은 문제지는 회독이 아니라 장식입니다. 오늘 것부터 제출.`
- **11/1 평일 전환 체크리스트** (그 전 실행 금지): ① cron 화·목 → 평일 ② 신규 원천에 기출 원문(`법무사/기출원문/`) 보충 허용 ③ 이 프로파일 v2 개정·기록.

## 오답퀴즈 (베이스_오답파이프라인용)

- **문항 형식**: 전 문항 단답형(answer+keywords+`sub`, exam:"2차") — 이 트랙 원장의 개념은 전부 단답 출신. **모범답안체 계약(위 데일리 문항 특칙 + `참조_법무사2차_문형사전_v1.md`)을 그대로 적용한다.** 변형 출제 각도로 문형사전 §5 감점 패턴(결론 카테고리 누락·범위 이탈·포섭 생략·조문 미인용 등)을 우선 활용. 미니답안형은 듀 개념이 사례·결론형일 때만 **최대 2문**(고정 편성 아님 — 듀 소재가 정한다). 계산형·객관식 금지. 개정법 필터 동일 적용.
- **오답 상한 (v1.5)**: `N = min(8, 듀 큐 길이)` — 주관식은 인출 부하가 커서 객관식 기본값 20의 40% 규모다(데일리 완전체 20 = 객관식 50의 40%와 같은 원칙).
- **검증 스크립트**: ① 베이스_오답파이프라인의 공통 스크립트를 인자 `"$ROOT/법무사/2차"`로 실행 → ② 이어서 아래 **2차 전용 추가 검증**을 실행. 둘 다 통과까지 반복:

```bash
cd "$RQ" && node -e '
const fs=require("fs");
const today=process.argv[1];
const html=fs.readFileSync(today+".html","utf8");
const Q=eval(html.match(/const QUESTIONS = (\[[\s\S]*?\n\]);/)[1]);
let fail=[]; const ok=(c,m)=>{ if(!c) fail.push(m); console.log((c?"PASS":"FAIL")+" — "+m); };
const SUBS=["결론이유","요건개수","판별식","목차순서","판례문구","용어정의","미니답안"];
ok(Q.every(q=>q.type==="단답"&&q.exam==="2차"&&!q.opts),"전 문항 단답·2차");
ok(Q.every(q=>SUBS.includes(q.sub)),"sub 소유형 유효 (7종)");
const mini=Q.filter(q=>q.sub==="미니답안");
ok(mini.length<=2,"미니답안 2문 이하 (현재 "+mini.length+")");
ok(mini.every(q=>q.answer.includes("<br>")&&q.answer.length>=150&&q.answer.length<=500&&/\(\d+점\)/.test(q.q)),"미니답안 골격(br 문단·150~500자·배점 표기)");
ok(Q.filter(q=>q.sub!=="미니답안").every(q=>q.answer.length<=260),"일반 정답 260자 이내");
ok(Q.every(q=>!/<(?!br\s*\/?>)/i.test(String(q.q)+String(q.answer)+String(q.expl))),"태그는 br만 (그 외 < 금지)");
ok(Q.every(q=>!/「|」/.test(String(q.answer))),"조문 인용 괄호형 (낫표 금지)");
ok(Q.every(q=>/(하시오|하십시오|기재하시오|서술하시오|쓰시오|논하시오|채우시오)/.test(q.q)),"발문 기출체 (하시오체 포함)");
console.log(fail.length?"\n❌ "+fail.length+"건 — 수정 후 재실행":"\n✅ 2차 추가검증 통과");
process.exit(fail.length?1:0);
' "$(date +%F)"
```

## 주간리포트 (베이스_주간리포트용)

- **대시보드**: 없음 — 원장 갱신까지만.
- 원장 초기(오답 9건 승계, 2026-08-24 가동) — 베이스 §4 "원장 초기" 규칙 적용.
