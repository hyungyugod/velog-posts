# 베이스_앙키덱 v1.1 — 주간 Anki 원자카드 덱 생성 + 숫자 총정리 갱신 절차 (2026-08-27)

> v1.1: §6 숫자 총정리 갱신(n: 태그 주간 시퀀스) 신설 — 같은 월요일 실행에 겸임 (스케줄러 추가 없음).

> 매주 월요일 아침 실행 (주간리포트와 같은 날 — 주간 사이클 마감과 함께). **원자층 전용** — 3초 안에 답하는 사실(숫자·기간·비율·요건 개수·두문자·판례결론 1줄)만 카드화한다. 문항형·서술형 카드는 만들지 않는다(적용층 퀴즈 엔진과 이중 복습 금지 — `공부루틴_OS_v1.md` §3 경계 규칙).
> 이 베이스는 프로파일 없이 단독 실행한다(시험 횡단 작업). 시험 폴더 경로만 아래 표를 따른다.

**[실행 원칙] ① 원문(원장 samples·정리본·백지복습 보고서)에 없는 내용 카드화 절대 금지 — 틀린 카드는 틀린 기억을 영구 각인한다 ② "(확인 필요)"·"⚠️" 항목 카드화 금지 ③ 조문번호·수치가 원문에 명시돼 있지 않으면 그 부분은 카드에서 뺀다 ④ `!`/`★` 표시 항목은 **전량 의무 편입 — 상한 제외** (데일리퀴즈가 모든 내용을 복습하지 못하므로 강조 표시는 절대 놓치지 않는다). 그 외 원천은 주 25장 상한 — 소재가 많으면 상습 > 백지 구멍 순으로 자르고 사유 보고 ⑤ 검증 통과 전 종료 금지.**

## 0. 경로 (마운트 지연 대비 — 데일리퀴즈 베이스 §0과 동일한 재시도)

```bash
for i in 1 2 3 4 5; do
  ROOT=$(find /sessions/*/mnt -maxdepth 2 -type d -name velog-posts 2>/dev/null | head -1)
  [ -n "$ROOT" ] && break; echo "velog-posts 대기... ($i/5)"; sleep 5
done
AK=$ROOT/_시험엔진/anki
```
- 5회 후에도 비면 파일 도구로 `~/Desktop/velog-posts` 직접 확인, 실패 시 "접근 불가" 보고 후 종료.
- `$AK/출고/{오늘날짜}.apkg` 존재 시 → "이번 주 덱은 이미 생성됨" 보고 후 종료.

## 1. 후보 수집 (시험별 — 활성 시험만)

| 원천 | 위치 | 추출 규칙 |
|---|---|---|
| ① 상습 오답 | `{시험폴더}/claude_ox_오답/_ledger/오답_원장.json`의 `ledger` 배열 | `timesWrong>=2 or retryMissed>=1` (status 문자열로 거르지 말 것 — 놓친다). 각 항목의 `samples`(q·expl)가 카드 문안의 유일한 근거 |
| ② !·★ 강조 | `{시험폴더}` 최상위 `20*.md` 중 **mtime −7일** (이번 주 정리본) | `!`/`!!`/`★`/`★★`가 앞에 붙은 문장 **전량 의무 편입(상한 제외)**. 문장이 원자 사실이 아니면 원자 사실 여러 장으로 쪼개 카드화한다 |
| ③ 백지 구멍 | `{시험폴더}/백지복습/*.md` mtime −7일 | "남은 구멍"·"놓친 개념" 표의 나열형·숫자형만. `status: 닫힘` 보고서 제외 |

- 시험폴더: `공인중개사` / `법무사` (법무사는 1차·2차 노트 공유 폴더 — 파일명 `-2차-` 유무로 태그만 구분).
- 프로파일에 `[종료: ...]`가 표기된 시험은 건너뛴다.
- **중복 방지**: `$AK/카드_*.tsv` 전체의 key 열을 취합해, 이미 출고된 key는 내용 갱신이 필요한 경우(원문이 바뀜)에만 재출고한다. 같은 key 재출고는 Anki에서 중복이 아니라 갱신이 된다(guid 결정론).

## 2. 카드 문안 작성 (원자화)

- 형식: cloze 우선. `도시개발조합 설립인가 동의: 토지면적 {{c1::3분의 2}} 이상 + 토지소유자 총수 {{c2::2분의 1}} 이상`
- basic은 "앞면 질문 1줄 → 뒷면 사실 1줄"이 자연스러운 것만 (예: 두문자 전개).
- 앞면에 맥락 1줄 필수 — 카드만 보고 무엇의 이야기인지 알게.
- 한 개념에서 사실이 2개면 카드 2장으로 쪼갠다 (cloze c1·c2는 같은 문장 안 동시 암기가 자연스러울 때만).
- 함정 대비: 원문 expl의 혼동 대상("농공단지 70%·산업단지 80%와 혼동")은 뒷면 보충(back)에 1줄로.
- key: 원장 항목은 `conceptKey` 그대로 / ★·백지 구멍은 `과목-소제목-요지` 짧게 신규 부여. 한 conceptKey에서 카드를 여러 장으로 쪼갤 때 둘째 장부터 `conceptKey-접미사`로 분기한다(key 중복 방지·guid 안정 — 예: `부동산공시법 1-3-관상수`).
- deck: `자격증::공인중개사::{과목}` / `자격증::법무사::{과목}` / tags: 시험명·과목·원천(상습/강조/백지).

## 3. TSV → 빌드

- `$AK/카드_{오늘날짜}.tsv` 작성 — 컬럼: `deck / type(basic|cloze) / front / back / tags(공백 구분) / src(출처 노트·원장 파일명) / key`. 필드 안 탭·줄바꿈 금지(줄바꿈은 `<br>`).
- 빌드: `pip install genanki --break-system-packages` 후 `python3 "$AK/build_apkg.py" "$AK/카드_{오늘날짜}.tsv"` → `$AK/출고/{오늘날짜}.apkg`.
- 특집 덱(별도 TSV)은 `카드_{날짜}-{특집명}.tsv`로 만들면 출력도 `{날짜}-{특집명}.apkg`로 분리된다 (빌더가 접미사 보존 — 2026-08-26 덮어쓰기 실사고 후 근본 수정).
- 빌더가 검증까지 수행(카드 수·cloze 문법·key 중복·빈 값 — FAIL 시 exit 1). FAIL이면 TSV 수정 후 재실행, 통과 전 종료 금지.
- **빌더 스크립트 수정 금지** (모델 ID 2종은 불가침 — 바꾸면 기존 카드 학습 이력이 끊긴다).

## 4. 검수리포트 (게이트 — 생략 불가)

`$AK/출고/검수_{오늘날짜}.md` 생성:
- 머리: 총 카드 수 · 시험/과목별 분포 · 원천별 분포(상습/강조/백지)
- 본문: 카드 전문 표 — `# | deck | 앞면 | 뒷면 | 원천 | src`
- 꼬리: 상한 초과로 잘라낸 후보 목록(다음 주 이월), 카드화 보류 항목(확인 필요 사유)

## 5. 전달

- present_files: `.apkg` + 검수리포트.
- 요약 3문장: 카드 수·원천 구성·보류 항목. 안내 고정 문구: "검수리포트 훑고 이상 없으면 .apkg 더블클릭 → Anki 임포트 → 동기화하세요. 이상한 카드가 있으면 번호로 알려주면 다음 출고에서 수정합니다."
- 사용자가 카드 수정을 요청하면: 같은 key로 TSV 수정 → 재빌드 → 재임포트 시 갱신됨을 안내.

## 6. 숫자 총정리 갱신 — n: 태그 주간 시퀀스 (v1.1)

덱 작업(§1~5)을 마친 뒤 같은 실행에서 이어서 한다. **숫자·날짜·기간은 흩어지면 못 외운다** — 사용자가 `n:`을 붙인 항목을 매주 시험별 총정리본에 통합한다.

1. **수집**: 각 활성 시험폴더 최상위 `20*.md` 중 **mtime −7일** 정리본에서 `n:` 이 붙은 라인 전부 grep (원문 필기의 `n:`이 정리본에 승계된 것).
2. **통합 대상 정본**:
   - 공인중개사 → `공인중개사/공인중개사_날짜기간_총정리_v1.md`
   - 법무사 → `법무사/법무사_숫자체계_v1.md` — **첫 n: 항목 발견 시 같은 양식(과목별 표 + 숫자 역인덱스 + 충돌 섹션)으로 생성**, 그 전엔 스킵 보고
3. **통합 규칙**: ① 신규 항목만 추가(기존 항목 중복 금지 — 항목명 대조) ② 해당 과목 표에 `항목 | 기산일 | 기간·수치 | 혼동 주의` 행 추가 ③ **숫자 역인덱스의 해당 숫자 버킷에도 편입** ④ 기존 항목과 수치가 충돌하면 본문에 넣지 말고 `⚠️ 충돌` 섹션에 나란히 기록 후 보고에 명시 ⑤ 원문에 없는 내용·기산일 추정 금지(없으면 `—`)
4. **보고**: 시험별 신규 N건 · 충돌 N건 · 역인덱스 최밀집 숫자 변동 여부 1줄.
5. n: 항목의 Anki 편입은 자동이 아니다 — `!`가 같이 붙은 항목만 §1 의무 편입 규칙을 따른다 (카드 폭증 방지).
