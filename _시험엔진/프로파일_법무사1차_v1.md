# 프로파일_법무사1차 v1.3 (2026-08-28)

> 베이스(데일리퀴즈·오답파이프라인·주간리포트)의 `⟨프로파일: KEY⟩` 슬롯을 채우는 파생 정의. 베이스와 충돌 시 이 파일이 우선.
> v1.3 (2026-08-28): **기출 실전형 전환** — 전 문항 5지선다(템플릿 무수정 동작 확인), 발문 화이트리스트("가장 옳지 않은 것은?" 기본·부정형 65~90%), 조합·개수형 규격, 함정 패턴 T1~T12, OX 폐지(기출 미출제), 오답퀴즈 검증을 프로파일 완결 스크립트로 대체(베이스의 4지 계약과 충돌 — 베이스는 중개사 공용이라 무수정). 근거: 제29~31회 600문항 전수 실측. 형식 정본 `참조_법무사1차_기출문형_v1.md` **실행 시 필독**. 파일명 v1 유지.
> v1.2 (2026-08-26): 장기복습 창 −7일(베이스 v1.2 정합) + ★ 강조 우선 출제 (`공부루틴_OS_v1.md` §4·§5). 파일명은 v1 유지.
> v1.1 (2026-08-25): 신규 원천에 `법무사/백지복습/` 추가(백지복습 보고서 편입 — 코어엔진 v3.3 §4-3). 파일명은 v1 유지(스케줄러 참조 보호).
> **트랙 분할**: 법무사는 1차(객관식)·2차(주관식 단답)를 별개 시험처럼 돌린다. 이 파일은 **1차 트랙** — 노트 원천은 `법무사/` 최상위를 2차와 공유하되 **파일명에 `-2차-`가 없는 날짜 정리본만** 이 트랙 소속. 산출물·원장·결과는 `법무사/1차/` 아래 완전 분리.
> 현재 1차 노트 0개 — 스킵 단계로 대기하는 게 정상. 1차 학습이 시작되면(노트 유입) 자동으로 램프업이 가동된다. 목표 완전체 = 객관식 50문(공인중개사와 같은 규모).

## 공통 파라미터

| KEY | 값 |
|---|---|
| EXAM_NAME | 법무사 1차 |
| EXAM_DIR | `법무사/1차` (velog-posts 하위 — 중첩 경로) |
| RESULT_GLOB | `법무사1차_*오답_*.json` |
| 데일리 결과 파일명 | `법무사1차_오답_{날짜}.json` (오답퀴즈는 `…-RQ.json`) |
| 원장 | `_ledger/build_ledger.py` (2026-08-25 트랙 분리판 — RESULT_GLOB만 2차판과 다름, 경로는 EXAM_DIR 하위 오답 수거함 폴더) |
| 대시보드 | 없음 |
| 데일리 템플릿 | `데일리퀴즈/_template.html` (법무사 1차판 — 단답 기능은 잔존하나 이 트랙은 사용 안 함) |
| 오답퀴즈 템플릿 | `오답퀴즈/_template.html` (`-RQ` 파일명) |
| 카톡푸시 | 연동 금지(P-5). `_push/` 페이로드는 기록용만, subject "법무사1차" |

## 데일리퀴즈 (베이스_데일리퀴즈용)

- **신규 원천 규칙**: `$ROOT/법무사` **최상위**(-maxdepth 1)의 `20*.md` 중 파일명 날짜(mtime 아님)가 오늘−7일 이후 ∧ **파일명에 `-2차-` 미포함**. 날짜 프리픽스 없는 파일·0바이트 파일 제외(0바이트는 보고에 명시). 전 문항 exam="1차" **5지선다 객관식**. 정리본의 `📐 공식` 블록·판별식·⚠ 예외가 최우선 소재. **백지복습 원천(v1.1)**: `$ROOT/법무사/백지복습` 최상위의 `20*.md` 중 **mtime −7일 이내**(보고서는 갱신 시 재출제 창이 다시 열려야 하므로 파일명 날짜가 아니라 mtime) ∧ 파일명에 `-2차-` 미포함 — "남은 구멍"·"교정된 개념" 표가 최우선 소재, "내가 쓴 오답" 열은 오답 보기 재료. `status: 닫힘`·⚠️ 섹션 출제 금지.
- **장기복습 원천 규칙**: 같은 폴더·같은 `-2차- 미포함` 필터, 파일명 날짜 ≤ 오늘−7일.
- **★ 가중(v1.2)**: 노트 안 `★`/`★★`(또는 `!`/`!!`) 표시 항목은 신규 문항의 우선 소재 — 과목 균형 내에서 ★★ 최우선.
- **출제 필터 (개정법)**: `ai-guidelines/20_프롬프트-라이브러리/19_법무사_개정법워치_v1.md` §2를 실행 시 직접 읽는다. **ai-guidelines 미마운트 시 최소 규칙 폴백**: 형사소송법 수사·기소 분리(2026.10 시행) 전까지 형소 수사 파트 출제 제외, 개정 영향 소재는 해설에 `⚠ 개정 반영`, 구법 확정 소재 금지, 못 읽었음을 보고.
- **구성 모델 (램프업형 — 객관식 완전체 50)**: 기계적 판정, 위에서부터 첫 일치. `신규`·`장기`=위 필터 통과 노트 수, `한달전창`=자기 데일리퀴즈 폴더 html 수:

| 단계 | 조건 | 총 문항 | 구성 |
|---|---|---|---|
| 스킵 | 신규 0 · 장기 0 | 0 | 생성 없이 "원천 없음" 1줄 보고 종료 |
| 유지 | 신규 0 | 장기 ≥3이면 **20**, 아니면 **15** | 전부 장기복습(⏪) |
| S3 완전체 | 한달전창 ≥1 ∧ 장기 ≥3 | **50** | 본편 40(신규 30+장기 10) + 한달전 10 |
| S2 | 장기 ≥3 | **40** | 신규 30 + 장기복습 10 |
| S1 | 신규 ≥3 | **30** | 전부 신규 |
| S0-2 | 신규 = 2 | **20** | 전부 신규 |
| S0-1 | 신규 = 1 | **15** | 전부 신규 |

  단계·총 문항을 보고 첫머리에 명시. 노트에 없는 내용으로 문항 수를 맞추지 않는다 — 모자라면 줄이고 사유 보고.
- **한달전 폴백**: 없음 — 창이 비면 한달전 블록 미편성. 한달전은 10문·과목당 ≤3.
- **추가 토큰**: `{{ALERT_HTML}}` → 미제출 경보 문구 또는 빈 문자열.
- **템플릿 토큰 문구**: `{{META_LINE}}` = "2026년 M월 D일 (요일) · {단계명} 객관식 {N}문항 · 장기복습 N(⏪) · 한달전 N(🔄)" / `{{TAGS_HTML}}` = `<span class="tag">⚖️ 법무사 1차</span><span class="tag">📝 실전 객관식 N</span>` + 장기복습·한달전 태그(해당 시) + 과목명 태그들.
- **과목 배분**: 발견 과목 수로 균등(차이 ≤1). *(11/1 이후 본격 가동 시 문항수 비례 가중으로 교체: 민법 8/민집 7/부등 6/상법 6/공탁 4/헌법 4/상등 3/가족 2 — 40문 기준·미진도 과목 제외 재배분.)*
- **유형 쿼터 (전 문항 5지선다 — 기출 실측 반영, 규격 정본 `참조_법무사1차_기출문형_v1.md` §2)**: 일반형 ~40%(선별·조문형 포함) / 판례결론형 ~30%(해설에 **결론을 가른 사실관계 변수 1개** 필수) / 사례형 ~15%(발문은 표준형 유지, 선지 안에 甲乙 사실관계 압축 — 별도 사례 박스 발문은 기출에 없음) / 조합형 ~10%(지문 4~6개 `<br>ㄱ.` 나열 + 선지 3개 조합 5쌍, 헌법·민집·상등 소재엔 편성 금지) / 개수형 세트당 0~1문(부동산등기·가족관계등록 소재일 때만 — 타 과목 기출 0건). **OX 폐지(기출 미출제, v1.3)·계산형·단답형 금지 — 4지 문항이 나오면 잘못 만든 것**(수치는 VOLATILE — 산식·판단 순서만 소재로).
- **문항 특칙 (기출 실전형 v1.3 — 발문·지문·함정 정본은 `참조_법무사1차_기출문형_v1.md` 필독)**:
  - 발문: "[주제어]에 관한 다음 설명 중 가장 옳지 않은 것은?"이 기본형 — 문형사전 §1-1 화이트리스트 밖 발문 금지, "가장" 누락 금지, **부정형 65~90%**(기출 실측 85%). 과목 블록 첫 문항 발문 끝에 해당 과목 지시문 1회 부착(§1-2 — 매 문항 반복은 기출과 다름). 밑줄형·보기 박스 금지(기출 0건).
  - 지문: 판례는 판결번호·"판례에 따르면" 없이 결론 평서화(P1), 조문은 번호+제도명(P2 — 노트 명시분만), 부등법 소재는 지문 5개 중 2~3개를 예규·선례 근거로(실측 예규 47%). **지문 길이 타깃 준수(§3 표: 민·상 100~120자 등 — 이게 실전 난도의 실체다, 짧고 쉬운 지문은 기출 감각을 훼손)**, 상한 200자(세트당 1~2개만 350자 허용).
  - 오답 지문 = 함정 패턴 T1~T12(§4)로 제작: T1 결론반전 30% / T5·T7 절차·기관 교체 20% / T6 기간·수치 15% / T3 원칙·예외 15% / T8 유사제도 10% / T2 절대화어 10%(세트당 3문 이내). 더미 금지, 정답 보기 길이 티 안 나게.
  - 정답 사전 배정: **0~4 균등**(차이 ≤3)·같은 인덱스 3연속 금지 — 베이스 §2의 "0~3 균등"은 4지용, 이 트랙이 대체한다. opts 문자열에 ①② 번호 금지(템플릿이 A~E 라벨 자동 부여). 발문 내 줄바꿈은 `<br>`만(개행문자 렌더 안 됨).
  - 근거 표기: 조문은 노트에 명시된 것만, 불확실한 번호는 번호 없이 내용으로, "(원문 확인 필요)"류 태그 금지.
- **추가 안티패턴**: 구법 소재 / 확인-필요 태그 잔존 / 단답형 혼입 / OX·4지 혼입 / 지문 내 판결번호·판례 인용구 / 화이트리스트 밖 발문 / 밑줄·보기박스 발문.
- **검증 스크립트** (그대로 실행, 통과까지 반복):

```bash
cd "$QD" && node -e '
const fs=require("fs"),path=require("path");
const today=process.argv[1];
const NOTES=path.resolve("../..");
const day=s=>Math.round((new Date(today)-new Date(s))/864e5);
const mds=fs.readdirSync(NOTES).filter(f=>/^20\d{2}-\d{2}-\d{2}.*\.md$/.test(f)&&!f.includes("-2차-")&&fs.statSync(path.join(NOTES,f)).size>0);
const nNew=mds.filter(f=>{const d=day(f.slice(0,10));return d>=0&&d<=7;}).length;
const nLong=mds.filter(f=>day(f.slice(0,10))>=14).length;
const nMon=fs.readdirSync(".").filter(f=>/^20\d{2}-\d{2}-\d{2}\.html$/.test(f)&&day(f.slice(0,10))>=27&&day(f.slice(0,10))<=33).length;
let stage,exp;
if(nNew===0&&nLong===0){stage="스킵";exp=0;}
else if(nNew===0){stage="유지";exp=nLong>=3?20:15;}
else if(nMon>=1&&nLong>=3){stage="S3";exp=50;}
else if(nLong>=3){stage="S2";exp=40;}
else if(nNew>=3){stage="S1";exp=30;}
else {stage="S0";exp=nNew===2?20:15;}
console.log("단계="+stage+" 기대문항="+exp+" (신규 "+nNew+"·장기 "+nLong+"·한달전창 "+nMon+")");
const html=fs.readFileSync(today+".html","utf8");
const Q=eval(html.match(/const QUESTIONS = (\[[\s\S]*?\n\]);/)[1]);
let fail=[]; const ok=(c,m)=>{ if(!c) fail.push(m); console.log((c?"PASS":"FAIL")+" — "+m); };
ok(!html.includes("{{"),"토큰 잔존 없음");
ok(!/\((원문|판례 번호|예규 번호|현행 예규) 확인 필요\)/.test(html),"태그 위생: 확인-필요류 잔존 0");
ok(Q.length===exp,"문항 수 "+exp+" (현재 "+Q.length+")");
const M=Q.filter(q=>q.monthlyOf), B=Q.filter(q=>!q.monthlyOf);
ok(M.length===(stage==="S3"?10:0),"한달전 "+(stage==="S3"?10:0)+" (현재 "+M.length+")");
if(M.length) ok(Q.slice(-M.length).every(q=>q.monthlyOf)&&Q.slice(0,Q.length-M.length).every(q=>!q.monthlyOf),"한달전 블록 맨 뒤");
ok(Q.every(q=>q.q&&q.expl&&q.src&&q.cat&&q.type&&q.conceptKey&&q.exam),"필수 필드(exam·conceptKey)");
ok(Q.every(q=>q.exam==="1차"&&q.type!=="단답"),"전 문항 객관식·exam 1차 (단답 혼입 금지)");
ok(Q.every(q=>Number.isInteger(q.answer)&&q.opts&&q.opts.length===5&&q.answer>=0&&q.answer<5),"객관식 계약(5지선다·정답 0~4)");
ok(Q.every(q=>q.type!=="OX"),"OX 없음 (기출 미출제 유형)");
ok(Q.every(q=>q.type!=="계산"&&!q.calc),"계산형 없음");
const STEM=/(가장 옳은 것은|가장 옳지 않은 것은|모두 고른 것은|몇 개인가|해당하지 않는 것은|아닌 것은|다른 하나는|고른 것은|연결된 것은|들어갈 내용으로 옳은 것은)/;
ok(Q.every(q=>STEM.test(String(q.q))),"발문 기출 상투구 (문형사전 §1-1)");
ok(Q.every(q=>!/(판례에 따르면|판시하였다|판결하였다)/.test(String(q.q)+q.opts.join(" "))),"판례 평서화 (지문 내 인용구 금지)");
ok(Q.every(q=>!/\d{2,4}[다도누허므마]\d{3,}/.test(String(q.q)+q.opts.join(" "))),"지문에 판결번호 없음");
ok(Q.filter(q=>q.type==="조합").every(q=>/<br>\s*(ㄱ|㉠|가|A)\./.test(q.q)),"조합형 지문 마커(<br>ㄱ. 형)");
const cnt=Q.filter(q=>q.type==="개수");
ok(cnt.length<=1,"개수형 1문 이하 (현재 "+cnt.length+")");
ok(cnt.every(q=>/몇 개인가/.test(q.q)),"개수형 발문");
Q.filter(q=>q.type==="판례").forEach((q,i)=>ok(/변수|가른|기준/.test(q.expl),"판례결론형 해설에 결론 가른 변수 명시 #"+(i+1)));
ok(Q.filter(q=>q.type==="조합").every(q=>q.q.includes("모두 고른")),"조합형 발문 형식");
const re=B.filter(q=>q.retryOf||String(q.q).includes("🔁")).length;
ok(re===0,"재도전 0 — 오답퀴즈 전담 (현재 "+re+")");
const lr=B.filter(q=>q.q.includes("⏪")).length;
if(stage==="S2"||stage==="S3") ok(lr===10,"장기복습 10 (현재 "+lr+")");
if(stage==="유지") ok(lr===B.length,"유지 모드: 전부 장기복습 (현재 "+lr+"/"+B.length+")");
ok(B.filter(q=>q.q.includes("⏪")).every(q=>/\(장기복습·\d+일전\)/.test(q.src)),"장기복습 표기 일관");
if(M.length){
  ok(M.every(q=>q.q.includes("🔄")&&/\(한달전 재출제·원본 \d{4}-\d{2}-\d{2}\)/.test(q.src)&&/^\d{4}-\d{2}-\d{2}$/.test(q.monthlyOf)),"한달전 표기 일관");
  const mc={}; M.forEach(q=>mc[q.cat]=(mc[q.cat]||0)+1);
  ok(Math.max(...Object.values(mc))<=3,"한달전 과목 골고루(≤3) "+JSON.stringify(mc));
}
const seq=B.map(q=>q.cat); const seen={}; let contig=true;
seq.forEach((c,i)=>{ if(!(c in seen)) seen[c]=1; else if(seq[i-1]!==c) contig=false; });
ok(contig,"같은 과목 연속 배치");
const mc5=B;
if(mc5.length>=10){
  const dist=[0,0,0,0,0]; mc5.forEach(q=>dist[q.answer]++);
  console.log("  객관식 정답 분포 0~4: "+dist.join("/"));
  ok(Math.max(...dist)-Math.min(...dist)<=3,"정답 분산(최대-최소 ≤3)");
  const neg=mc5.filter(q=>/않은|않는|아닌/.test(q.q)).length;
  ok(neg>=Math.floor(mc5.length*0.65)&&neg<=Math.ceil(mc5.length*0.9),"부정형 65~90% (현재 "+neg+"/"+mc5.length+")");
  const seqA=B.map(q=>q.answer);
  let run=1,mx=1; for(let i=1;i<seqA.length;i++){run=(seqA[i]===seqA[i-1])?run+1:1;mx=Math.max(mx,run);}
  ok(mx<=2,"같은 정답 3연속 없음 (최대 "+mx+")");
}
const cats={}; B.forEach(q=>cats[q.cat]=(cats[q.cat]||0)+1); console.log("  과목 분포: "+JSON.stringify(cats));
const norm=s=>String(s).replace(/<[^>]+>/g,"").replace(/[\s🔁🔄⏪]/g,"");
const past=new Set();
fs.readdirSync(".").filter(f=>f.endsWith(".html")&&!f.startsWith(today)&&!f.startsWith("_")).forEach(f=>{
  (fs.readFileSync(f,"utf8").match(/q: *"[^"]*"/g)||[]).forEach(m=>past.add(norm(m.replace(/^q: *"/,"").replace(/"$/,""))));
});
const dup=B.filter(q=>{const n=norm(q.q);return [...past].some(p=>p.slice(0,30)===n.slice(0,30));});
ok(dup.length===0,"과거 문제문과 도입부 중복 없음"+(dup.length?" — "+dup.map(d=>d.q.slice(0,20)).join(" | "):""));
console.log(fail.length? "\n❌ "+fail.length+"건 — 수정 후 재실행":"\n✅ 전체 통과");
process.exit(fail.length?1:0);
' "$(date +%F)"
```

- **푸시 페이로드** (검증 통과 후 `$QD/_push/{오늘날짜}.json` — 기록용, kakao 미연동): 법무사 스키마 그대로에 `"subject":"법무사1차"`. `counts.재도전` 항상 0, `ox` 3개(오늘 원천 노트 개념에서, 큰따옴표·백틱 금지), `alert` = 경보 문구 또는 null, `dueBacklog` = 듀 큐 길이(원장 없으면 null). 생성 후 python json 파싱 검증. 스킵 단계면 페이로드도 생성하지 않는다.
- **미제출 경보**: 직전 2개 실행일의 데일리 제출 신호(`법무사1차_오답_{날짜}.json` ± 사본, `-RQ` 제외) 확인. Downloads 미마운트면 확인 불가 처리(오탐 금지). 2회 연속 미제출 시 `{{ALERT_HTML}}` = `⚠ 최근 2회 미제출 · 인출 루프 단절 — 풀지 않은 문제지는 회독이 아니라 장식입니다. 오늘 것부터 제출.` (퀴즈가 생성된 실행일만 대상 — 스킵 날은 세지 않는다.)
- **11/1 평일 전환 체크리스트** (그 전 실행 금지): ① cron 화·목 → 평일 ② 과목 배분을 문항수 비례 가중으로 교체(위 표) ③ 신규 원천에 기출 원문(`법무사/기출원문/`) 보충 허용 ④ kakao_push 법무사판 연동 검토(중개사 4잡 disable과 함께) ⑤ 이 프로파일 v2 개정·기록.

## 오답퀴즈 (베이스_오답파이프라인용)

- **문항 형식**: 전 문항 5지선다(exam:"1차") — **기출 실전형 계약(위 데일리 문항 특칙 + `참조_법무사1차_기출문형_v1.md`)을 그대로 적용한다.** OX·계산형·단답형 금지, 변형 출제 각도는 함정 패턴 T1~T12 우선. 개정법 필터 동일 적용.
- **검증 스크립트 (대체 — v1.3)**: 이 트랙은 5지선다라 베이스 공통 스크립트의 객관식 계약(보기 4)과 충돌한다. **베이스 §3-4 공통 스크립트를 실행하지 않고 아래 완결 스크립트로 대체한다**(프로파일 우선 규칙). 통과까지 반복:

```bash
cd "$RQ" && node -e '
const fs=require("fs");
const today=process.argv[1], base=process.argv[2];
const led=JSON.parse(fs.readFileSync(base+"/claude_ox_오답/_ledger/오답_원장.json","utf8"));
const due=(led.dueQueue||[]), exp=Math.min(20,due.length), keys=new Set(due.map(d=>d.conceptKey));
const html=fs.readFileSync(today+".html","utf8");
const Q=eval(html.match(/const QUESTIONS = (\[[\s\S]*?\n\]);/)[1]);
let fail=[]; const ok=(c,m)=>{ if(!c) fail.push(m); console.log((c?"PASS":"FAIL")+" — "+m); };
ok(!html.includes("{{"),"토큰 잔존 없음");
ok(Q.length===exp,"문항 수 = min(20,듀) = "+exp+" (현재 "+Q.length+")");
ok(Q.every(q=>q.retryOf&&q.retryOf===q.conceptKey&&q.q.includes("🔁")&&/\(재도전\)/.test(q.src)),"전 문항 재도전 표기(retryOf·🔁·src)");
ok(Q.every(q=>keys.has(q.conceptKey)),"conceptKey 전부 듀큐에 존재");
ok(Q.every(q=>q.q&&q.expl&&q.src&&q.cat&&q.type),"필수 필드");
ok(Q.every(q=>q.exam==="1차"&&q.type!=="단답"&&q.type!=="OX"&&!q.calc),"전 문항 객관식·1차 (단답·OX·계산 금지)");
ok(Q.every(q=>Number.isInteger(q.answer)&&q.opts&&q.opts.length===5&&q.answer>=0&&q.answer<5),"객관식 계약(5지선다·정답 0~4)");
const STEM=/(가장 옳은 것은|가장 옳지 않은 것은|모두 고른 것은|몇 개인가|해당하지 않는 것은|아닌 것은|다른 하나는|고른 것은|연결된 것은|들어갈 내용으로 옳은 것은)/;
ok(Q.every(q=>STEM.test(String(q.q))),"발문 기출 상투구");
ok(Q.every(q=>!/(판례에 따르면|판시하였다|판결하였다)/.test(String(q.q)+q.opts.join(" "))),"판례 평서화 (인용구 금지)");
ok(Q.every(q=>!/\d{2,4}[다도누허므마]\d{3,}/.test(String(q.q)+q.opts.join(" "))),"지문에 판결번호 없음");
if(Q.length>=10){
  const dist=[0,0,0,0,0]; Q.forEach(q=>dist[q.answer]++);
  console.log("  정답 분포 0~4: "+dist.join("/"));
  ok(Math.max(...dist)-Math.min(...dist)<=3,"정답 분산(최대-최소 ≤3)");
}
const sq=Q.map(q=>q.answer); let run=1,mx=1;
for(let i=1;i<sq.length;i++){run=(sq[i]===sq[i-1])?run+1:1;mx=Math.max(mx,run);}
ok(mx<=2,"같은 정답 3연속 없음 (최대 "+mx+")");
console.log(fail.length?"\n❌ "+fail.length+"건 — 수정 후 재실행":"\n✅ 1차 대체검증 통과");
process.exit(fail.length?1:0);
' "$(date +%F)" "$ROOT/법무사/1차"
```

## 주간리포트 (베이스_주간리포트용)

- **대시보드**: 없음 — 원장 갱신까지만.
- 원장 미가동(1차 결과 0건) 동안은 exit 2 보고 후 종료가 정상 — 베이스 §2 규칙.
