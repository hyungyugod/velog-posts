> ⚠️ [구본 — 2026-08-25 이후] 정본은 velog-posts/_시험엔진/ 의 베이스+프로파일 체계로 이전됨. 이 파일은 이력 보존용 — 수정 금지, 스케줄러가 더 이상 읽지 않는다.

# 주간 오답 리포트 지침 v4 (2026-08-24 개정 — 오답 파이프라인 분리)

> 스케줄러 `weekly-wrong-answers-gongin`에 적용된 프롬프트의 사본. 수정하려면 이 파일을 고친 뒤 "이 파일로 스케줄러 지침 갱신해줘"라고 요청.
> **v4 변경점:** ① 평일 수거·원장 갱신·오답 재도전 퀴즈를 `daily-wrong-answers-pipeline`(평일 08:40, 정본: `ai-guidelines/50_스케줄-자동화/스케줄_프롬프트_오답파이프라인_v1.md`)으로 분리 — 월요일 잡은 주간 리포트 + 안전망 수거 전담 ② 법무사 결과 안전망 수거 추가(자격증별 분리 저장) ③ 기대치 검증에서 데일리 결과(`공인중개사_오답_{날짜}.json`)와 오답퀴즈 결과(`…-RQ.json`)를 구분 집계 ④ 데일리퀴즈 v5는 재도전(🔁) 미탑재 — 듀 큐 소화는 오답 재도전 퀴즈 전담.
> 구버전: `_지침아카이브/오답노트_지침_v2.md`, `_지침아카이브/오답노트_지침_v3.md`.

---

매주 월요일 실행되는 "주간 오답 리포트 + 원장 갱신" 작업이다.

**[역할 분담 — 2026-08-24 v3.2]** 평일 수거·원장 갱신·오답 재도전 퀴즈 생성은 `daily-wrong-answers-pipeline`(평일 08:40)이 담당한다. 이 월요일 잡은 ① 안전망 수거 ② 공인중개사 주간 리포트(오답노트) 생성을 전담한다.

**[실행 원칙]** 오답 데이터는 결과 JSON에 있는 것만 쓴다(지어내기 금지). 집계는 build_ledger.py가 한다. 접근이 안 되면 추측하지 말고 그대로 보고한다.

**[저장 위치 불변 규칙 — 최우선]** 주간 오답노트·원장·`_inbox`·`_raw`·`_processed.txt`의 정본은 오직 `<velog-posts>/공인중개사/claude_ox_오답/`. Downloads는 원천일 뿐 저장소가 아니다. velog-posts 접근 불가 시 노트를 만들지 말고 보고 후 종료.

## 요약 (전문은 스케줄러 SKILL.md = 정본)

| 단계 | 내용 |
|---|---|
| 0 | 경로 탐색 — 마운트 지연 대비 5회 재시도, `$BASE/{오늘}-claude_oxquize.md` 있으면 종료 |
| 1 | 안전망 수거 — `공인중개사_*오답_*.json`(RQ 포함) → `_inbox`. **법무사분(`법무사_*오답_*.json`)은 `법무사/claude_ox_오답/_inbox`로 분리 복사만** |
| 2 | `build_ledger.py --ingest` → 원장 v3.1(FSRS-6) 갱신 + `build_dashboard.py` 퀘스트 보드 갱신 |
| 3 | 기대치 검증 — 데일리 결과와 `-RQ`(오답퀴즈) 결과 **구분 대조**, 미도착 목록 채팅+노트 양쪽 기재 |
| 4 | 주간 오답노트 생성 — 원장 뷰 9개 섹션(오답 상세/졸업/상습/듀큐/패턴/암기카드/콘텐츠 소재/검증) |
| 5 | `_processed.txt` append + present_files 전달 |

세부 규칙(파일명 패턴 사고 이력, 폴백, 섹션별 구성)은 스케줄러 `weekly-wrong-answers-gongin`의 SKILL.md가 정본이다 — 이 파일과 어긋나면 정본이 우선.
