# 📌 1. 25년 2회 상설 유형(모의고사22)-엑셀
## 1-1. 조건은 [K2:L5] 영역 내에
> 위와 같이 지문이 나오면 그 영역을 다쓰지 않아도 그냥 그 영역안에만 작성하면된다.

## 1-2. 조건부 서식은 밖에서 적고 붙여넣어야한다.
> find의 경우 찾을문자, 문자를 찾을 문자열 이렇게 줘야하는데 바로 조건부서식에서 입력하면 실수하기 쉽다.
> 따라서 미리 밖에서 작성하고 ctrl+x하면 정리하기 쉽다.

## 1-3. find + isnumber
> =ISNUMBER(FIND("경기",$D3)) 처럼 find의 결과가 숫자로 나오면 포함한다는 의미이므로 이렇게 특정 글자가 포함되었는지를 논리식으로 나타낼 수 있다.

## 1-4. 인쇄 영역 추가
> 인쇄 영역을 추가하는 방법은 영역을 선택하고 인쇄영역-> 인쇄 영역 추가를 누르면 된다.

## 1-5. QUOTIENT와 MOD
> 몫을 구하는 함수 QUOTIENT와 나머지를 구하는 함수 MOD 모두 나눌 대상, 을 나눌 수 형식이다.

## 1-6. 날짜가 45612 이런식으로 숫자로 나올경우
> 위와 같은 경우 ctrl+1에서 형식을 날짜형식에서 적당한 형식으로 골라주면 된다.

## 1-7. count는 숫자만 세는 함수이다.
> =COUNT(IF((YEAR($E$29:$E$46)=I$28)*(LEFT($C$29:$C$46,1)=$H29),1)) 이건 되고 =COUNT(IF((YEAR($E$29:$E$46)=I$28)*(LEFT($C$29:$C$46,1)=$H29),$B$29:$B$46))이건 안되는 이유는 count는 숫자만 세는 함수인데 $B$29:$B$46가 숫자가 아니라 텍스트이기 때문에 개수가 0개가 나오는 것이다.

## 1-8. select문에서 or사용불가
```
Public Function fn비고(소속, 평가점수)

If 평가점수 >= 80 Then
    Select Case Left(소속, 2)
        Case "대기"
            fn비고 = "1차"
        Case "환경"
            fn비고 = "1차"
        Case "녹지"
            fn비고 = "1차"
        
        Case "자연"
            fn비고 = "2차"
        Case "도심"
            fn비고 = "2차"
        
        Case Else
            fn비고 = "3차"
    End Select
Else
    fn비고 = ""
End If

End Function
```
> select문에서 or이 사용불가하므로 그냥 위처럼 복사, 붙여넣기를 통해 식을 전부 따로 적어줘야 한다.

## 1-9. sumif에서 creteria로 H$2&"*"도 된다.
> creteria에서 부등호 대신 *같은 것도 표기 가능하다. 합쳐서 문자열로만 만들 수 있으면 된다. 

> 뭔가 문자열을 다뤄야하는데 left같은 함수가 없으면 별이나 & + 등을 떠올리자

## 1-10. lookup 배열상수 활용하기
> 원래 배열상수는 참조표가 있어야 한다. 즉 조건표와 결과표를 제공해주고 판단할 값이 조건표에 있으면 결과표의 값을 반환해주는 역할을 하는 것이 lookup이다. 만약 참조표가 없다면 직접 내부에 배열을 {"국외세액";"지방세액"} 처럼 입력해줘야한다.
> 이때 조건표 즉 분류표는 오름차순으로 입력되어야한다.
> 이때 두 배열상수를 ;로 구분하면 모두 ;를 구분, 그리고 ,로 구분했으면 ,로 같이 구분해줘야한다.

## 1-11. 서식을 변경할때 substitude를 이용하여 변경해줄 수도 있다.
> 2025.01.13 이런 자료에서 .을 ""로 바꾸면 20250113 이렇게 변경되는 것이다.

## 1-12. 유효성검사에서 목록을 넣을때 ,로 구분한다.
> "서울응급센터","경기응급센터","부산응급센터","대구응급센터" 이렇게 콤마로 구분하여 목록을 채워넣는다.

## 1-13. 하강선과 최고/최저값 연결선
> 차트요소추가 -> 선에 하강선이나 최고/최저값 연결선을 추가할 수 있다.

## 1-14. 항목을 거꾸로와 값을 거꾸로
> 항목을 거꾸로는 가로축 옵션에 있고 항목들의 순서를 거꾸로 한다. 값을 거꾸로는 세로축에 있고 세로축 값들의 순서를 거꾸로 한다.

## 1-15. 차트 이름지어주기
> 함수 옆 이름 설정영역에서 차트든 셀이든 범위든 이름을 정해주면 된다.

## 1-16. 특정 데이터만 둘재영역으로 넘기기
> 이어진 원형 차트에서 특정영역을 선택하고 서식을 열면 요소가 속한 그룹을 두번째 영역으로 선택할 수 있다.

## 1-17. currentragion할때 표 양쪽 어디에라도 글자가 있어서 연결되어있을 수 있으니 확인해야한다.
> currentragion할때 표 양쪽 어디에라도 글자가 있어서 연결되어있을 수 있으니 확인해야한다.
> 문자열로 날짜와 시간을 입력받았을때 Format(Txt일자, "yyyy-mm-dd")
Format(Txt시간, "h:nn") 이렇게 문자열 그대로 포멧에 넣고 출력해야한다.

## 1-18. vba에서 날짜를 비교하는 법
- 아래와 같이 vba에서 날짜를 비교해야 한다. CDate(Txt시간) 이 값과 어떻게 날짜를 비교해야지? 할때 #으로 감싸주면 자동으로 날짜 형식으로 변환되어서 비교해줄 수 있게 된다.
```
If CDate(Txt시간) > #12:00:00 PM# Then

    Cells(ir, 7) = "오후출발"
Else
    Cells(ir, 7) = "오전출발"
End If
```
