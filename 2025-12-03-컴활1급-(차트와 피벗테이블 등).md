# 📌 0. 엑셀 함수 총정리
## 0-1. 통계함수

| 함수                                  | 설명                       |
| ----------------------------------- | ------------------------ |
| **MAX(인수1, 인수2, …)**                | 인수 중 최대값(논리값/텍스트 제외)     |
| **MAXA(인수, …)**                     | 인수 중 최대값(논리값/텍스트 포함)     |
| **MIN(인수, …)**                      | 인수 중 최소값(논리값/텍스트 제외)     |
| **MINA(인수, …)**                     | 인수 중 최소값(논리값/텍스트 포함)     |
| **LARGE(범위, n번째)**                  | 범위의 n번째로 큰 값             |
| **SMALL(범위, n번째)**                  | 범위의 n번째로 작은 값            |
| **MEDIAN(인수, …)**                   | 중간값                      |
| **MODE.SNGL(인수…)**                  | 가장 자주 발생하는 값(최빈수)        |
| **VAR.S(인수…)**                      | 표본 분산                    |
| **STDEV.S(인수…)**                    | 표본 표준편차                  |
| **RANK.EQ(인수, 범위, 정렬 방식)**          | 순위 구함 (0=내림차순 / 1=오름차순)  |
| **AVERAGE(인수…)**                    | 평균                       |
| **AVERAGEA(인수…)**                   | 텍스트/논리값 포함 평균            |
| **AVERAGEIF(범위, 조건, 실제 계산 범위)**     | 조건 충족 값들의 평균             |
| **AVERAGEIFS(범위, 조건 범위1, 조건1, …)**  | 여러 조건 충족 값들의 평균          |
| **COUNT(범위)**                       | 숫자 개수                    |
| **COUNTA(범위)**                      | 비어있지 않은 셀 개수             |
| **COUNTIF(범위, 조건)**                 | 조건에 맞는 셀 개수              |
| **COUNTIFS(범위1, 조건1, 범위2, 조건2, …)** | 여러 조건 충족 개수              |
| **COUNTBLANK(범위)**                  | 빈 셀 개수                   |
| **FREQUENCY(배열1, 배열2)**             | 배열1 값의 구간별 빈도를 수직 배열로 반환 |
| **PERCENTILE.INC(범위, 인수)**          | 0~1 사이 백분위수 반환           |

## 0-2. 수학, 삼각함수

| 함수                                | 설명               |
| --------------------------------- | ---------------- |
| **SUM(인수…)**                      | 합계               |
| **SUMIF(조건 범위, 조건, 합계 범위)**       | 조건에 맞는 값의 합      |
| **SUMIFS(합계 범위, 조건 범위1, 조건1, …)** | 여러 조건 충족 값의 합    |
| **ROUND(인수, 자릿수)**                | 반올림              |
| **ROUNDUP(인수, 자릿수)**              | 올림               |
| **ROUNDDOWN(인수, 자릿수)**            | 내림               |
| **ABS(인수)**                       | 절대값              |
| **INT(인수)**                       | 정수 부분만           |
| **MOD(인수1, 인수2)**                 | 나머지              |
| **RAND()**                        | 0~1 난수           |
| **SQRT(인수)**                      | 제곱근              |
| **POWER(수1, 수2)**                 | 거듭제곱(수1^수2)      |
| **PI()**                          | π 반환             |
| **EXP(인수)**                       | e^인수             |
| **FACT(인수)**                      | 계승               |
| **TRUNC(인수, 소수점 이하 자릿수)**         | 소수점 이하 버림        |
| **PRODUCT(인수…)**                  | 곱                |
| **MDETERM(배열)**                   | 행렬식              |
| **MINVERSE(배열)**                  | 역행렬              |
| **MMULT(배열1, 배열2)**               | 행렬 곱             |
| **SUMPRODUCT(배열1, 배열2, …)**       | 배열끼리 대응요소 곱해 합산  |
| **SIGN(인수)**                      | 양수=1, 0=0, 음수=-1 |

## 0-3. 텍스트 함수

| 함수                                 | 설명                  |
| ---------------------------------- | ------------------- |
| **LEFT(텍스트, 개수)**                  | 왼쪽에서 n개 문자          |
| **RIGHT(텍스트, 개수)**                 | 오른쪽에서 n개 문자         |
| **MID(텍스트, 시작 위치, 개수)**            | 중간에서 n개 문자          |
| **LOWER(텍스트)**                     | 소문자로                |
| **UPPER(텍스트)**                     | 대문자로                |
| **PROPER(텍스트)**                    | 첫 글자만 대문자로          |
| **TRIM(텍스트)**                      | 불필요한 공백 제거          |
| **CONCAT(텍스트…)**                   | 텍스트 연결              |
| **REPLACE(텍스트, 시작 위치, 개수, 새 텍스트)** | 일부 치환               |
| **SUBSTITUTE(텍스트, 찾을 문자, 바꿀 문자)**  | 특정 텍스트 치환           |
| **EXACT(텍스트1, 텍스트2)**              | 두 텍스트가 완전 동일하면 TRUE |
| **FIND(찾을 문자, 텍스트, 시작 위치)**        | 특정 문자 위치            |
| **REPT(텍스트, 반복 횟수)**               | 텍스트 반복              |

## 0-4. 논리함수

| 함수                           | 설명              |
| ---------------------------- | --------------- |
| **AND(인수…)**                 | 모두 TRUE면 TRUE   |
| **OR(인수…)**                  | 하나라도 TRUE면 TRUE |
| **IF(조건, 값1, 값2)**           | 조건에 따라 값 선택     |
| **IFS(조건1, 값1, 조건2, 값2, …)** | 여러 조건 순차적 평가    |
| **SWITCH(값, 케이스1, 결과1, …)**  | 특정 값에 따른 결과 반환  |
| **IFERROR(수식, 오류일 때 반환값)**   | 오류 발생 시 대체값     |

## 0-5. 날짜, 시간함수

| 함수                         | 설명             |
| -------------------------- | -------------- |
| **DATE(년, 월, 일)**          | 날짜 값 생성        |
| **EDATE(시작 날짜, 개월 수)**     | 특정 개월 전/후 날짜   |
| **DAYS(종료, 시작)**           | 날짜 차이(일수)      |
| **TIME(시, 분, 초)**          | 시간 값 생성        |
| **HOUR(시간)**               | 시간에서 ‘시’ 추출    |
| **MINUTE(시간)**             | 분 추출           |
| **SECOND(시간)**             | 초 추출           |
| **NOW()**                  | 현재 날짜·시간       |
| **TODAY()**                | 오늘 날짜          |
| **YEAR(날짜)**               | 연도 추출          |
| **MONTH(날짜)**              | 월 추출           |
| **EOMONTH(날짜, 개월 수)**      | 월 마지막 날짜       |
| **DAY(날짜)**                | 일(day) 추출      |
| **WEEKDAY(날짜, 유형)**        | 요일 번호          |
| **NETWORKDAYS(시작, 끝, 휴일)** | 주말 제외 근무일수     |
| **WEEKNUM(날짜, 유형)**        | N번째 주          |
| **WORKDAY(날짜, 날짜 수, 휴일)**  | 근무일 기준 n일 뒤 날짜 |

## 0-6. 찾기, 참조함수

| 함수                              | 설명                |
| ------------------------------- | ----------------- |
| **XLOOKUP(찾을값, 찾을범위, 반환범위, …)** | 최신 버전의 통합 검색 함수   |
| **VLOOKUP(찾을값, 범위, 열번호, 옵션)**   | 세로 방향 검색          |
| **HLOOKUP(찾을값, 범위, 행번호, 옵션)**   | 가로 방향 검색          |
| **CHOOSE(번호, 값1, 값2, …)**       | n번째 값을 선택         |
| **INDEX(범위, 행 번호, 열 번호)**       | 특정 행/열의 값 반환      |
| **OFFSET(셀, 행 수, 열 수, 높이, 너비)** | 기준 셀에서 떨어진 범위를 참조 |
| **MATCH(값, 범위, 옵션)**            | 값의 위치(순번) 반환      |
| **XMATCH(값, 범위, 검색방식)**         | MATCH의 고급버전(정확도↑) |

## 0-7. 정보함수

| 함수              | 의미                       | 반환값                                        |
| --------------- | ------------------------ | ------------------------------------------ |
| CELL(정보유형, 셀주소) | 셀의 서식, 위치, 내용에 대한 정보를 조회 | 다양한 text 정보(row, format, contents, type 등) |
| TYPE(값)         | 값의 데이터 유형을 숫자로 반환        | 숫자 1, 텍스트 2, 논리 4, 오류 16                   |
| ISERR(값)        | **#N/A 제외** 오류면 TRUE     | TRUE/FALSE                                 |
| ISERROR(값)      | 모든 오류면 TRUE              | TRUE/FALSE                                 |
| ISEVEN(값)       | 짝수면 TRUE                 | TRUE/FALSE                                 |
| ISODD(값)        | 홀수면 TRUE                 | TRUE/FALSE                                 |
| ISTEXT(값)       | 텍스트면 TRUE                | TRUE/FALSE                                 |
| ISNONTEXT(값)    | 텍스트가 아니면 TRUE            | TRUE/FALSE                                 |
| ISNUMBER(값)     | 숫자면 TRUE                 | TRUE/FALSE                                 |
| ISLOGICAL(값)    | 논리값이면 TRUE               | TRUE/FALSE                                 |

## 0-8. 재무함수

| 함수                            | 인수                        | 의미                                                 |
| ----------------------------- | ------------------------- | -------------------------------------------------- |
| FV(이자율, 기간, 금액, 현재가치, 납입시점)   | rate, nper, pmt, pv, type | 일정 금액을 일정 이율로 적립할 때 **미래 가치(Future Value)** 계산     |
| PV(이자율, 기간, 금액, 미래가치, 납입시점)   | rate, nper, pmt, fv, type | 미래의 지불·수령 금액에 기반한 **현재 가치(Present Value)** 계산      |
| NPV(할인율, 금액1, 금액2 …)          | rate, value1…             | 할인율과 미래 현금흐름을 사용하여 **순현재가치(Net Present Value)** 계산 |
| PMT(이자율, 기간, 현재가치/미래가치, 납입시점) | rate, nper, pv/fv, type   | 일정 이율이 적용되는 대출의 **매월 상환액** 계산                      |

## 0-9. 데이터베이스 함수 (D- 함수)

| 함수                    | 인수                  | 의미                           |
| --------------------- | ------------------- | ---------------------------- |
| DMAX(범위, 열위치, 조건)     | DB, field, criteria | 조건에 맞는 값 중 **최대값**           |
| DMIN(범위, 열위치, 조건)     | DB, field, criteria | 조건에 맞는 값 중 **최소값**           |
| DSUM(범위, 열위치, 조건)     | DB, field, criteria | 조건에 맞는 값들의 **합계**            |
| DCOUNT(범위, 열위치, 조건)   | DB, field, criteria | 조건에 맞는 숫자 데이터 개수             |
| DCOUNTA(범위, 열위치, 조건)  | DB, field, criteria | 조건에 맞는 **비어 있지 않은** 데이터 개수   |
| DAVERAGE(범위, 열위치, 조건) | DB, field, criteria | 조건에 맞는 값들의 **평균**            |
| DVAR(범위, 열위치, 조건)     | DB, field, criteria | 조건에 맞는 값들의 **표본분산**          |
| DSTDEV(범위, 열위치, 조건)   | DB, field, criteria | 조건에 맞는 값들의 **표본표준편차**        |
| DGET(범위, 열위치, 조건)     | DB, field, criteria | 조건에 맞는 값이 **단 하나이면 그 값을 반환** |
| DPRODUCT(범위, 열위치, 조건) | DB, field, criteria | 조건에 맞는 값들의 **곱**             |

# 📌 1. 배열 수식과 논리 연산, 오류 처리

## 1-1. 배열 수식에서 AND/OR가 `*`, `+` 인 이유

엑셀 배열 수식에서 조건은 내부적으로 이렇게 처리된다.

- TRUE → $1$
- FALSE → $0$

그래서 논리 AND, OR가 자연스럽게 다음처럼 대응된다.

- AND:  
  - 진리표:  
    - $1 \land 1 = 1$, 나머지는 $0$  
  - 수학적으로는 곱셈과 동일  
    - $1 \times 1 = 1$  
    - $1 \times 0 = 0$, $0 \times 1 = 0$, $0 \times 0 = 0$  
  - 그래서 배열 수식에서 `AND`는 `*` 로 표현  
    - 예: `=SUM((A:A="아침")*(B:B>=120))`

- OR:  
  - 진리표:  
    - $1 \lor 0 = 1$, $0 \lor 1 = 1$, $1 \lor 1 = 1$, $0 \lor 0 = 0$  
  - 수학적으로는 “더한 뒤 0이 아니면 TRUE로 보는” 구조  
    - $1+0=1$, $0+1=1$, $1+1=2$ (2도 TRUE처럼 취급)  
  - 그래서 배열 수식에서 `OR`는 `+` 로 표현  
    - 예: `=SUM(((A:A="아침")+(A:A="점심"))*(C:C>100))`

NOT은 보통 $1-\text{조건}$ 으로 처리할 수 있다.

- 조건이 TRUE(1)일 때: $1-1=0$ (FALSE)
- 조건이 FALSE(0)일 때: $1-0=1$ (TRUE)

---

## 1-2. 배열 상수에서 오류 값이 있을 때의 동작

배열 상수 예:

- `{1,2,3}` → 정상  
- `{1,2,#N/A}` → 오류 포함

`=SUM({1,2,#N/A})` 처럼 사용하면:

- `SUM` 함수는 배열 안의 모든 원소를 숫자로 읽으려다가 `#N/A`를 만나면 전체 결과를 오류로 반환
- 즉, **배열 상수 안에 실제 오류 값이 들어 있으면, 그 배열을 읽는 함수 전체가 오류로 터진다**

오류를 무시하고 싶을 때 자주 쓰는 패턴:

- `IFERROR`  
- 조건 필터링 등

---

## 1-3. IF에서 `""`(빈 문자열)을 넣는 이유

예시 배열 수식:

```
{=COUNT(IF((vSystolic>=130)*(vDiastolic<=85), vHeartRate, ""))}
```

- 조건 TRUE → 숫자(심박수)
- 조건 FALSE → `""` → COUNT에서 무시됨

`""`는:

- 0처럼 숫자로 오인되지 않음  
- 오류처럼 전체 계산을 망치지도 않음  

그래서 “조건 불만족 위치 제거용 dummy 값”으로 최적이다.

---

# 📌 2. 차트 개념: 원형, 특수 차트, 데이터 계열, 범례

## 2-1. 원형 차트와 데이터 테이블 불가 이유

원형 차트 특징:

- 축이 없음  
- 단일 계열만 존재  
- 2D 구조를 갖지 않음  
- 비율만 표현

데이터 테이블은:

- X축·Y축 등 2차원 축 기반  
- 여러 계열 값을 표 구조로 표시하는 기능

원형 차트는 축이 없어서 **데이터 테이블 적용이 불가능**하다.

---

## 2-2. 특수 차트가 혼합형 불가인 이유

### 1) 거품형(Bubble)
- X, Y, Size 3축 구조  
- 일반 차트(값축 1개)와 데이터 구조가 다름

### 2) 주식형(Stock)
- OHLC(4열) 묶음이 1계열  
- 일반 계열과 호환 불가

### 3) 표면형(Surface)
- X축, Y축, Z축 3D 격자  
- 렌더링 엔진 자체가 다름

### 4) 3차원 차트
- 깊이축, 투영 등 독자적 렌더링  
- 2D 차트와 병합 불가

그래서 이들은 **혼합형 차트 대상에서 제외**된다.

---

## 2-3. 데이터 계열(Data Series)의 의미

- “한 열”이 아니라  
  **차트에서 하나의 시각적 흐름을 구성하는 값들의 묶음**
- 행 기준, 열 기준 모두 가능  
- 거품형은 3열이 1계열이 되기도 함  
- 원형 차트는 단일 계열만 가능

---

## 2-4. 범례(Legend)의 의미

범례는:

- **계열 이름을 표시하는 설명표**
- 세로축(Y축)과 무관  
- 단위나 값의 크기를 나타내지 않음  
- 단지 “이 색 = 이 계열”을 알려주는 역할

---

# 📌 3. 인쇄 / 페이지 나누기 / 틀 고정 vs 창 나누기

## 3-1. 자동/수동 페이지 나누기

| 종류 | 표시 | 생성 방식 |
|------|-------|--------------|
| 자동 | 점선 | 용지/여백/배율에 따라 엑셀 자동 생성 |
| 수동 | 실선 | 사용자가 직접 삽입 |

“수동=점선, 자동=실선”이라고 말하면 **완전히 틀림**.

---

## 3-2. 페이지 설정에서 인쇄 영역 변경이 불가한 이유

다음 상황에서 발생한다:

1. 여러 시트 동시 선택  
2. 표(Structured Table) 내부  
3. 머리글/바닥글 편집 중  
4. 페이지 미리보기에서 특정 제한 상태  

즉, “현재 UI 모드에서 인쇄 영역 조정이 논리적으로 충돌”하면 제한이 걸린다.

---

## 3-3. 페이지 나누기 삽입 기준: 왜 선택한 셀의 왼쪽·위쪽인가?

페이지는 “셀 중간”을 자를 수 없다.

따라서 새 페이지의 첫 셀 = **선택한 셀**이 된다.

그러면:

- 그 셀의 **왼쪽 열 경계**에 세로 나누기  
- 그 셀의 **위쪽 행 경계**에 가로 나누기  

를 넣어야 새 페이지가 정확히 시작된다.

---

## 3-4. 틀 고정과 창 나누기가 동시에 안 되는 이유

- 둘은 내부 엔진이 다르며 **상호 배타적 기능**  
- FreezePanes를 켜면 Split은 꺼짐  
- Split을 켜면 FreezePanes는 해제됨

화면이 4분할처럼 보일 수 있어 둘이 동시에 되는 것처럼 느껴지는 것뿐이다.

---

## 3-5. 창 나누기 최대 분할 개수

- 수평 1개  
- 수직 1개  
- 총 4개 화면까지 가능

그 이상 많은 화면은 “새 창 여러 개 띄우기(윈도우 분할)”이지 창 나누기가 아니다.

---

# 📌 4. 정렬 / 필터 / 부분합

## 4-1. 문자·숫자 우선순위는 정렬에서 직접 지정 불가

- 엑셀 기본 정렬은 자료형별 기본 규칙 사용  
- 사용자가 “문자 → 숫자 → 오류 → 빈칸” 같은 우선순위를  
  직접 지정할 수 없음  
- 해결: **도우미 열(헬퍼 컬럼) 사용**

---

## 4-2. 자동 필터 vs 고급 필터

| 기능 | 자동 필터 | 고급 필터 |
|------|-----------|-----------|
| 행 선택 | O | O |
| 특정 열만 출력 | X | O |
| 다른 위치에 복사 | X | O |
| 조건 범위를 따로 만들기 | X | O |

자동 필터는 항상 **전체 열을 포함한 행**을 보여주는 기능.  
고급 필터는 조건 필드·출력 필드를 분리하여 특정 열만 추출 가능.

---

## 4-3. 부분합은 왜 정렬 필수인가?

부분합 동작 원리:

1. 기준 열을 위→아래로 스캔  
2. 값이 바뀌면 **그 지점을 그룹 경계**로 판단  
3. 그룹마다 SUBTOTAL 삽입  

따라서 같은 값이 섞여 있으면 다른 그룹으로 인식된다.  
즉, 부분합은 “같은 값끼리 묶는 기능”이 아니라  
**“연속된 구간”을 묶는 기능**이다.

---

## 4-4. 여러 열의 부분합 계산

부분합 기능:

- 함수는 **하나만** 선택  
- 여러 열을 동시에 선택하면 모두 같은 함수가 적용됨  

열마다 다른 함수를 쓰고 싶다면:

- 부분합을 여러 번 실행하거나  
- 피벗테이블을 사용해야 한다.

---

# 📌 5. 파워 쿼리 / 데이터표 / 엑셀 엔진 구조

## 5-1. 통합 문서에서 “새 쿼리”의 의미

- 통합문서 파일 **한 개**를 쿼리 원본으로 선택  
- 내부적으로 M 언어 스크립트를 생성  
- 원본 파일이 변해도 “새로 고침”으로 동일 변환을 재실행

---

## 5-2. 왜 엑셀을 가져오면 표가 생기는데 그게 쿼리인가?

겉보기: 그냥 표  
실제로:  

```
File.Contents()
Excel.Workbook()
Table.TransformColumnTypes()
...
```

이런 일련의 **변환 단계(step) 스크립트**가 저장된 것이다.

즉:

- “표가 생긴다” = “쿼리 결과를 로드했다”는 의미

---

## 5-3. 데이터표(What-If Data Table)

- 하나 또는 둘의 입력값을 바꿔가며  
  같은 수식 계산을 반복시키는 시뮬레이터

### 단일 입력표
- 한 변수만 반복 변경  
- 행이나 열 중 한 방향 사용

### 이중 입력표
- 변수 2개 조합  
- 행과 열 각각에 입력 값 목록  
- 좌상단에 결과 셀 참조

---

## 5-4. 엑셀 전체가 쿼리 기반은 아님

엑셀 엔진은 4개 구조가 각각 존재한다:

1. **수식 계산 엔진**  
2. **파워 쿼리 엔진**  
3. **피벗 엔진**  
4. **UI 엔진(정렬, 필터 등)**  

엑셀 전체가 SQL식 쿼리로 구성된 건 아니다.

---

# 📌 6. 바로가기 메뉴

바로가기 메뉴 = **우클릭 컨텍스트 메뉴**

- 클릭한 대상에 따라 다르게 구성  
- 셀/시트/차트/피벗 등 상황에 맞는 기능 제공

---

# 📌 7. 피벗테이블, 그룹, 목표값 찾기, 시나리오

## 7-1. 피벗테이블 삭제해도 피벗차트가 남는 이유

둘은 서로 종속된 객체가 아니라:

- 원본 데이터 → PivotCache  
- 피벗테이블 = PivotCache 뷰  
- 피벗차트 = PivotCache 뷰  

즉, 피벗차트는 피벗테이블
