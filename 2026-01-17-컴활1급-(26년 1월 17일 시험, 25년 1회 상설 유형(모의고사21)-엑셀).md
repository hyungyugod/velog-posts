# 📌 1. 26년 1월 17일 시험
## 1-1. 전체적인 포인트
> 1. 점수배점 확인하고 막히면 붙잡고있지 말기
> 2. 엑셀 1-3-4-2로 풀고 vba 마지막 문제풀기전에 계산문제부터 풀기
> 3. 엑세스는 순서대로 풀기
> 4. 시험장에서 함수 자동완성되는지 확인하기
> 5. 피벗테이블 시트 새로생김문제가 있을 수 있는데 피벗테이블 만들때 새로운 시트에 만들지 않도록 유심히 봐야함.->기존 워크시트에 생성하기

## 1-2. 차트제목에 연동
> 차트제목객체를 클릭해서 제목객체가 실선인 상태로 만들고 수식 입력칸에 =을 입력하므로서 차트의 제목과 연결할 셀을 클릭해야한다.
> 만약 점선이라면 텍스트 편집상태이므로 esc를 눌러서 빠져나와야한다.

## 1-3. 차트 축 단위 변경
> 표시단위를 변경하는 옵션이 아래 표시단위라는 레이블과 목록값으로 나와있다 거기서 선택을 하면 된다.

## 1-4. 엑세스 조건에서 like 활용할때
> Like "\*" & [조회할 제품명의 일부를 입력하시오] & "\*" 이렇게만 해주면 된다. 이유는 최종입력이 like \*검색어\* 로만 나타나면 되기 때문이다.

## 1-5. Group by와 조건 엮일때 쿼리에 포함되지 않음
> GROUP BY 절에 포함되지 않는 column (집계 함수 제외) 을 SELECT 절에 포함 시킬 경우, 컬럼을 어떤 그룹에 포함시켜야 할 지 모호한 case 가 발생한다.

> 위의 문제는 집계함수를 써야할때 쓰지 못했거나 묶는 방법으로 설정해야할 것을 묶는 방법으로 설정하지 못했을때 발생하는 문제이다.

# 📌 2. 25년 1회 상설 유형(모의고사21)-엑셀
## 1-1. 행을 한페이지에 전부 넣기 위해서는 용지 높이를 1로 설정한다.
> 행을 한페이지에 넣는다는 말은 엑셀의 행 번호들이 한페이지에 들어가는 느낌으로 생각하면 된다.

## 1-2. 텍스트 상자의 잠금과 텍스트 잠금기능
> 도형서식/옵션 -> 크기 및 속성에서 잠금과 텍스트 잠금을 적용해주는 것이다. 이후 시트보호를 사용해서 잠금을 적용한다.

## 1-3. time과 value활용
> 우선 12시 이전이면 12시를 포함하는 것을 의미한다. 12시까지 와라 랑 같은 의미이다.

> value는 보통 left, right, mid등의 결과가 문자열이기 때문에 이를 숫자로 만들어서 time의 인수로 넣거나 조건식을 만들때 사용한다. 이 문제에서는 time의 인수로 넣을때 숫자를 넣어야하므로 value를 이용해주었다.

## 1-4. 일을 년단위로 끊기
> days를 통해 일수를 구했다면 이를 연으로 만들어주기 위해서는 365로 나눠줘야 한다. 또한 남은 일수를 즉 나머지를 30일로 나누면 개월수를 구할 수 있다.

## 1-5. 프로시저에서 그외는 공란으로 표시할때 그냥 ""를 주기
> 공란을 표시할때 그냥 ""이렇게 빈값을 주는게 마음 편하기는 하다.

## 1-6. dget과 dmax
> dmax로 특정 필드에서 where절을 걸고 그들 중 가장 최대값을 구할 수 있다. 이때 where절 즉 creteria는 엑셀에서 범위로 지정해준다. dmax(데이터베이스,대상필드,조건범위)이다. 이때 조건범위는 고급필터에서처럼 활용할 수 있다. 대상 필드는 데이터 베이스의 필드명 셀을 선택한다.

> dget은 특정데이터베이스, 특정 열에서 조건을 만족하는 필드의 값을 가져오는 역할을 한다.

## 1-7. 데이터표
> 데이터표에서 열입력셀 = 열방향입력셀이다. 즉 열방향으로 입력되는 즉 세로로 있는 데이터가 들어갈 곳은 열 입력셀에 참조해줘야 한다.

## 1-8. 설명메세지에서
> 설명메세지에서 줄바꿈이 그냥 생기는데 이는 탭으로 구분하면 안되고 그냥 띄어쓰기를 해야한다. 띄어쓰기를 안하면 그냥 쭉 이어서 나온다. 이를 잘 구분해야한다.

## 1-9. 차트데이터에서 선택된 영역외의 필드를 기준으로 데이터들을 선택하는 법
> 보통 그러면 일대일인 포함된 필드가 있을 것이다. 데이터 선택에서 행열전환 이후에 그 필드들을 손수 삭제해주면 된다.

## 1-10. 기본보조 눈끔선
> 차트요소 추가 - 눈금선에서 기본 주 가로나 기본 보조 가로 중 하나를 선택할 수 있다.

> 주 눈금선이 더 러프하고 진하다 보조 눈금선은 촘촘하고 연하다.

## 1-11. 사용자 지정 서식에서 문자와 글자가 떨어져있는 경우
> 이는 문자와 글자 사이에 스페이스가 반복되어있는 형태이므로 ★ *0.0 이런식으로 작성해주면 된다.

## 1-12. 텍스트 상자들을 비활성화 시키기
> 텍스트 상자명.enable = false로 비활성화 시킬 수 있다.

## 1-13. for each in range
```
isfind = 0
for each 반복변수 in range("b6:b13")
    if 반복변수 = txt지역명 then
        txt구역 = format(cells(반복변수.row,3),">")
    end if

    isfind = 1
next 반복변수

if isfind = 0 then
    msgbox ~
```
- foreach문의 사용과 셀.row로 셀의 행주소를 가져오는 방법이 있고 플래그를 활용해서 해당 값을 찾았는지 검사하는 방법이 있다.
- 또한 format에서는 형식에서 사용하던 >를 유일하게 사용할 수 있는데 이는 해당 문자를 대문자로 만든다.

| 분류       | 기호    | 필수 / 선택 | 허용되는 입력            | 의미              |
| -------- | ----- | ------- | ------------------ | --------------- |
| 숫자 전용    | **0** | 필수      | 숫자만 (0~9)          | 반드시 숫자 하나 입력    |
| 숫자 전용    | **9** | 선택      | 숫자 또는 공백           | 숫자 입력 가능, 없어도 됨 |
| 문자 전용    | **L** | 필수      | 영문자, 한글            | 반드시 문자 1개       |
| 문자 전용    | **?** | 선택      | 영문자, 한글            | 문자 입력 가능, 없어도 됨 |
| 문자+숫자    | **A** | 필수      | 영문, 한글, 숫자         | 반드시 1글자         |
| 문자+숫자    | **a** | 선택      | 영문, 한글, 숫자         | 선택 1글자          |
| 문자+숫자+공백 | **&** | 필수      | 모든 문자 + 공백         | 반드시 1글자         |
| 문자+숫자+공백 | **C** | 선택      | 모든 문자 + 공백         | 선택 1글자          |
| 대소문자 변환  | **>** | 제어      | 이후 입력을 **대문자**로 강제 |                 |
| 대소문자 변환  | **<** | 제어      | 이후 입력을 **소문자**로 강제 |                 |
| 선택 요소    | **#** | 선택      | 숫자, 공백, +, -       | 전화번호·부호용        |