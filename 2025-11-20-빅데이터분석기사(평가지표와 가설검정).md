# 📌 1. 평가지표 관련 정리
## 1-1. 이진분류
- accuracy_score는 기존에 하던대로 하면 되기 때문에 어렵지 않다.
- f1_score의 경우 보통 0이 음성이고 1이 양성이지만 classes_가 라벨로 주어질 경우 f_score에서 뭐가 양성인지 찍어줄 필요가 있다.
- pos_label은 “positive label”의 축약으로 말 그대로 '이걸 긍정(Positive) 클래스로 취급해라' 하고 평가 지표에게 알려주는 역할이다.
- 참고로 f1_score는 정밀도와 재현율의 조화평균으로 둘 중 하나라도 값이 낮으면 점수가 떨어진다. 즉 찾아야 할 것을 얼마나 잘 찾았는가를 토대로 머신을 평가한다.
```py
# F1 스코어(F1 Score) ***
from sklearn.metrics import f1_score
f1 = f1_score(y_true, y_pred)
print("F1 스코어:", f1)

f1 = f1_score(y_true_str, y_pred_str, pos_label='A')
print("F1 스코어:", f1)
```
- 또 이진분류에서 이전에 하던 것처럼 roc_auc_score를 활용해서 이진분류를 수행할 수 있다.

## 1-2. 다중분류
- 다중분류도 정확도를 사용할 수 있지만 f1_score의 쓰임이 중요하다.
```py
# F1 스코어(F1 Score) ***
from sklearn.metrics import f1_score
f1 = f1_score(y_true, y_pred, average='macro')  # average= micro, macro, weighted
print("F1 스코어:", f1)

f1 = f1_score(y_true_str, y_pred_str, average='macro')
print("F1 스코어:", f1)
```

| 항목                     | 의미                    | 작동 방식 / 특징                                          | 주의할 점                           |
| ---------------------- | --------------------- | --------------------------------------------------- | ------------------------------- |
| **y_true**             | 정답 라벨                 | 실제 클래스값(숫자/문자열 모두 가능)                               | shape이 y_pred와 같아야 한다           |
| **y_pred**             | 모델 예측 라벨              | 모델이 예측한 클래스값                                        | y_true와 비교하여 TP/FP/FN 계산에 사용    |
| **average='micro'**    | 마이크로 평균               | 모든 클래스의 TP/FP/FN을 **합쳐서** 단 한 번의 F1 계산              | 클래스 비율이 큰 클래스가 점수에 큰 영향         |
| **average='macro'**    | 매크로 평균                | 각 클래스별 F1을 계산 후 **동일 비중으로 평균**                      | 클래스 불균형 여부 무시, 드문 클래스도 동일 중요도   |
| **average='weighted'** | 가중 평균                 | 클래스별 F1 × 클래스 샘플 수로 가중 평균                           | 균형 잡힌 성능을 반영하지만 드문 클래스 영향은 작아짐  |
| **pos_label**          | positive label 기준     | 이 라벨을 positive로 간주해 Precision/Recall/F1 계산          | binary + 문자열 라벨일 때 필수적          |
| **라벨 타입(숫자)**          | 숫자라벨(0,1,… )          | sklearn이 자동으로 “1을 positive”로 인식                     | binary일 때 특별 설정 필요 없음           |
| **라벨 타입(문자열)**         | 문자열라벨(‘A’, ‘spam’, …) | macro/micro는 자동 처리되지만 **binary에서는 pos_label 지정 필요** | pos_label 누락 시 ValueError 발생 가능 |

| average 옵션   | 계산 방식                   | 어떤 상황에 적합한가                   | 단점                       |
| ------------ | ----------------------- | ----------------------------- | ------------------------ |
| **micro**    | 전체 TP/FP/FN을 합산해 한 번 계산 | 데이터 불균형이 심하고 전체 정확을 보고 싶을 때   | 특정 클래스가 지배함              |
| **macro**    | 클래스별 F1을 계산 후 단순 평균     | 모든 클래스를 “동등하게” 중요하게 다룰 때      | 빈도 낮은 클래스의 영향이 과도해질 수 있음 |
| **weighted** | 클래스 비율로 가중 평균           | 불균형 데이터를 좀 더 현실성 있게 반영하고 싶을 때 | 드문 클래스가 macro보다 덜 반영됨    |

- ROC AUC는 원래 이진(binary) 문제에서만 바로 계산할 수 있다.
- 그런데 멀티클래스(예: A/B/C) 상황에서는 “한 번에 ROC AUC를 계산할 수 없다”.
- 그래서 멀티클래스를 여러 개의 binary 문제로 ‘변환’해서 계산한다.
- 그 변환 방식이 바로 ovr와 ovo다.
- OVR (One-vs-Rest) 방식 — “한 놈 vs 나머지”
- 클래스 A vs (B,C)
- 클래스 B vs (A,C)
- 클래스 C vs (A,B)
- OVO (One-vs-One) 방식 - 모든 클래스 쌍을 만들어 ROC AUC를 계산한다:
- A vs B
- A vs C
- B vs C
- 또한 비교할 칼럼의 칼럼 순서가 같아야 같이 roc에 넣어서 확인할 수 있다.
- 이진 문제로 쪼개려면 정답(y_true)을 “원-핫 인코딩”해야 하며 즉 클래스별 칼럼으로 나누어야한다는 뜻이다. 
- 이때 y_true가 보통 dataframe이므로 0번칼럼을 선택하여 대입한다. (슬라이스만 행별이고 나머지는 다 열)
```py
from sklearn.metrics import roc_auc_score

# 수치형일 때는 자동으로 원핫인코딩에서 제외함. 컬럼 지정 필요
y_true_onehot = pd.get_dummies(y_true[0])

# 인코딩된 순서와 확률 컬럼 순서가 같은지 확인
print("y_true의 원-핫 인코딩된 컬럼 순서:", y_true_onehot.columns)
print("y_pred_proba의 컬럼 순서:", y_pred_proba.columns)

# 'ovo' 방식
roc_score_ovo = roc_auc_score(y_true_onehot, y_pred_proba, multi_class='ovo')
print("ROC AUC (OvO):", roc_score_ovo)

# 'ovr' 방식
roc_score_ovr = roc_auc_score(y_true_onehot, y_pred_proba, multi_class='ovr')
print("ROC AUC (OvR):", roc_score_ovr)
```
- 회귀에 대한 평가지표는 앞서 내가 정리했던 정리본들에 모두 정리되어있다.

# 📌 2. 가설검정 (t-test) 개요
## 2-1. t-검정 종류

| 검정 종류 | 모집단 구성 | 예시 |
|-----------|-------------|-------|
| 단일표본 t-검정 | 모집단 1개 | 과자의 무게가 200g과 다른지 |
| 대응표본 t-검정 | 모집단 2개(같은 집단) | 약물 효과 전후 비교 |
| 독립표본 t-검정 | 모집단 2개(다른 집단) | 두 반의 성적 차이 비교 |

## 2-2. 단일 표본 t검정 
1. 정규성 검정: `shapiro(data)`
2. 정규성 만족 → `ttest_1samp(data, 기대값)`
3. 정규성 불만족 → `wilcoxon(data - 기대값)`

## 2-3. 수식

평균 $\bar{x}$  
표본크기 $n$  
표준편차 $s$  
기대값 $\mu_0$

$$
t = \frac{\bar{x} - \mu_0}{s / \sqrt{n}}
$$

## 2-4. 대응표본 t검정 절차
1. $d = data1 - data2$
2. 정규성 검정: `shapiro(d)`
3. 정규성 만족 → `ttest_rel(data1, data2)`
4. 정규성 불만족 → `wilcoxon(data1, data2)`

## 2-5. 수식

$d_i = x_{1i} - x_{2i}$  
평균 $\bar{d}$  
표준편차 $s_d$

$$
t = \frac{\bar{d}}{s_d / \sqrt{n}}
$$

## 2-6. 독립표본 t검정 절차
1. 정규성 검정  
2. 등분산 검정: `levene(data1, data2)`
3. 등분산 만족 → 독립표본 t-검정  
4. 등분산 불만족 → Welch t-검정  
5. 정규성 불만족 → Mann–Whitney U 검정

## 2-7. 등분산 t-검정(통합분산) 수식

$$
s_p^2 = \frac{(n_1-1)s_1^2 + (n_2-1)s_2^2}{n_1 + n_2 - 2}
$$

$$
t = \frac{\bar{x}_1 - \bar{x}_2}{s_p \sqrt{\frac{1}{n_1} + \frac{1}{n_2}}}
$$

## 2-8. Welch t-검정 수식

$$
t = \frac{\bar{x}_1 - \bar{x}_2}{\sqrt{\frac{s_1^2}{n_1} + \frac{s_2^2}{n_2}}}
$$

자유도(df):

$$
df =
\frac{
\left(\frac{s_1^2}{n_1} + \frac{s_2^2}{n_2}\right)^2
}{
\frac{(s_1^2/n_1)^2}{n_1 - 1} +
\frac{(s_2^2/n_2)^2}{n_2 - 1}
}
$$

## 2-9. Mann–Whitney U 검정

$$
U_1 = n_1 n_2 + \frac{n_1(n_1+1)}{2} - R_1
$$

## 2-10. 왜 t분포를 사용하는가
정규분포의 모분산을 모르는 경우, 표본으로 추정된 $s$ 를 사용하면  
평균의 분포는 더 퍼진 형태가 된다. 이 분포가 **t-분포**이다.

정리하면:

- 모분산 $\sigma^2$ 을 모른다  
- 대신 표본 분산 $s^2$ 로 추정한다  
- 표본 크기가 작다  
→ 평균 추론은 정규분포 대신 **t-분포**를 따른다.

또한 표본 크기 $n$ 이 커질수록 t-분포는 정규분포에 수렴한다.

# 📌 3. 단일표본 검정
## 3-1. 일반 단일표본 검정
- 대립가설: 합격 원두(dark)" 상품의 평균 무게는 120g 아니다.
- scipy에 stats에 있다. (해당 표본집단이 평균이 120인 표본분포에 비해 얼마나 차이가 나는가) 
- alternative='two-sided'로 양측검정이 기본값이다.
- alternative는 대립가설을 무엇으로 둘 것인지에 대한 이야기이다.

| alternative 값 | 의미        | 대립가설(H₁) |
| ------------- | --------- | -------- |
| `"two-sided"` | 양측검정      | μ ≠ μ₀   |
| `"greater"`   | 단측검정(오른쪽) | μ > μ₀   |
| `"less"`      | 단측검정(왼쪽)  | μ < μ₀   |


```py
from scipy import stats
print(stats.ttest_1samp(df['무게'], 120))
```
- 대립가설: 합격 원두(dark)" 상품의 평균 무게는 120g 보다 크다
- 이때 alternative='greater' 이다.
```py
print(stats.ttest_1samp(df['무게'], 120, alternative='greater'))
```
- 대립가설: 합격 원두(dark)" 상품의 평균 무게는 120g 보다 작다
```py
from scipy import stats
print(stats.ttest_1samp(df['무게'], 120, alternative='less'))
```

## 3-2. 정규성 조건을 만족하는지 우선 테스트해봐야할 경우
- 문제에서 데이터가 정규분포를 따른다는 가정이 없다면 우선 이를 테스트해보고 아니면 비모수 검정을 해야한다.
- 보통 이는 shapiro-wilk 검정을 사용한다. 
- 귀무가설 (H0): 주어진 데이터 샘플은 정규 분포를 따른다.
- 대립가설(H1): 주어진 데이터 샘플은 정규 분포를 따르지 않는다.
- Shapiro–Wilk은 데이터가 정규분포라면 정렬된 값들과 이론적 정규분포의 expected order statistics(기댓값) 사이에 특정한 선형 관계가 성립한다는 사실을 이용한다.
```py
# Shapiro-Wilk(샤피로-윌크) 정규성 검정
from scipy import stats 
print(stats.shapiro(df['무게']))
# pvalue=np.float64(0.9020825084089065) 이므로 정규분포를 따르지 않는다.
```
- 만약 정규성 가정을 충족하지 못했다면 윌콕슨의 부호 순위 검정을 사용한다.
- Wilcoxon 검정은 정규성을 만족하지 않는 상황에서 평균 차이를 비교할 때 사용하는 비모수(non-parametric) 통계 검정으로 귀무가설(H₀): 두 모집단의 중앙값(median)에 차이가 없다. (혹은 대응 전후의 차이가 0이다.) 대립가설(H₁): 중앙값이 차이가 있다 / 크다 / 작다 (단측·양측 모두 가능)를 비교한다.
- 즉, 전후 변화가 유의미한지를 정규성 없이 확인하는 검정이다.
- 매개변수는 ttest_1samp과 동일하다.
```py
from scipy import stats
stats.wilcoxon(df['무게'] - 120, alternative='greater')
# pvalue=np.float64(0.023729842171861575)이므로 0.05 유의수준에서 유의하다.
```

# 📌 4. 대응표본 검정
## 4-1. 기본 경우
- df['before'], df['after']는 df['before'] - df['after']를 의미하고 문제에서 이 값이 음수여야 이후효과가 큰 것이므로 이 차이의 평균이 less인게 대립가설이 된다.
- 빼는 위치를 바뀌고 대립가설 정의 부호도 바꾸면 같은 결과를 낸다.
```py
# 대응표본검정
from scipy import stats
stats.ttest_rel(df['before'], df['after'], alternative='less')
```

## 4-2. 정규성 검정
- 여기서는 두 값의 차이가 정규분포를 따르는지 보는 것이므로 차이에 대한 컬럼을 만든후 이를 기반으로 shapiro 검정을 돌린다.
```py
# Shapiro-Wilk(샤피로-윌크) 정규성 검정
from scipy import stats
df['diff'] = df['before'] - df['after']
stats.shapiro(df['diff'])
```

## 4-3. 정규성을 따르지 않을 경우 대응표본 윌콕슨 부호 순위 검정 수행
```py
# Wilcoxon(윌콕슨)의 부호 순위 검정 수행
from scipy import stats
stats.wilcoxon(df['diff'], alternative='less')
```

# 📌 5. 독립표본 검정
## 5-1. 기본 검정
- 이전까지 단일 집단에 대해 다뤘다면 이제는 서로 다른 두 집단에 대해 다룬다.
- SciPy의 t-test는 “숫자 배열(1차원)”만 받는다 즉, 리스트든 NumPy 배열이든 Pandas Series든 “숫자 벡터” 형태로만 받는다. 즉 series를 넣던 리스트를 넣던 차이가 없다.
- 기본값은 양측검정이지만 만약 단측검정이라면 alternative를 less나 greater로 적절히 적용하면 된다.
```py
# 독립표본검정
from scipy import stats
stats.ttest_ind(A, B)
```

## 5-2. 등분산 가정이 주어지지 않을경우 (다른 두집단이니까 체크 필요)
- equal_var=False로 설정한다. 기본값은 equal_value=True 이다.
- 
```py
# 두 집단의 분산이 다르다(equal_var=False)
from scipy import stats
stats.ttest_ind(A, B, equal_var=False)
```

## 5-3. 정규성 검정
- 똑같이 shapiro를 활용한다.
- 집단이 두개이므로 각각 하나씩 해야한다.
```py
from scipy import stats
stats.shapiro(A)
stats.shapiro(B)
```

## 5-4. 등분산 검정
- levene 테스트를 통해 두 집단이 등분산성을 만족하는지 판단한다.
```py
from scipy import stats
stats.levene(A,B)
```

## 5-5. 등분산성을 만족하지 않을 경우 -> 만휘트니 유 검정
- mann whitney u 이다.
- 매개변수는 똑같다. 비모수검정은 평균이 아닌 중앙값으로 판단한다.
```py
from scipy import stats
stats.mannwhitneyu(A, B, alternative='less')
```