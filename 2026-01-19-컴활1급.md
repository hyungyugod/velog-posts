# 📌 0. 2026-01-19 컴활시험
## 0-1. substitude를 활용해 A를 공백으로 대체하라하면 “ “인가 “”인가
> 문제에서 공백은 웬만하면 ""이다. 시험때 헷갈리는 경우는 그냥 ""하는게 좋은 것 같다. 

## 0-2. 유효성검사에서 사용자 저정조건에서 어떤 열의 값보다 크다를 어떻게 표현
> 맨 위 한셀끼리 비교 (조건부서식처럼) , 부등호실수주의 그러니까 = e3 > f3 이렇게 지정해도 표시험 범위는 한번 내려가면서 만들때 쳐둔 범위 내 모든 셀에 상대적으로 첫셀의 유효성검사를 다 지정해준다.

## 0-3. Rank.eq와 max 활용한 배열수식 문제에서 rank.eq배열수식 지정법
> if와 max를 이용해서 조건에 맞는 가장 큰값을 구해두고 그 값이 전체에서 순위가 어떻게 되는지의 순서로 구해야한다. 즉 if와 max만으로 우선 최대값들을 뽑고 다시 rank로 그 최대값들이 원본에서 몇번째에 위치하는지 구하는 것이다.

## 0-4. 조건부 서식 지정 유의사항
> [>0][빨강]”기호”0.0; [<0][파랑]”기호”0.0;0.0;”이상없음”; 

> -> 양수, 음수는 그냥 [>0] 이거 안쓰고 작성하기

>  ;은 조건의 시작을 의미하고 문자 마지막에 ;이 하나 있어서 오류가 발생한다.

>  엑셀 서식코드와 문자열을 구분하기 위해 문자열, 기호 자체에는 큰 따옴표를 써주는게 좋음

## 0-5. 쿼리만들때 어떤 필드는 iif와 in을 사용해서 "A"거나 "B"거나 null이 아닐경우만 count하고 어떤 필드는 전체를 다 카운트해서 각 열에 표시해야할경우 
> Count(IIf([필드명] In ('A','B') And Not [필드명] Is Null, 1, Null)) 이렇게 iif가 임의적으로 해당 필드를 1혹은 null로 정의하고 sum은 이 필터를 거친 필드값들을 합선하게 된다.
> 시험때는 이 정보가 각행에 나왔어야 하므로 group by를 했어야했다.


## 0-6. 전체적인 교훈
> 계산문제 막히면 계산문제 마지막 프로시저 풀기
> 엑세스 4번 첫번째문제가 좀 복잡함 넘어가는 것도 좋은 판단임


# 📌 1. 24년 3회 상설 유형(모의고사19)-엑셀
## 1-1. 고급필터 문장 주의
> 이거나로 끊기면 그때부터 양쪽은 다른 조건인 것이다. 1개 이거나 2개이면서 3개이면 1 or (2 and 3) 이다.

> 문제를 완전히 이해하고 함수를 빼먹으면 안되고 모든 함수를 썼는지 확인해야한다.

## 1-2. 행은 1페이지 내에 인쇄되어야한다는 말 잘 보기
> 행은 1페이지에 인쇄되어야한다고만 나오면 너비는 비워두고 높이면 1로 주면 된다.

## 1-3. 압축형식이 기본값이다, 열합계 비율, 회계형식
> 피벗테이블에서 압축형식이 기본값이다.

> 열합계 비율은 행방향 합계와 같은 말이며 계산필드를 넣고 값 필드 설정 에서 지정할 수 있다.

> 만약 피벗테이블에서 0이 -로 나와있다면 회계형식임을 알아야한다.

## 1-4. 2월에 해당하는 요약값 표시에 사용된 데이터를 출력하기
> 해당 행의 요약값 즉 전체 합계나 전체 비율 같은 요약값들 중 하나를 더블클릭하면 된다.

## 1-5. 데이터 유효성 검사 사용자 지정수식
> 데이터 유효성 검사에서 사용자 지정 서식도 절대화를 신경써야 한다.

## 1-6. 농경지 관련 항목과 비농경지에 대한 면적 합으로 요약
> 데이터 통합에서 농경지(A)와 농경지(B)를 하나로 묶어써야하는 상황이었는데 이는 조건식을 쓸 수 있으므로 농경지\* 이렇게 쓰면 된다. 또한 데이터 통합을 사용하기위해 필드명도 꼭 통합하는 테이블에 써주어야 한다.

> 결과표가 토지에서부터 시작하면 원본표도 토지에서부터 시작하도록 범위를 맞춰주면 된다. -> 보통 첫번째만 맞추고 전체범위를 범위로 넣는것이 관례이다.

## 1-7. 축 표시형식
> 월의 jan이렇게 하려면 mmm을 지정하면 된다. 요일 영어는 ddd이고 한글은 aaa이다. -> 서식코드 추가에서 서식을 추가하여 사용한다.
> 또한 축에서 표시형식을 볼때 단위로 구분한 것과 , 표시등의 서식을 유의해서 봐야한다.
> 또한 한 레이블만 선택하려면 더블클릭하면 된다.

## 1-8. 기본 주 세로 눈금선
> 이건 그냥 차트요소추가에서 눈금선으로 추가한다.

## 1-9. 프로시적에서 목록에 값넣기
> 목록에 값을 넣을 때는 목록컨트롤명.additem 하면 된다.

## 1-10. match에서 한 셀에 값이 문화/공용 이렇게 있을때 해결법
> 문자가 한셀에 저렇게 붙어있을때야말로 match함수 2번째 인자를 -1로 주어서 근사값으로 처리할 수 있도록 해야하는 것이다. 이때 1이나 -1를 판단하는 기준은 문자의 배치가 한글 내림차순인지 오름차순인지 판단해야만 하는 것이다.

> 만약 예술, 문화/공연이 있다면 원리는 ㄱㄴㄷㄹㅁㅂㅅ 순서에서 ㄱ 과 ㅁ을 지나 ㅇ이 나오기 전까지가 문화/공연에 해당하게 되는 것이다.