# 📌 1. 네트워크 · 보안
## 1-1. 원격접속·통신 프로토콜·계층
1) 원격 호스트에 안전하게 로그인·명령 실행을 제공하는 암호화/무결성/인증 프로토콜(포트 22) – **SSH (Secure Shell)**
2) 같은 브로드캐스트 도메인에서 IP→MAC을 질의/응답으로 해석 – **ARP (Address Resolution Protocol)**
3) 브로커 중심 발행·구독 경량 메시징(IoT, QoS 0/1/2) – **MQTT (Message Queuing Telemetry Transport)**
4) 경로·논리 주소·라우팅 담당 계층 – **OSI 7계층(네트워크 계층) (OSI Layer 3, Network Layer)**

## 1-2. 공격·위협·기만
5) 출발지=목적지 위조 패킷으로 자원 고갈 유발 – **LAND 공격 (Local Area Network Denial attack)**
6) 분산된 대량 트래픽으로 서비스 불능 – **DDoS (Distributed Denial of Service)**
7) MAC 테이블 오염으로 스위치를 허브처럼 만듦 – **스위치 재밍 (Switch MAC Table Flooding)**
8) 공격자 유인·관찰용 기만 시스템 – **Honeypot (Honeypot)**

## 1-3. 접근 통제·보안 속성
9) 데몬 접근을 IP/도메인 기준 제어하는 래핑 유틸 – **tcp wrapper (TCP Wrapper)**
10) 사용자가 주장하는 신원을 증명 – **인증 (Authentication)**
11) 데이터의 비인가 변경 방지/탐지 – **무결성 (Integrity)**
12) 정보의 비인가 열람 방지 – **기밀성 (Confidentiality)**
13) 요구 시 자원의 이용 가능성 보장 – **가용성 (Availability)**

# 📌 2. 소프트웨어 공학 · 프로세스
## 2-1. 생명주기(프로세스) 모델
1) 요구→설계→구현→시험 순차 진행 – **Waterfall Model (Waterfall Model)**
2) 반복마다 위험 분석·프로토타이핑 – **Spiral Model (Spiral Model)**
3) 시제품으로 요구 확인·리스크 저감 – **Prototype Model (Prototype Model)**

## 2-2. 비용·일정 추정·관리
4) 규모·유형별 상수로 노력/기간 산정 – **COCOMO (Constructive Cost Model)**
5) 레일리 곡선 기반 노력/일정 추정 – **Putnam (SLIM/Putnam Model)**
6) 사용자 기능 수 기반 규모 측정 – **Function Point (Function Point Analysis)**
7) 3점 추정·확률 경로 일정기법 – **PERT (Program Evaluation and Review Technique)**
8) 코드 라인 수 기반 추정 – **LOC 기법 (Lines of Code method)**
9) 위험 식별·분석·대응 – **Risk Analysis (Risk Analysis)**

## 2-3. 품질·요구 기법
10) 형식적 동료 검토 – **FTR (Formal Technical Review)**
11) 실행 없이 읽기·체크리스트로 결함 탐지 – **인스펙션 (Inspection)**
12) 5대 가치와 짧은 반복 실천 – **XP (eXtreme Programming)**
13) 고객 가치 단위의 요구 카드 – **사용자 스토리 (User Story)**
14) 기술/설계 불확실성 검증 실험 – **스파이크 (Spike)**
15) 구조적 논리 표현 설계도 – **NS Chart (Nassi–Shneiderman Chart)**

## 2-4. 모듈성 지표·경험칙
16) 한 모듈을 호출하는 상위 모듈 수 – **Fan-in (Fan-in)**
17) 한 모듈이 호출하는 하위 모듈 수 – **Fan-out (Fan-out)**
18) 모듈 간 의존 강도 – **결합도 (Coupling)**
19) 모듈 내부 책임 집중도 – **응집도 (Cohesion)**
20) 소수 원인이 다수 결과를 만든다는 경험칙 – **Pareto의 법칙 (Pareto Principle, 80/20 rule)**

# 📌 3. 설계 패턴 · UML · UI
## 3-1. 생성 패턴
1) 전역 인스턴스 하나만 보장 – **Singleton (Singleton Pattern)**
2) 원형 객체 복제로 생성 – **Prototype (Prototype Pattern)**
3) 생성 책임을 서브클래스에 위임 – **Factory Method (Factory Method Pattern)**

## 3-2. 구조 패턴
4) 추상과 구현의 분리로 독립 확장 – **Bridge (Bridge Pattern)**

## 3-3. UML 핵심 개념
5) 구조/행동/주해 등 UML 요소 – **UML 사물 (UML Things: Structural/Behavioral/Annotation)**

## 3-4. UI 유형·산출물
6) 자연 동작 기반 상호작용 – **NUI (Natural User Interface)**
7) 그래픽 위젯 기반 인터페이스 – **GUI (Graphical User Interface)**
8) 텍스트 명령 기반 인터페이스 – **CLI (Command-Line Interface)**
9) 실제와 유사한 정적 화면 견본 – **목업 (Mockup)**
10) 화면 흐름/콘텐츠 설계 문서 – **스토리보드 (Storyboard)**
11) 작동 가능한 초기 구현물 – **프로토타입 (Prototype)**

# 📌 4. 데이터베이스 · SQL · 모델링
## 4-1. 권한·객체
1) 권한 회수 – **REVOKE (REVOKE)**
2) 권한 부여 – **GRANT (GRANT)**
3) 논리적 가상 테이블 – **VIEW (View)**

## 4-2. 연산·관계대수·집계
4) 공통 속성 결합 – **조인 (Join, ⋈)**
5) 조건으로 행 선택 – **선택 (Selection, σ)**
6) 지정 열만 남김 – **투영 (Projection, π)**
7) 두 릴레이션 모든 튜플 조합 – **카티션 프로덕트 (Cartesian Product)**
8) 그룹화 기준 – **GROUP BY (GROUP BY)**
9) 그룹 결과 필터 – **HAVING (HAVING)**

## 4-3. 정규화·무결성·트랜잭션(ACID)
10) 부분/이행 종속 제거 정규형 – **제3정규형 (3NF, Third Normal Form)**
11) 모든 결정자가 후보키인 정규형 – **BCNF (Boyce–Codd Normal Form)**
12) 기본키 NULL/중복 금지 – **개체 무결성 (Entity Integrity)**
13) 전부 반영/전혀 반영 – **원자성 (Atomicity)**
14) 제약 유지 – **일관성 (Consistency)**
15) 동시 간섭 최소화 – **격리성 (Isolation)**
16) 커밋 결과 영구 보존 – **지속성 (Durability)**

## 4-4. 저장·구조·속성
17) 서버 직접 연결 저장장치 – **DAS (Direct-Attached Storage)**
18) 행 수 – **Cardinality (Cardinality)**
19) 열 수 – **Degree (Degree)**
20) 값이 여러 개인 속성 표기 – **다중값 속성 (Multivalued Attribute, Double Ellipse)**

## 4-5. SQL 구문 분류·조건
21) 데이터 조작어 – **DML (Data Manipulation Language)**
22) 데이터 정의어 – **DDL (Data Definition Language)**
23) 컬럼=값 갱신 절 – **UPDATE … SET (UPDATE … SET)**
24) 널이 아닌 행 조건 – **IS NOT NULL (IS NOT NULL)**

# 📌 5. 자료구조 · 알고리즘 · 테스트
## 5-1. 자료구조·표현
1) n×n 행렬로 연결 표현 – **인접 행렬 (Adjacency Matrix)**
2) 한쪽 끝에서만 삽입/삭제 – **스택 (Stack, LIFO: Last-In, First-Out)**
3) 양끝 분리 삽입/삭제 – **큐 (Queue, FIFO: First-In, First-Out)**
4) 부모–자식 비선형 구조 – **트리 (Tree)**
5) 정점/간선 관계 구조 – **그래프 (Graph)**

## 5-2. 정렬
6) 현재 원소를 적소에 삽입(O(n²)) – **삽입 정렬 (Insertion Sort)**
7) 피벗 분할 후 재귀 정렬 – **퀵 정렬 (Quick Sort)**

## 5-3. 테스트 기법
8) 명세 기반 시험 – **블랙박스 테스트 (Black-box Testing)**
9) 경계 근처 입력 설계 – **경계값 분석 (Boundary Value Analysis)**
10) 입력의 등가 클래스 대표값 시험 – **동등 분할 (Equivalence Partitioning)**
11) 경험/패턴 기반 취약 영역 예측 – **오류 예측 (Error Guessing)**
12) 무작위 입력으로 결함 탐지 – **랜덤 테스트 (Random Testing)**

# 📌 6. 운영체제 · 컴퓨터 구조
## 6-1. 메모리/할당·교체
1) 가장 작은 맞춤 공간에 적재 – **Best Fit (Best-Fit Allocation)**
2) 가장 큰 빈 공간에 적재 – **Worst Fit (Worst-Fit Allocation)**
3) 첫 번째로 맞는 공간에 적재 – **First Fit (First-Fit Allocation)**
4) 참조 비트로 한 번 더 기회 – **Second-Chance 알고리즘 (Clock/Second-Chance Page Replacement)**

## 6-2. 프로세스/스케줄링
5) 프로세스 상태/문맥/자원 정보 블록 – **PCB (Process Control Block)**
6) 응답비율이 큰 작업 우선 – **HRN 스케줄링 (Highest Response Ratio Next)**
7) 가장 짧은 작업 우선 – **SJF 스케줄링 (Shortest Job First)**

## 6-3. 주소 변환·교착 회피
8) 세그먼트 시작주소+오프셋 변환 – **세그먼트 테이블 (Segment Table)**
9) 안전 상태만 허용하는 교착 회피 – **은행가 알고리즘 (Banker’s Algorithm)**

# 📌 7. 프로그래밍 언어(문법·식별자)
## 7-1. 연산자·타입
1) 하나라도 참이면 참 – **논리합 연산자 (Logical OR, ||)**
2) 참/거짓 자료형 – **boolean (Boolean Type)**

## 7-2. 접근·식별자·단위
3) 멤버 접근 범위 지정자 – **접근제어자 (Access Modifiers: public/default/protected/private)**
4) 식별자 작성 규칙 – **변수명 제약 (Identifier Naming Rules)**
5) 4비트(half-byte) – **nibble (Nibble)**

# 📌 8. 정보시스템 구축 · 신기술 · 클라우드
## 8-1. SDx/가상화
1) 자원을 소프트웨어로 정의/자동화한 데이터센터 – **SDDC (Software-Defined Data Center)**
2) 스토리지 가상화·풀링·정책 제공 – **SDS (Software-Defined Storage)**

## 8-2. 분산·빅데이터/ML
3) 분할(Map)→집계(Reduce) 분산 처리 – **MapReduce (MapReduce)**
4) 수치연산/자동미분/딥러닝 프레임워크 – **TensorFlow (TensorFlow)**

## 8-3. 클라우드 서비스·플랫폼
5) 국내 오픈 PaaS 표준 플랫폼 – **PaaS-TA (Platform as a Service–TA)**
6) 클라우드 서비스 분류 – **IaaS (Infrastructure as a Service) / PaaS (Platform as a Service) / SaaS (Software as a Service)**

## 8-4. 기타 신기술 용어
7) 발표만 되고 실제 배포는 안 된 소프트웨어 – **Vaporware (Vaporware)**
8) 현실과 디지털이 융합된 3D 가상 세계 – **Metaverse (Metaverse)**

# 📌 9. 생성(Creational) 패턴 (Java)

> 한줄정의: **객체 생성의 책임과 시점을 유연하게 분리해 결합도를 낮추는 패턴군**  
> 아이디어: 생성 과정을 감추거나 지연/대체/표준화하여 **확장 가능성과 테스트 용이성**을 확보한다.

---

## 9-1. Abstract Factory
- **한줄정의:** 관련된 **제품군**을 생성하는 인터페이스를 제공하되, 구상 클래스를 감춘다.
- **아이디어:** 클라이언트는 팩토리 인터페이스만 알고, 서로 호환되는 제품 세트를 일관되게 만든다.
```java
interface Button { String skin(); }
interface Window { String skin(); }

interface WidgetFactory {
  Button createButton();
  Window createWindow();
}

class MacFactory implements WidgetFactory {
  public Button createButton() { return () -> "mac"; }
  public Window createWindow() { return () -> "mac"; }
}
class WinFactory implements WidgetFactory {
  public Button createButton() { return () -> "win"; }
  public Window createWindow() { return () -> "win"; }
}

class App {
  App(WidgetFactory f) {
    System.out.println(f.createButton().skin() + " / " + f.createWindow().skin());
  }
}
```

## 9-2. Builder
- **한줄정의:** 복잡한 객체의 **조립 절차**를 분리해, **같은 절차로 다른 표현**을 만든다.
- **아이디어:** 불변/가독성/선택적 필드에 유리한 체이닝 API.
```java
class HttpReq {
  final String method, path; final boolean keep;
  private HttpReq(Builder b){ this.method=b.m; this.path=b.p; this.keep=b.k; }
  static class Builder {
    String m="GET", p="/"; boolean k;
    Builder method(String v){ m=v; return this; }
    Builder path(String v){ p=v; return this; }
    Builder keepAlive(boolean v){ k=v; return this; }
    HttpReq build(){ return new HttpReq(this); }
  }
}
```

## 9-3. Factory Method
- **한줄정의:** 객체 생성을 **하위 클래스**(또는 전달된 전략)에 위임한다.
- **아이디어:** 상위는 생성 타이밍만 정하고, 구상 타입 선택은 하위가 담당.
```java
abstract class Creator {
  abstract Product create();            // 팩토리 메서드
  Product order(){ return create(); }   // 고정 골격
}
interface Product { String name(); }
class A implements Product { public String name(){ return "A"; } }
class ACreator extends Creator { Product create(){ return new A(); } }
```

## 9-4. Prototype
- **한줄정의:** 원형 객체를 **복제(clone)**하여 새 객체를 만든다.
- **아이디어:** 생성 비용이 큰 객체, 런타임에 새로운 “타입”을 등록/생성.
```java
class Enemy implements Cloneable {
  int hp; String type;
  Enemy(int hp, String type){ this.hp=hp; this.type=type; }
  public Enemy clone(){ try { return (Enemy)super.clone(); } catch(CloneNotSupportedException e){ throw new AssertionError(); } }
}
```

## 9-5. Singleton
- **한줄정의:** 시스템 전역에 **하나의 인스턴스**만 보장한다.
- **아이디어:** private 생성자 + 정적 접근자(스레드 안전 방식 권장).
```java
class Logger {
  private Logger(){}
  private static class Holder { static final Logger I = new Logger(); }
  public static Logger get(){ return Holder.I; }
}
```

---

# 📌 10. 구조(Structural) 패턴 (Java)

> 한줄정의: **클래스/객체의 조합 방식을 바꿔 인터페이스 호환, 책임 분리, 재사용을 높이는 패턴군**  
> 아이디어: 합성/위임으로 유연성을 확보하고 상속 남용을 줄인다.

---

## 10-1. Adapter
- **한줄정의:** **호환되지 않는 인터페이스**를 변환하는 중간 래퍼.
- **아이디어:** 기대 인터페이스(Lightning)를 구현하고, 실제 대상(TypeC)에 위임.
```java
interface Lightning { void charge(); }
class TypeC { void cCharge(){ System.out.println("Type-C 25W!"); } }
class LightningToTypeCAdapter implements Lightning {
  private final TypeC c;
  LightningToTypeCAdapter(TypeC c){ this.c=c; }
  public void charge(){ c.cCharge(); }
}
```

## 10-2. Bridge
- **한줄정의:** **추상**과 **구현**을 분리해 독립적으로 확장한다.
- **아이디어:** Abstraction이 Implementor를 합성.
```java
interface Renderer { void circle(int x,int y,int r); }
class Raster implements Renderer { public void circle(int x,int y,int r){ System.out.println("Raster"); } }
class Vector implements Renderer { public void circle(int x,int y,int r){ System.out.println("Vector"); } }
class Circle {
  private final Renderer r; int x,y,rad;
  Circle(Renderer r,int x,int y,int rad){ this.r=r; this.x=x; this.y=y; this.rad=rad; }
  void draw(){ r.circle(x,y,rad); }
}
```

## 10-3. Composite
- **한줄정의:** **부분-전체**를 동일 인터페이스로 다루는 트리 구조.
- **아이디어:** Component에 Leaf/Composite가 공존.
```java
import java.util.*;
interface Node { void render(); }
class Leaf implements Node { public void render(){ System.out.println("Leaf"); } }
class Group implements Node {
  private final List<Node> children=new ArrayList<>();
  void add(Node n){ children.add(n); }
  public void render(){ System.out.println("Group{"); children.forEach(Node::render); System.out.println("}"); }
}
```

## 10-4. Decorator
- **한줄정의:** 객체를 **감싸서 기능을 동적으로 덧붙인다**.
- **아이디어:** 동일 인터페이스 구현 + 내부에 원 객체 보관.
```java
interface View { void draw(); }
class TextView implements View { public void draw(){ System.out.println("Text"); } }
class BorderDecorator implements View {
  private final View inner;
  BorderDecorator(View v){ inner=v; }
  public void draw(){ inner.draw(); System.out.println("+Border"); }
}
```

## 10-5. Facade
- **한줄정의:** 복잡한 서브시스템에 **단순 통합 인터페이스** 제공.
- **아이디어:** 워크플로우만 조정하고 내부 세부를 감춤.
```java
class Audio { void on(){ System.out.println("Audio"); } }
class Net { void connect(){ System.out.println("Net"); } }
class Scene { void load(){ System.out.println("Scene"); } }
class GameFacade {
  private final Audio a=new Audio(); private final Net n=new Net(); private final Scene s=new Scene();
  void start(){ a.on(); n.connect(); s.load(); System.out.println("Start!"); }
}
```

## 10-6. Flyweight
- **한줄정의:** **공유 가능한 내재 상태**를 캐시해 메모리를 절감.
- **아이디어:** intrinsic(공유)/extrinsic(호출 시 전달) 분리.
```java
class Glyph { final char ch; Glyph(char c){ ch=c; } void draw(int x,int y){ System.out.println(ch+"@"+x+","+y); } }
class GlyphFactory {
  private static final java.util.Map<Character,Glyph> pool = new java.util.HashMap<>();
  static Glyph get(char c){ return pool.computeIfAbsent(c, Glyph::new); }
}
```

## 10-7. Proxy
- **한줄정의:** 동일 인터페이스로 **접근을 가로채** 권한/캐시/지연 로딩 수행.
- **아이디어:** Subject 인터페이스 동일, 내부에 RealSubject 합성.
```java
interface Subject { void fetch(); }
class RealSubject implements Subject { public void fetch(){ System.out.println("Heavy DB"); } }
class AuthProxy implements Subject {
  private final Subject real; private final boolean allowed;
  AuthProxy(Subject real, boolean allowed){ this.real=real; this.allowed=allowed; }
  public void fetch(){ if(!allowed) System.out.println("DENY"); else real.fetch(); }
}
```

---

# 📌 11. 행위(Behavioral) 패턴 (Java)

> 한줄정의: **책임 분배와 상호작용의 규칙**을 정의해 유연성과 응집도를 높이는 패턴군  
> 아이디어: 조건·분기·콜백·프로토콜을 객체 간 계약으로 바꾸어 변화에 강하게 만든다.

---

## 11-1. Chain of Responsibility
- **한줄정의:** 요청을 **연결된 처리자**들에 넘겨 적절한 곳에서 처리.
- **아이디어:** 핸들러가 다음 핸들러를 참조.
```java
abstract class Handler {
  private Handler next;
  Handler link(Handler n){ this.next=n; return n; }
  final boolean handle(int v){ return exec(v) || (next!=null && next.handle(v)); }
  abstract boolean exec(int v);
}
class Limit10 extends Handler { boolean exec(int v){ if(v<=10){ System.out.println("<=10"); return true;} return false; } }
class Limit20 extends Handler { boolean exec(int v){ if(v<=20){ System.out.println("<=20"); return true;} return false; } }
```

## 11-2. Command
- **한줄정의:** 요청을 **객체로 캡슐화**하여 실행/큐/Undo를 표준화.
- **아이디어:** Command.execute()에 동작을 모은다.
```java
interface Command { void exec(); }
class Light { boolean on; void on(){ on=true; System.out.println("ON"); } }
class LightOn implements Command {
  private final Light l; LightOn(Light l){ this.l=l; }
  public void exec(){ l.on(); }
}
```

## 11-3. Interpreter
- **한줄정의:** **문법 규칙**을 클래스로 모델링해 해석한다.
- **아이디어:** AST 각 노드가 interpret를 구현.
```java
interface Expr { int eval(); }
record Num(int v) implements Expr { public int eval(){ return v; } }
record Add(Expr a, Expr b) implements Expr { public int eval(){ return a.eval()+b.eval(); } }
```

## 11-4. Iterator
- **한줄정의:** 내부 표현을 노출하지 않고 **표준 순회** 제공.
- **아이디어:** hasNext/next 계약.
```java
import java.util.Iterator;
class ArrIter implements Iterator<Integer> {
  private final int[] a; private int i=0;
  ArrIter(int[] a){ this.a=a; }
  public boolean hasNext(){ return i<a.length; }
  public Integer next(){ return a[i++]; }
}
```

## 11-5. Mediator
- **한줄정의:** 객체 간 상호작용을 **중재자**에게 위임해 N:N 의존을 1:N으로.
- **아이디어:** 동료들은 Mediator만 참조.
```java
interface Mediator { void notify(String who, String ev); }
class Button { private final Mediator m; Button(Mediator m){ this.m=m; } void click(){ m.notify("Button","click"); } }
```

## 11-6. Memento
- **한줄정의:** 캡슐화 유지하며 **상태 스냅샷**을 저장/복원.
- **아이디어:** Originator/ Memento/ Caretaker 분리.
```java
class Player { int x,y; Snapshot save(){ return new Snapshot(x,y); } void load(Snapshot s){ x=s.x; y=s.y; } }
record Snapshot(int x,int y){}
```

## 11-7. Observer
- **한줄정의:** **발행-구독** 모델로 상태 변화 통지.
- **아이디어:** Subject가 구독자를 목록으로 관리하고 notify.
```java
import java.util.*;
interface Observer { void update(int t); }
class Thermometer {
  private final List<Observer> obs = new ArrayList<>(); private int t;
  void sub(Observer o){ obs.add(o); }
  void setTemp(int nt){ t=nt; obs.forEach(o->o.update(t)); }
}
```

## 11-8. State
- **한줄정의:** 상태에 따라 **행위를 교체**(거대한 if-else 제거).
- **아이디어:** Context가 현재 State에 위임하고 교체.
```java
interface DoorState { void push(Door ctx); }
class Door {
  DoorState st = new Closed();
  void push(){ st.push(this); }
  void set(DoorState s){ st=s; }
}
class Closed implements DoorState { public void push(Door c){ System.out.println("Open"); c.set(new Open()); } }
class Open implements DoorState { public void push(Door c){ System.out.println("Close"); c.set(new Closed()); } }
```

## 11-9. Strategy
- **한줄정의:** 알고리즘을 **교체 가능한 전략 객체**로 캡슐화.
- **아이디어:** Context가 전략을 합성.
```java
interface Pay { int doPay(int amt); }
class Card implements Pay { public int doPay(int a){ System.out.println("Card"); return a; } }
class Cash implements Pay { public int doPay(int a){ System.out.println("Cash"); return a; } }
class Checkout { Pay p; Checkout(Pay p){ this.p=p; } int pay(int a){ return p.doPay(a); } }
```

## 11-10. Template Method
- **한줄정의:** 알고리즘의 **골격**을 정의하고 세부는 하위에서 구현.
- **아이디어:** final 템플릿 + 훅 메서드.
```java
abstract class Algo {
  public final void run(){ System.out.println("Start"); step1(); step2(); System.out.println("End"); }
  protected abstract void step1();
  protected abstract void step2();
}
```

## 11-11. Visitor
- **한줄정의:** **구조는 고정**하고, 새로운 연산을 **방문자**로 추가.
- **아이디어:** double-dispatch(`accept/visit`).
```java
interface Visitor { void visit(Circle c); void visit(Rect r); }
interface Element { void accept(Visitor v); }
class Circle implements Element { int r; public void accept(Visitor v){ v.visit(this); } }
class Rect implements Element { int w,h; public void accept(Visitor v){ v.visit(this); } }
```

