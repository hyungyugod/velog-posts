# 📌 1. 3회 기출
## 1-1. 1유형
- df = df.dropna()는 결측치가 하나라도 있는 행을 전부 제거한다.
```py
df = df.dropna()
df70 = df.iloc[:int(len(df)*0.7), :]
print(df70['f1'].quantile(.25)) # 57.0
```
- 만약 행에 있는 조건을 열로 옮기는게 편한 상황이면 전치를 한다. 이때 dft.colums로 확인해보면 데이터타입이 int일 수 있어 칼럼을 선택할때 주의해야한다.
- df[숫자]는 기본적으로 칼럼이름으로 호출한다.
- df = df.T 이러면 실행할때마다 전치되어서 결과에 오류가 생길 수 있다.
- 전치를 하지 않고 m = df.loc[2000].mean() 이렇게 하면 바로 행이름이 2000인 행을 선택할 수 있어 이렇게 풀어나가도 좋다.
```py
dft = df.T
print(sum(dft[2000] > dft[2000].mean())) # 100

m = df.loc[2000].mean()
```
- 결측치가 가장 많은 행은 그냥 isna().sum()을 이용한다.
```py
df.isna().sum() # f1
```